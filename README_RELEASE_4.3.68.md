# Release 4.3.68

Personnel schema finalized: `position` is used for سمت; removed `personnel_type`, `phone`, `collaboration_start`, and `contract_end_date`. Excel import accepts `تاریخ شروع همکاری` as a legacy alias for `hire_date`.
