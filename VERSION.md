# Powerline Web — v4.3.51 (2026-09-02)

## اصلاحات این نسخه

### ۱) رفع خطای client-side در «تغییر گروهی قرارداد» دکل‌ها
- بلوک پیشرفت داخل دیالوگ دکل‌ها به فیلدهای ناموجود (`progress.batch` / `progress.totalBatches`) اشاره می‌کرد و هنگام اجرای عملیات کل اپ کرش می‌کرد («Application error: a client-side exception»).
- دیالوگ انتخاب مقدار حالا هنگام شروع عملیات گروهی بسته می‌شود (مثل بخش خطوط) تا دو Dialog رادیکس هم‌زمان باز نمانند. پیشرفت واقعی در پنجره «تأیید عملیات گروهی» نمایش داده می‌شود.

### ۲) انتخاب «نامشخص» در تغییر گروهی قرارداد
- دکل‌ها و ماژول‌های عمومی (بازدید، دستورکار، دیفکت، پرسنل، تجهیزات، حوادث...): انتخاب «نامشخص» حالا قرارداد ردیف‌ها را NULL می‌کند (پاک شدن قرارداد) و دکمه «اعمال روی همه» فعال می‌ماند.
- بک‌اند این مقدار NULL را برای همه PUT ها / bulk-update می‌پذیرد (array_key_exists).

### ۳) رفع «اتصال گروهی دکل‌ها به خط»
- فیلد `line_id` به allowedFields مسیر `towers/bulk-update` اضافه شد + اعتبارسنجی وجود خط و همگام‌سازی ساختار خطوط مبدأ/مقصد.

### ۴) رفع دکمه «کپی برای اکسل»
- پیام «کپی شد» حالا به‌صورت متمرکز در DataTable نمایش داده می‌شود (در همه صفحات یکسان)؛ اگر ردیفی برای کپی نباشد یا مرورگر اجازه کپی ندهد پیام مناسب نشان داده می‌شود. توست‌های تکراری صفحات حذف شدند.

### ۵) بک‌اند ماژول‌های عمومی
- `PUT /invoices/{id}` اضافه شد (ویرایش تکی و تغییر گروهی قرارداد صورت‌وضعیت‌ها + محاسبه مجدد مالیات/مبلغ نهایی).
- `PUT /equipment/{id}` حالا فیلد `contract_id` را می‌پذیرد (قبلاً تغییر گروهی قرارداد تجهیزات خطای «هیچ فیلدی ارسال نشده» می‌داد).

## فایل‌های تغییریافته
- src/components/towers/bulk-towers-actions.tsx
- src/components/generic-bulk-actions.tsx
- src/components/data-table.tsx
- src/lib/version.ts ، package.json (bump به v4.3.51)
- Powerline/endpoints/towers.php
- Powerline/endpoints/modules.php

## نکته استقرار
- بعد از آپلود، فایل‌های PHP بک‌اند (towers.php و modules.php) هم باید روی هاست جایگزین شوند.
