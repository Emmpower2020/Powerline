# Powerline Web — v4.3.71 (2026-09-02)

## اصلاحات
۱) قالب دانلود ایمپورت پرسنل: عنوان ستون «پست» → «سمت» (هر دو کلمه در ایمپورت پذیرفته می‌شود).
۲) ذخیره سمت پرسنل: ستون position از جدول personnel دیتابیس لایو حذف شده بود —
   مایگریشن `database/migration_v4.3.71_add_personnel_position.sql` اضافه شد (یک خط ALTER).

## تست‌شده روی دیتابیس واقعی (لوکال)
- ایمپورت با ستون سمت قبل از مایگریشن → سمت نادیده گرفته می‌شد (بدون خطا)
- بعد از مایگریشن → سمت ذخیره شد ✓

## استقرار
1. سورس فرانت‌اند (zip) در Vercel — سایدبار باید v4.3.71 نشان دهد
2. `Powerline/endpoints/modules.php` بدون تغییر (همان v4.3.70)
3. اجرای SQL مایگریشن در phpMyAdmin (یک خط)
