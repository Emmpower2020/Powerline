# Powerline EAM/CMMS - Web Frontend

## نسخه فعلی: v1.3.0
**تاریخ:** 2026-08-20  
**نوع تغییر:** MINOR — انتقال سورت به داخل پنجره فیلتر + حذف آیکون سورت از هدر

---

## سیستم نسخه‌بندی (Semantic Versioning)

| قسمت | چه زمانی افزایش می‌یابد | مثال |
|------|--------------------------|------|
| **MAJOR** | تغییرات بزرگ: ماژول جدید، تغییر معماری، تغییرات ناسازگار | v2.0.0 |
| **MINOR** | رفع چند اشکال، بهبود UI/UX، افزودن قابلیت کوچک | v1.1.0 |
| **PATCH** | رفع یک اشکال جزئی، اصلاح متن یا استایل | v1.0.1 |

---

## تاریخچه نسخه‌ها

### v1.3.0 — 2026-08-20
**نوع:** MINOR — انتقال سورت به داخل پنجره فیلتر + حذف آیکون سورت از هدر

#### 🔄 تغییرات:

**۱) حذف آیکون سورت از هدر ستون‌ها**
- **مشکل:** آیکون سورت قبل از نام ستون قرار داشت و باعث می‌شد نام هدر و فیلدها در یک راستا قرار نگیرند (نا فرم بود).
- **راه‌حل:** حذف کامل دکمه سورت از هدر. حالا فقط نام ستون نمایش داده می‌شود و همه هدرها در یک راستا هستند.

**۲) انتقال گزینه‌های سورت به داخل پنجره فیلتر**
- **قابلیت جدید:** در پنجره فیلتر (که با کلیک روی آیکون قیف باز می‌شود)، یک بخش "مرتب‌سازی" اضافه شد با ۳ دکمه:
  - **صعودی** (آیکون ↑) — مرتب‌سازی از کوچک به بزرگ
  - **نزولی** (آیکون ↓) — مرتب‌سازی از بزرگ به کوچک
  - **حذف** (آیکون ⇅) — حذف مرتب‌سازی
- دکمه فعال شده با رنگ بنفش (`bg-indigo-600 text-white`) متمایز می‌شود
- فقط ستون‌هایی که `sortable: true` دارند، این بخش را نمایش می‌دهند

**۳) بهبور آیکون فیلتر**
- حالا وقتی ستون سورت شده یا فیلتر شده باشد، آیکون قیف بنفش می‌شود (تا کاربر ببیند این ستون فعال است)
- title آیکون از "فیلتر" به "فیلتر و سورت" تغییر کرد

**۴) بهبور دکمه "پاک کردن"**
- حالا هم فیلتر و هم سورت را همزمان پاک می‌کند (قبلاً فقط فیلتر را پاک می‌کرد)

#### 🛠️ فایل‌های تغییر یافته:
- `src/components/data-table.tsx`:
  - حذف `{col.sortable && <button>...</button>}` از هدر
  - افزودن بخش سورت به داخل `DropdownMenuContent`
  - به‌روزرسانی `className` آیکون فیلتر برای تشخیص سورت فعال
  - به‌روزرسانی `onClick` دکمه "پاک کردن"

#### 📋 دستورالعمل اعمال:
```bash
# ۱) آپلود کدهای frontend جدید (build و deploy)
# ۲) ریفرش مرورگر با Ctrl+Shift+R
```

#### 💡 نحوه استفاده:
1. روی آیکون قیف کنار هر ستون کلیک کنید
2. در بخش "مرتب‌سازی"، یکی از گزینه‌های صعودی/نزولی را انتخاب کنید
3. اگر می‌خواهید فیلتر هم اعمال کنید، مقادیر را تیک بزنید و "اعمال فیلتر" را بزنید
4. برای حذف سورت، روی "حذف" کلیک کنید یا از دکمه "پاک کردن" بالای پنجره استفاده کنید

---

### v1.2.9 — 2026-08-20
**نوع:** PATCH — راست‌چین کردن همه ستون‌های جدول

- تنظیم `align: "right"` برای همه ۲۵ ستون
- به‌روزرسانی منطق ترازبندی در `th` و `td` (پیش‌فرض `text-right`)
- حذف منطق قدیمی که `type === "number"` را به‌طور خودکار چپ‌چین می‌کرد

---

### v1.2.8 — 2026-08-20
- کاهش عرض نام خط از 360px به 340px

---

### v1.2.7 — 2026-08-20
- کاهش عرض ستون نام خط از 560px به 360px

---

### v1.2.6 — 2026-08-20
- افزودن کلاس `table-fixed` به المان `<table>` (حل ریشه‌ای مشکل عرض)

---

### v1.2.5 — 2026-08-20
1. کاهش padding از p-3 به p-2
2. کاهش cap ستون‌های کوتاه از 400px به 220px
3. اصلاح کار نکردن جستجوی داخل فیلتر با getFilteredUniqueValues
4. افزودن autoFocus و پیام "موردی یافت نشد"

---

### v1.2.4 — 2026-08-20
1. تغییر ترتیب فیلدها در فرم
2. تغییر نام "نام مجموعه" به "نام مجموعه خط"

---

### v1.2.3 — 2026-08-20
1. اصلاح چیدمان فرم: نام خط و نام مجموعه به تمام عرض
2. پاک کردن error هنگام باز کردن مجدد فرم
3. افزودن تابع `getColumnWidth`
4. افزودن try-catch برای خطای UNIQUE Constraint

---

### v1.2.2 — 2026-08-20
1. رفع اشتباه UNIQUE INDEX روی dispatch_code
2. اصلاح دکمه close
3. افزودن filterable به ستون‌های عددی
4. قابلیت wrap متن

---

### v1.2.1 — 2026-08-20
1. حل خطای duplicate dispatch_code
2. حذف پیام soft delete
3. clear خودکار selectedRows
4. حذف فیلدهای تاریخ از فرم
5. دکمه ویرایش
6. پشتیبانی از حالت ویرایش

---

### v1.2.0 — 2026-08-20
1. تبدیل soft delete به HARD DELETE
2. حذف ستون‌های تاریخ
3. انتقال دکمه close به چپ
4. راست‌چین DialogHeader و DialogFooter
5. تغییر متن "کل دکل‌ها" به "تعداد کل دکل‌ها"
6. بک‌گراند هدر سفید
7. cursor: pointer با !important

---

### v1.1.0 — 2026-08-20
1. ستون چک‌باکس جداگانه
2. badge "X ردیف انتخاب شده"
3. اصلاح دکمه حذف
4. کپی به فرمت TSV

---

### v1.0.1 — 2026-08-20
بازنویسی `formatLineRow()` در PHP

---

### v1.0.0 — 2026-08-20
نسخه پایه

---

## راهنمای نصب

```bash
npm install
npm run dev        # محیط توسعه
npm run build      # بیلد production
npm run start      # اجرای production
```

## پیکربندی

- آدرس API: در `src/lib/api-config.ts` متغیر `API_BASE_URL`
- proxy داخلی: `src/app/api/proxy/[...path]/route.ts` برای حل CORS
- احراز هویت: JWT با refresh token در `src/lib/auth-context.tsx`

## فایل‌های مرجع
- `CHECKLIST.md` — لیست کامل کارهای انجام‌شده و باقی‌مانده
- `VERSION.md` — همین فایل — تاریخچه نسخه‌ها
