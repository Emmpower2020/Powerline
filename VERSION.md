# Powerline Web — v4.3.78 (2026-09-03)

## ۱) امور بهره‌برداری (قابلیت جدید)
- جدول جدید `districts` در داده‌های پایه برای تعریف امورهای مختلف (کردستان، ایلام، کرمانشاه غربی، کرمانشاه شرقی)
- فیلد «امور بهره‌برداری» در فرم‌های ثبت/ویرایش/کپی: مدارها، خطوط، دکل‌ها، پرسنل، تجهیزات، بازدیدها، عیوب، دستورکارها، صورت‌وضعیت‌ها، حوادث ایمنی/شخصی و حوادث خطوط + نقشه‌ها (خودکار)
- ستون «امور بهره‌برداری» در همهٔ جدول‌های بالا + کاربران
- کاربر اموردار فقط داده‌های امور خودش را می‌بیند؛ مدیر برنامه (بدون امور) همه را می‌بیند
- صفحهٔ کاربران: ویرایش امور و وضعیت هر کاربر + نمایش امور روی کارت
- پیمانکار و قرارداد عمداً امور نمی‌گیرند (یک قرارداد می‌تواند چند امور را پوشش دهد)
- اجرای فایل `Powerline_v4.3.78_Migration.sql` روی دیتابیس الزامی است

## ۲) استانداردسازی ستون وضعیت (فعال/غیرفعال)
- مدارها: ستون وضعیت جدید (قبلاً نداشت) + فیلتر + ویرایش گروهی
- تجهیزات: ستون `status` متنی استاندارد (جایگزین نمایش is_active عددی)
- بازدیدها، عیوب، دستورکارها، صورت‌وضعیت‌ها، حوادث: ستون جدید «وضعیت» (فعال/غیرفعال) — ستون مرحله‌ای قبلی با نام «مرحله» حفظ شد
- فیلد وضعیت از همهٔ فرم‌های ثبت/ویرایش/کپی حذف شد — هر ثبت جدید پیش‌فرض «غیرفعال» است
- فعال‌سازی از طریق ویرایش گروهی (عملیات گروهی ← فعال/غیرفعال کردن) در همهٔ جدول‌ها
- قرارداد: وضعیت چهارگانه «فعال / غیرفعال / پیش‌نویس / اتمام قرارداد» (فقط قرارداد فرم وضعیت دارد)
- گارد حذف بر اساس وضعیت جدید در همهٔ جداول (حتی حذف انبوه) تعمیم یافت

## ۳) اصلاحات ریز
- توضیح فرمت تاریخ زیر فیلدهای تاریخ حذف شد
- پیمانکاران: فیلد آدرس به پایین فرم منتقل شد — عرض کامل، ارتفاع زیاد و قابل تغییر اندازه (textarea)

## استقرار
1. فایل `Powerline_v4.3.78_Migration.sql` را در phpMyAdmin روی دیتابیس اجرا کنید
2. پوشهٔ `Powerline` را روی هاست آپلود کنید (بک‌اند جدید)
3. سورس فرانت‌اند را دیپلوی کنید (یا فایل zip را باز کنید)
4. برنامه تا قبل از اجرای migration هم بدون خطا کار می‌کند (امور و وضعیت‌های جدید به‌صورت خودکار فعال می‌شوند)

---

# Powerline Web — v4.3.77 (2026-09-03)

## ۱) پنجره فیلتر ستون وضعیت — فقط «فعال» و «غیرفعال»
در همه جدول‌هایی که ستون وضعیت فعال/غیرفعال دارند (خطوط، دکل‌ها، پیمانکار، پرسنل،
تجهیزات، چک‌لیست، فهرست بها، مراجع دکل و...) پنجره فیلتر قبلاً مقدار خام دیتابیس را
نشان می‌داد (active، deactive، یا حتی عدد ۱). اکنون:
- فهرست فیلتر فقط دو مقدار فارسی «فعال» و «غیرفعال» را نشان می‌دهد — هیچ چیز دیگر
- نمایش سلول همیشه فارسی است؛ مقدار خام هر اسکیما باشد (active/inactive/deactive
  یا ۱/۰ یا true/false) با نرمال‌سازی واحد به فارسی تبدیل می‌شود
- ستون‌های بج (مثل وضعیت قرارداد: پیش‌نویس/فعال/منقضی/تکمیل) هم لیبل فارسی خود را
  در فیلتر نشان می‌دهند، نه مقدار انگلیسی خام
- منطق جدید در src/lib/status.ts متمرکز شده و در data-table اعمال می‌شود

## ۲) قانون امنیتی حذف — ردیف «فعال» قابل حذف نیست
تا زمانی که وضعیت یک ردیف «فعال» است، دکمه حذف آن را مسدود می‌کند و پیام واضح
می‌دهد: «ابتدا وضعیت را غیرفعال کنید؛ ردیف‌های غیرفعال قابل حذف هستند».
- اعمال‌شده در همه جدول‌های دارای ستون فعال/غیرفعال (فرانت‌اند، در data-table متمرکز)
- مسیر غیرفعال‌سازی همان «عملیات گروهی ← غیرفعال کردن» یا ویرایش رکورد است
- سمت سرور (Powerline): رکورد فعال در guardedDelete، حذف انبوه خطوط/دکل‌ها/پرسنل و
  مراجع دکل (ساختار/کد نوع) هم مسدود می‌شود — دفاع دولایه. برای فعال شدن این لایه،
  پوشه Powerline را روی هاست دوباره آپلود کنید؛ لایه فرانت‌اند بلافاصله فعال است.

## استقرار
- فرانت‌اند (Vercel): تغییرات status.ts + data-table.tsx — کافی است دیپلوی کنید.
- بک‌اند (اختیاری برای دفاع دولایه): endpoints/modules.php، lines.php، towers.php
