# Powerline Web — v4.3.70 (2026-09-02)

## اصلاح این نسخه: رفع خطای ۵۰۰ صفحه پرسنل

### مشکل
جدول personnel روی دیتابیس لایو نه ستون position دارد نه personnel_type
(و phone / collaboration_start هم وجود ندارند) — همه کوئری‌های پرسنل روی ستون‌های
ناموجود ساخته می‌شدند و صفحه پرسنل Internal Server Error می‌داد.

### راه‌حل: پرسنل کاملاً «ستون‌آگاه» شد
- بک‌اند موقع اجرا ستون‌های واقعی جدول را می‌خواند (SHOW COLUMNS) و همه کوئری‌ها
  — لیست/جستجو، ایجاد، ویرایش، حذف انبوه، ورود انبوه از اکسل و bootstrap —
  فقط با ستون‌های موجود ساخته می‌شوند. ستون سمت اگر نباشد، خالی برگردانده می‌شود.
- با هر سه ساختار دیتابیس کار می‌کند: با position، با personnel_type، و بدون هیچ‌کدام.
- کمبوباکس «سرپرست خط»: اگر ستون سمت وجود نداشته باشد، سرپرست‌ها از
  فیلد «نام سرپرست» هر پرسنل استخراج می‌شوند.

## فایل‌های تغییریافده
- Powerline/endpoints/modules.php
- src/hooks/use-personnel-options.ts
- src/lib/version.ts ، package.json

## نکته استقرار
- `Powerline/endpoints/modules.php` روی هاست + سورس فرانت‌اند در Vercel.
