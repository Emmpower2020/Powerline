# Powerline Web — v4.3.53 (2026-09-02)

## فهرست اصلاحات این نسخه

### ۱) نقشه‌ها
- متن لیست خطوط در پنل «انتخاب خطوط» تیره‌تر و خواناتر شد (slate-800 + font-medium).
- رفع باز نشدن منوی «قرارداد جاری» هدر روی صفحه نقشه: همه منوهای Popover/Dropdown رادیکس و دیالوگ‌ها
  حالا با z-index بالاتر از لایه‌های نقشه (60000) رندر می‌شوند.
- با تغییر «قرارداد جاری» در هدر، داده نقشه خودکار رفرش می‌شود — خطوط و دکل‌های همان قرارداد دیده می‌شود.

### ۲) انتخاب همه (چک‌باکس بالای جدول)
- انتخاب حالا اُپتیمیزه است: تیک همان لحظه برمی‌گردد (بدون نمایش علامت منفی/نیمه‌انتخاب موقت)
  و سپس انتخاب همه صفحات در پس‌زمینه تکمیل می‌شود. در صورت خطا به حالت قبل برمی‌گردد.

### ۳) متن عملیات گروهی
- «در حال اجرای قرارداد تغییر کرد» → «در حال اجرای تغییر قرارداد» — برای همه بخش‌ها اصلاح شد
  (تغییر ولتاژ، تغییر نوع سیم، فعال/غیرفعال کردن، انتقال به قرارداد و...).

### ۴) سرعت عملیات گروهی
- خطوط: مانند دکل‌ها بسته‌های ۱۰۰تایی با یک درخواست اجرا می‌شود (endpoint جدید `lines/bulk-update`).
- ماژول‌های عمومی (پیمانکاران، پرسنل، تجهیزات، ...): درخواست‌ها به‌جای یکی‌یکی، دسته‌های ۱۰تایی موازی.

### ۵) استانداردسازی ماژول‌های عمومی (پیمانکاران و...)
- «وارد کردن از اکسل» حالا همان دیالوگ استاندارد مدارها/خطوط است (تشخیص تکراری، آپدیت/رد، قالب اکسل).
- تأیید حذف حالا دیالوگ استاندارد وسط صفحه با نوار پیشرفت است (به‌جای پنجره confirm مرورگر بالای صفحه).
- حذف ردیف‌ها دسته‌ای ۱۰تایی موازی انجام می‌شود.

### ۶) قراردادها
- هدر ستون‌ها: «کد قرارداد / عنوان قرارداد / شروع قرارداد / پایان قرارداد».
- تاریخ شروع/پایان در «قرارداد جدید» تکست‌باکس ساده شد با راهنمای:
  «توضیح: تاریخ می‌بایست با فرمت 1405/05/30 نوشته شود» — و در صورت فرمت اشتباه هشدار می‌دهد و ذخیره نمی‌شود.
- فرم ویرایش: همه فیلدها برچسب فارسی دارند (دیگر contract_code/start_date و... دیده نمی‌شود)؛
  وضعیت/نوع قرارداد/پیمانکار به‌صورت Select فارسی؛ تاریخ‌ها همان تکست‌باکس شمسی با اعتبارسنجی.

### ۷) تنظیمات
- بخش جدید «نمایش رابط کاربری»: سوییچ نمایش/عدم نمایش کارت‌های آماری بالای صفحات
  (خطوط، دکل‌ها، مدارها، دیفکت‌ها، پرسنل) — بلافاصله در همه صفحات اعمال می‌شود.

### ۸) اصلاحات جانبی
- فیلد «تلفن» در فرم افزودن پرسنل که بی‌صدا ذخیره نمی‌شد، درست شد.

## فایل‌های تغییریافده (اصلی)
- src/components/pages/generic-module-page.tsx (بازنویسی EditorDialog + import/حذف استاندارد)
- src/components/create-dialogs.tsx ، src/components/data-table.tsx
- src/components/pages/map-page.tsx ، settings-page.tsx ، lines/towers/circuits/defects/personnel pages
- src/components/{generic-bulk-actions,lines/bulk-lines-actions,towers/bulk-towers-actions}.tsx
- src/components/ui/dialog.tsx ، src/app/globals.css (لایه‌بندی z-index)
- src/hooks/use-stats-visible.ts (جدید) ، src/lib/{api-config,jalali استفاده موجود}
- Powerline/endpoints/lines.php (endpoint جدید lines/bulk-update)

## نکته استقرار
- ⚠️ فایل `Powerline/endpoints/lines.php` تغییر کرده — حتماً روی هاست جایگزین شود (سایر فایل‌های PHP مثل نسخه 4.3.51).
