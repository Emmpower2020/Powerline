# Powerline Web — v4.3.57 (2026-09-02)

## اصلاح این نسخه: پیام خطای واضح هنگام حذف رکورد استفاده‌شده

### مشکل
حذف پیمانکاری که در قرارداد/صورت‌وضعیت استفاده شده بود، خطای خام دیتابیس (کلید خارجی MySQL) می‌داد.
رفتار درست است (رکورد استفاده‌شده نباید حذف شود) ولی پیام باید دلیل را واضح بگوید.

### راه‌حل
- حذف محافظت‌شده در بک‌اند: قبل از حذف بررسی می‌شود رکورد در کجا استفاده شده؛
  در صورت وجود، پیام فارسی دقیق برمی‌گردد، مثلاً:
  «این پیمانکار در «قراردادها» استفاده می‌شود (۲ مورد) و حذف آن ممکن نیست.
  ابتدا آن رکوردها را حذف کنید یا پیمانکار آنها را تغییر دهید.»
- اعمال‌شده روی: پیمانکاران، قراردادها، پرسنل، تجهیزات.
- فرانت‌اند: توست «حذف ناقص» حالا دلیل خطا (پیام سرور) را هم نمایش می‌دهد.

## فایل‌های تغییریافده
- Powerline/endpoints/modules.php (بک‌اند)
- src/components/pages/generic-module-page.tsx (نمایش دلیل خطا در توست)
- src/lib/version.ts ، package.json

## نکته استقرار
- `Powerline/endpoints/modules.php` روی هاست + سورس فرانت‌اند در Vercel.
