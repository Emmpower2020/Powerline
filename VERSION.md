# Powerline Web — v4.3.69 (2026-09-02)

بیس: بسته v4.3.68 کاربر + دیتابیس نهایی jibimar1_Powerline

## اصلاحات این نسخه

### ۱) رفع خطای ایمپورت پرسنل («Unknown column 'position'»)
- ساختار ستون «سمت» پرسنل در نسخه‌های مختلف دیتابیس متفاوت است (position یا personnel_type).
- بک‌اند حالا موقع اجرا ستون واقعی را تشخیص می‌دهد و GET/ایجاد/ویرایش/ورود-انبوه پرسنل
  با همان ستون کار می‌کند — روی هر دو ساختار دیتابیس بدون خطا.

### ۲) فارسی‌سازی کامل رابط
- فایل اکسل «خطاهای import» حالا هدرهای فارسی دارد (نام/نام خانوادگی/کد ملی...) —
  نام انگلیسی فقط در دیتابیس می‌ماند.
- هدرهای انگلیسی باقی‌مانده در جدول‌ها فارسی شدند: ID → شناسه، IP → آی‌پی.
- خطاهای خام MySQL به پیام فارسی واضح ترجمه می‌شوند (۲۰ نقطه در بک‌اند)؛ مثلاً:
  «ستون «position» در دیتابیس وجود ندارد — ساختار دیتابیس با نسخه برنامه هماهنگ نیست...»،
  «این رکورد در جداول دیگر استفاده شده است...»، «رکورد تکراری است...»،
  «فرمت مقدار یکی از فیلدها (تاریخ یا عدد) درست نیست».

## فایل‌های تغییریافده
- Powerline/endpoints/{modules,lines,towers,defects}.php
- src/components/import-excel-dialog.tsx
- src/components/pages/generic-module-page.tsx
- src/lib/version.ts ، package.json

## نکته استقرار
- چهار فایل PHP بک‌اند روی هاست + سورس فرانت‌اند در Vercel جایگزین شود.
