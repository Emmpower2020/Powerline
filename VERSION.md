# Powerline Web — v4.3.54 (2026-09-02)

## اصلاح این نسخه

### رفع باز نشدن کمبوباکس‌ها داخل دیالوگ‌ها (مثل فرم ویرایش قرارداد)
- مشکل: منوی Select (وضعیت، نوع قرارداد، پیمانکار...) باز می‌شد اما زیر محتوای دیالوگ می‌رفت و فقط
  لبهٔ آن پایین صفحه دیده می‌شد — چون z-index محتوای دیالوگ (100001) از منوی بازشده (100000) بالاتر بود.
- راه‌حل: همهٔ منوهای Popper رادیکس (Select/Dropdown/Popover) حالا z-index 100100 دارند،
  یعنی همیشه بالاتر از دیالوگ‌ها و البته بالاتر از لایه‌های نقشه.
- هم‌راستاسازی بقیهٔ پنجره‌ها: AlertDialog / Drawer / Sheet هم مانند Dialog با z-index جدید
  (روکش 100000، محتوا 100001) بالاتر از نقشه قرار گرفتند.

## فایل‌های تغییریافده
- src/app/globals.css
- src/components/ui/{alert-dialog,drawer,sheet}.tsx
- src/lib/version.ts ، package.json

## نکته استقرار
- فقط فرانت‌اند؛ تغییر بک‌اندی نسبت به v4.3.53 ندارد.
