# Powerline Web — v4.3.61 (2026-09-02)

## اصلاحات این نسخه

### ۱) حذف هوشمند پیمانکار و نمایش دقیق علت خطا
- حذف رکوردهایی که در جداول دیگر با Foreign Key از نوع RESTRICT / NO ACTION استفاده شده‌اند، پیش از اجرای DELETE شناسایی می‌شود.
- پیام خطا به‌صورت فارسی و روشن، نام بخش وابسته و تعداد رکوردهای مانع را نمایش می‌دهد.
- روابطی که خود دیتابیس برای آن‌ها `ON DELETE SET NULL` یا `CASCADE` تعریف کرده است، بی‌جهت مانع حذف نمی‌شوند.
- برای دیتابیس فعلی پروژه، پیمانکار «نیروپویان غرب» با شناسه ۵ در جدول قراردادها استفاده شده و دلیل قابل حذف نبودن آن به‌صورت واضح نمایش داده خواهد شد.
- متن خام خطای SQL / اطلاعات داخلی سرور در UI نمایش داده نمی‌شود و جزئیات فنی فقط در لاگ سرور ثبت می‌شود.

### ۲) اصلاح انتخاب سطرها در تمام جدول‌ها
- پس از دریافت دادهٔ تازه (حذف، ویرایش، رفرش و بارگذاری مجدد)، تمام انتخاب‌های قبلی و شمارندهٔ «ردیف انتخاب شده» پاک می‌شوند.
- انتخاب‌های قدیمی دیگر به رکوردهای جدید منتقل نمی‌شوند.

### ۳) راهنمای دکمه‌ها
- دکمه‌های غیرفعال نیز Tooltip/راهنمای بومی خود را از طریق `title` نمایش می‌دهند؛ pointer-events مربوط به حالت disabled دیگر راهنما را مسدود نمی‌کند.
- این رفتار در کامپوننت پایه Button اعمال شده و روی جدول‌ها یکپارچه است.

### ۴) نمایش بهتر پیام‌های چندخطی
- Toastها برای پیام‌های توضیحی و خطا، شکست خط را حفظ می‌کنند تا دلایل وابستگی‌ها خوانا و واضح نمایش داده شوند.

## فایل‌های اصلی تغییریافته
- `Powerline/endpoints/modules.php`
- `src/components/data-table.tsx`
- `src/components/ui/button.tsx`
- `src/components/ui/toast.tsx`
- `src/lib/version.ts`
- `package.json` / `package-lock.json`

## نسخه
`v4.3.61`


## تغییرات v4.3.61

- کد قرارداد در فرم ثبت/کپی/ویرایش دستی و اجباری شد و تولید خودکار حذف شد.
- فرم CRUD ماژول‌های عمومی برای ثبت، کپی و ویرایش یکسان شد.
- ورودی تمام تاریخ‌ها در UI به TextBox متنی استاندارد تبدیل شد.
- حذف قرارداد یا پیمانکار فعال و همچنین حذف موجودیت دارای وابستگی برای قرارداد/پیمانکار در Backend مسدود و با پیام دقیق توضیح داده می‌شود.
- حالت انتخاب/hover قرارداد جاری و پنجره اطلاعات کاربر بهبود یافت.
- نشانگر ممنوع برای کنترل‌های غیرفعال حذف و نشانگر عادی جایگزین شد.
- هیچ تغییر SQL، Migration یا تغییر ساختار دیتابیس انجام نشد.
