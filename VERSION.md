# Powerline EAM/CMMS - Web Frontend

## نسخه فعلی: v4.3.0

### v4.2.7 — 2026-08-26
- اصلاح نمایش نام خطوط فارسی روی نقشه: حذف SVG textPath و استفاده از برچسب HTML با Vazirmatn برای جلوگیری از شکستن حروف
- بهبود کنتراست، پس‌زمینه و خوانایی نام خطوط در نقشه‌های روشن و تیره
- بهبود تایپوگرافی نام خطوط روی نقشه با Vazirmatn
- کاهش ضخامت هاله متن و تنظیم اندازه/وزن فونت برای خوانایی بهتر
- بهبود رندر متن‌های برداری Leaflet با `geometricPrecision`
**تاریخ:** 2026-08-26  
**نوع تغییر:** PATCH — کاهش وزن فونت نام خط روی مسیر از 700 به 500 (طبق درخواست کاربر)

---

## سیستم نسخه‌بندی (Semantic Versioning)

| قسمت | چه زمانی افزایش می‌یابد | مثال |
|------|--------------------------|------|
| **MAJOR** | تغییرات بزرگ: ماژول جدید، تغییر معماری، تغییرات ناسازگار | v2.0.0 |
| **MINOR** | رفع چند اشکال، بهبود UI/UX، افزودن قابلیت کوچک | v1.1.0 |
| **PATCH** | رفع یک اشکال جزئی، اصلاح متن یا استایل | v1.0.1 |

---

## تاریخچه نسخه‌ها

## v4.3.0 — 2026-08-26
**نوع:** MINOR — اصلاح ریسپانسیو نوار ابزار نقشه و انتخاب همه خطوط

- نوار ابزار موبایل با فاصله ثابت ۸px از دو طرف صفحه قرار می‌گیرد و دیگر به‌دلیل `left/transform` از صفحه خارج نمی‌شود.
- عرض نوار ابزار در موبایل دقیقاً با پنل انتخاب خطوط یکسان شد.
- نوار ابزار در عرض‌های کم به‌صورت افقی قابل اسکرول است و همه ابزارها در دسترس می‌مانند.
- پیام بزرگ «هیچ خطی روی نقشه نمایش داده نشده» حذف شد.
- چک‌باکس «همه» به هدر پنل انتخاب خطوط اضافه شد؛ انتخاب/لغو انتخاب همه خطوط را یکجا انجام می‌دهد و حالت indeterminate هم دارد.

### v4.2.4 — 2026-08-26
**نوع:** PATCH — اصلاح وزن فونت برچسب نام خط روی مسیر

- وزن فونت نام خط روی مسیر از 700 به 500 کاهش یافت (طبق درخواست کاربر: «وزن 400 یا 500 کافیه، سنگین نکن»)
- سایر تنظیمات v4.2.3 (سایز 16px، هاله 5px، فونت Vazirmatn از طریق CSS var) دست‌نخورده باقی ماند

### v4.2.3 — 2026-08-26
**نوع:** PATCH — رفع سه مورد از بازخورد کاربر روی ماژول نقشه

**۱) نوار وضعیت پایین به وسط منتقل شد:**
- نوار وضعیت (تعداد خط انتخاب‌شده، تعداد دکل، کیلومتر) از گوشه پایین-چپ به **وسط پایین** صفحه منتقل شد
- با این تغییر، نوار وضعیت دیگر با پایینِ پنل «انتخاب خطوط» (سایدبار سمت چپ) تداخل ندارد

**۲) افزایش سایز فونت پنل «انتخاب خطوط» (۲ پیکسل):**
- عنوان پنل: 14px → 16px
- جستجو: 12px → 14px
- عنوان گروه ولتاژ: 12px → 14px
- شمارنده گروه ولتاژ: 10px → 12px
- نام خط: 11.5px → 13.5px
- کد خط و تعداد دکل: 9.5px → 11.5px
- آمار فوتر: 11px → 13px
- دکمه «پاک کردن» و «خط انتخاب شده»: 11px → 13px

**۳) بهبود خوانایی نام خط روی مسیر:**
- سایز فونت SVG textpath: 12px → 16px
- وزن فونت: 600 → 700
- ضخامت هاله سفید دور متن: 4px → 5px
- فاصله عمودی متن از خط: -8 → -10
- فونت فقط از CSS variable که next/font ثبت کرده + Tahoma fallback (حذف نام Vazirmatn تحت‌اللعن‌شده که در SVG presentation attribute کار نمی‌کرد)

### v4.0.0 — 2026-08-25
**نوع:** MAJOR — بازطراحی کامل ماژول «نقشه‌ها»: نقشه تمام‌صفحه، مسیر واقعی خطوط بر اساس «مجموعه خط»، نقشه‌های پایه متنوع + صفحه خالی، فهرست خطوط به تفکیک ولتاژ

**۱) نقشه تمام‌صفحه و زیبا:**
- نقشه کل فضای صفحه را می‌گیرد (لبه‌به‌لبه) و پنل‌ها به‌صورت شناور روی آن قرار گرفته‌اند
- دکمه «تمام‌صفحه» واقعی (Fullscreen API مرورگر) + همگام‌سازی خودکار اندازه نقشه
- نوار وضعیت پایین: تعداد خط انتخاب‌شده، تعداد دکل روی نقشه، طول کل مسیر (کیلومتر)

**۲) رسم مسیر خط بر اساس «مجموعه خط» (مهم‌ترین تغییر — طبق نیاز صریح کاربر):**
- دکل‌های هر خط بر اساس **شماره دکل** مرتب و به هم وصل می‌شوند (ترتیب فیزیکی مسیر)
- خطوطی که **نام مجموعه (group_name)** یکسان دارند به‌صورت یک مسیر پیوسته زنجیر می‌شوند: قطعه‌ها با الگوریتم «نزدیک‌ترین سر» (greedy nearest-endpoint) سر-به-سر وصل می‌شوند — نه وصل کردن الکی همه به همه
- شکست هوشمند مسیر در پرش‌های غیرعادی (>۳ کیلومتر بین دو دکل متوالی) تا دکل جاافتاده/مشترک خط صاف غلط نکشد
- اتصال‌های بین قطعه‌های یک مجموعه با خط‌چین رسم می‌شوند (پیوند بین خطوط)
- تست واقعی: مجموعه «کرمانشاه ۲-ماهیدشت قدیم-اسلام آباد ۱» (۵ خط، ۶۵۰ دکل) به‌صورت یک مسیر پیوسته ۷۹ کیلومتری از کرمانشاه ۲ تا اسلام‌آباد رسم می‌شود ✓

**۳) فهرست خطوط به تفکیک ولتاژ (مثل تصویر مرجع):**
- پنل «انتخاب خطوط» سمت راست با گروه‌بندی ولتاژ: ۴۰۰ (بنفش) / ۲۳۰ (قرمز) / ۱۳۲ (سبز) / ۶۳ (آبی) + «بدون ولتاژ»
- هر گروه: چک‌باکس سه‌حالته (همه/هیچ/مجموعه) + جمع‌شدن/باز شدن + شمارنده خطوط
- هر خط: چک‌باکس + دات رنگ ولتاژ + نام + کد خط + تعداد دکل (خطوط بدون دکل مشخص می‌شوند)
- جستجوی زنده در نام/کد/مجموعه + دکمه «پاک کردن همه»
- **پیش‌فرض هیچ خطی انتخاب نشده** (طبق نیاز کاربر — تعداد خطوط و دکل‌ها زیاد است) + راهنمای وسط نقشه با میان‌برهای سریع هر ولتاژ
- hover روی خط در فهرست ← هایلایت همان خط روی نقشه (ضخیم‌تر می‌شود)
- با اولین انتخاب، نقشه خودکار روی محدوده خطوط تنظیم می‌شود (fitBounds)

**۴) نقشه‌های پایه متنوع + حالت «صفحه خالی» (مثل تصویر مرجع):**
- انتخابگر نقشه با شبکه بندانگشتی: صفحه خالی (سفید)، خیابانی OSM، انسانی HOT، روشن مینیمال Carto، تیره Carto، ماهواره‌ای Esri، خیابانی رنگی Esri، توپوگرافی Esri، کوهستانی OpenTopoMap — مجموعاً ۹ نقشه
- **حالت «صفحه خالی»**: بدون هیچ کاشی، پس‌زمینه سفید خالص — برای دیدن واضح و تمیز خطوط (طبق نیاز صریح کاربر)
- بندانگشتی‌ها از خود کاشی‌های هر منبع ساخته می‌شوند (پیش‌نمایش واقعی منطقه کرمانشاه)

**۵) نماد دکل‌ها بر اساس نوع سازه + رنگ بر اساس ولتاژ (طبق نیاز صریح کاربر):**
- فلزی مشبک: **مربع** | تیر چوبی: **مثلث** | تیر بتنی: **دایره** | تلسکوپی (بتنی/فلزی): **لوزی** | سایر: ضربدر
- رنگ دکل‌ها = رنگ ولتاژ خط (۴۰۰ بنفش، ۲۳۰ قرمز، ۱۳۲ سبز، ۶۳ آبی)
- اندازه دکل‌ها با زوم تطبیقی می‌شود (زوم ≥۱۳ دکل‌ها ظاهر می‌شوند، ≥۱۴ برچسب کد دکل با هاله سفید خوانا)
- در زوم‌های پایین فقط مسیر خط‌ها دیده می‌شود (مثل نقشه‌های حرفه‌ای GIS — دکل‌های متراکم خط را خط‌چین نشان نمی‌دهند)

**۶) کارایی و جزئیات فنی:**
- رندرکننده Canvas سفارشی Leaflet (زیرکلاس L.Canvas) — رسم هزاران دکل با شکل‌های متفاوت روی یک بوم واحد (کارایی بالا با ۳٬۴۵۲ دکل)
- ترتیب رسم صحیح: دورخط سفید همه مسیرها ← مسیرهای رنگی ← اتصال‌ها ← دکل‌ها (تا دورخط سفید رنگ‌ها را نپوشاند)
- کلیک روی دکل ← پاپ‌آپ جزئیات فارسی (کد، شماره، خط، ولتاژ، نوع سازه، ساختار، مختصات) با آیکون شکل و رنگ ولتاژ
- تولتیپ روی مسیر خط (نام + کد + ولتاژ + تعداد دکل)
- راهنمای (Legend) کامل: رنگ ولتاژها + شکل انواع دکل + نمونه اتصال مجموعه‌ای
- مقیاس (Scale) + کنترل زوم + سازگار با تم تیره
- مدیریت خطا: پیام + دکمه «تلاش مجدد» (هماهنگ با الگوی بقیه صفحات)

#### 🛠️ فایل‌های جدید:
- `src/lib/map-config.ts` — نقشه‌های پایه، رنگ‌بندی ولتاژ، شکل‌های دکل
- `src/lib/line-routes.ts` — الگوریتم ساخت مسیر (مرتب‌سازی شماره دکل + شکست پرش + زنجیر مجموعه)

#### 🛠️ فایل‌های تغییر یافته:
- `src/components/pages/map-page.tsx` — بازنویسی کامل (چیدمان تمام‌صفحه، پنل خطوط، انتخابگر نقشه، نوار وضعیت)
- `src/components/tower-map-inner.tsx` — بازنویسی کامل (رندرکننده Canvas سفارشی، مسیرها، دکل‌ها، برچسب‌ها)
- `src/components/tower-map.tsx` — هماهنگ با پراپ‌های جدید
- `src/lib/types.ts` — افزودن group_name و dispatch_code به تایپ Line
- `src/app/globals.css` — استایل پاپ‌آپ/تولتیپ/کنترل‌های Leaflet + تم تیره
- `eslint.config.mjs` — مستثنی کردن پوشه‌های غیر سورس

#### ✅ تست‌های انجام‌شده (مرورگر + سرور واقعی هاست sabadgame.com با داده واقعی):
- ورود واقعی admin ✓ — ۳۳ خط و ۳٬۴۵۲ دکل واقعی بارگذاری ✓
- پیش‌فرض بدون انتخاب + راهنمای وسط با میان‌بر ولتاژ ✓
- انتخاب خط 400kV «شرق کرمانشاه-مرصاد» (۲۰۶ دکل): مسیر بنفش پیوسته ✓ — نوار وضعیت «۱ خط | ۲۰۶ دکل | ۸۴ کیلومتر» ✓
- مجموعه ۶۳ کیلوولت ماهیدشت (۵ خط هم‌مجموعه): زنجیر پیوسته ۶۵۰ دکل / ۷۹ کیلومتر — تحلیل پیکسلی: ۹۰٪ مسیر یک قطعه پیوسته ✓
- پرش ۳۳ کیلومتری دکل شماره ۱ خط 61404 به‌درستی شکسته شد (خط صاف غلط رسم نمی‌شود) ✓
- ۹ نقشه پایه + صفحه خالی سفید (پس‌زمینه خالص بدون کاشی) ✓
- زوم ۱۵: دکل‌های مربعی آبی + برچسب کدها (61404-177 و…) خوانا ✓
- زوم ۱۶: هر ۴ شکل دکل (مربع/مثلث/دایره/لوزی) با رنگ ولتاژ ✓
- کلیک دکل: پاپ‌آپ کامل (90101-108: خط، ولتاژ ۴۰۰، فلزی مشبک، آویزی، مختصات) ✓
- راهنما: رنگ ولتاژ + شکل دکل‌ها + نمونه اتصال ✓
- ماهواره‌ای: مسیر بنفش با دورخط سفید کاملاً واضح (ارزیابی کیفی ۸/۱۰) ✓
- tsc صفر خطا ✓ + lint فایل‌های جدید بدون خطا ✓

#### 🚀 استقرار: فقط فایل‌های وب (src + VERSION.md + package.json) — PHP و SQL جدیدی ندارد
- داده مورد نیاز (group_name و voltage_kv و شماره دکل‌ها) از قبل در دیتابیس و API موجود است

---

### v3.5.5 — 2026-08-24
**نوع:** PATCH — عرض ستون «نام سیم» جدول سیم‌ها به ۲۴۰px افزایش یافت (نام ترکیبی فارسی+انگلیسی جا داشته باشد)

#### 🚀 استقرار: فقط فایل‌های وب

### v3.5.4 — 2026-08-24
**نوع:** PATCH — سه اصلاح UI بر اساس درخواست کاربر

**۱) منوی «انواع سیم‌ها» به بخش جدید «داده‌های پایه» منتقل شد:**
- قبلاً بین مدارها و خطوط در گروه «خطوط و مدارها» بود؛ حالا گروه مستقل بعد از «ایمنی» و قبل از «سیستمی»

**۲) فیلتر خودکار ستون‌های عددی (همه جدول‌ها):**
- تغییر مرکزی در DataTable: هر ستونی که type="number" دارد همیشه دکمه فیلتر/سورت می‌گیرد
- شامل: سیم‌ها (سطح مقطع، قطر، وزن، تنش، مقاومت)، فهرست بها (بهای واحد)، قراردادها/صورت‌وضعیت (مبلغ)، دکل‌ها (ارتفاع پایه‌ها، تعداد مقره‌ها، GPS) و هر جدول آینده

**۳) فرم سیم — فیلدهای رشته‌بندی تمام‌عرض شدند:**
- قبلاً دو فیلد کوچک در یک‌سوم عرض فرم بودند؛ حالا در خط جدید با دو فیلد نیم‌عرض + برچسب توضیحی («لایه بیرونی آلومینیوم» / «هسته فولاد») + placeholder نمونه (30/2.79)

#### 🛠️ فایل‌ها:
- `src/components/data-table.tsx` (فیلتر عددی)، `src/components/dashboard-layout.tsx` (منو)، `src/components/pages/conductors-page.tsx` (فرم)

#### 🚀 استقرار: فقط فایل‌های وب (src + VERSION.md + package.json)

### v3.5.3 — 2026-08-24
**نوع:** PATCH — بهبود نمایش ماژول سیم‌ها (درخواست کاربر) + اصلاح SQL

**۱) نام سیم‌ها به شکل «لینکس (Lynx)»:**
- جدول سیم‌ها، دیالوگ ویرایش/کپی، و کمبوباکس «نوع سیم» در فرم خطوط و عملیات گروهی — همه با نام فارسی + انگلیسی
- ۱۵ سیم استاندارد نقشه فارسی دارند (فاکس، مینک، داگ، هاینا، پارتریج، اوریول، لینکس، شاهین، طاووس، اسکواب، دریک، کاناری، کاردینال، کرلو، مارتین)؛ نام‌های ناشناخته بدون تغییر
- جستجو روی هر دو نام کار می‌کند (فارسی و انگلیسی)
- مقدار ذخیره‌شده در خطوط = همان قالب «لینکس (Lynx)» — هماهنگ با فهرست ثابت v3.4.1 و داده قدیمی

**۲) برچسب «تاوده» → «رشته‌بندی (رو/داخل)»:**
- ستون جدول + فیلد فرم + tooltip توضیح («تعداد رشته/قطر هر رشته mm») + کلیدهای import جدید (کلیدهای قدیمی برای فایل‌های اکسل قبلی حفظ شدند)

**۳) اصلاح fix_v3.5.1.sql:** دو `;` جاافتاده باعث خطای 1064 phpMyAdmin می‌شد — اصلاح و روی dump واقعی هاست در MariaDB محلی دوبار اجرا و تأیید شد (کوتیشن صفر، Bs صفر، ۲۷ permission + ۲۵ گرنت)

#### 🛠️ فایل‌ها:
- `src/hooks/use-conductors.ts` (+CONDUCTOR_NAME_FA، +conductorDisplayName) و `src/components/pages/conductors-page.tsx`
- `database/fix_v3.5.1.sql` (semicolon ها)

#### 🚀 استقرار: فقط فایل‌های وب (src + VERSION.md + package.json) — PHP/SQL جدیدی ندارد (اگر fix_v3.5.1.sql را با نسخه قبلی اجرا نکردید، نسخه اصلاح‌شده همین بسته را اجرا کنید)

### v3.5.2 — 2026-08-24
**نوع:** MINOR — endpoint تجمیعی /bootstrap + دیتابیس محلی مرورگر (کش با TTL) + نمایش آفلاین

**زمینه:** ایمیل نت‌افراز تأیید کرد دامین تحت حمله DDoS واقعی است و لایه آنتی‌دی‌داس «تا پایان حمله» فعال می‌ماند؛
یعنی چالش‌ها موقتی‌اند اما ممکن است تکرار شوند — این نسخه اپ را برای آن دوره تاب‌آور می‌کند.

**۱) Endpoint تجمیعی GET /bootstrap (PHP):**
- یک درخواست = پرسنل (فیلدهای کمبوباکس) + مدارها + سیم‌ها + خطوط (سبک — دکل‌ها عمداً خارج)
- هر بخش permission جداگانه (`canAccess` جدید بدون خطا — بدون دسترسی = کلید حذف، نه 403 کل)
- هر بخش try/catch خودش — خطای یک جدول بقیه را زمین نمی‌زند (errors بخشی)
- باز شدن صفحه: از ~۹ درخواست به ۱-۲ → عملاً آستانه ضد DDoS لمس نمی‌شود

**۲) دیتابیس محلی مرورگر (`src/lib/local-cache.ts`):**
- داده مرجع پس از اولین دریافت در localStorage سیستم کاربر ذخیره می‌شود
- stale-while-revalidate: نمایش فوری از حافظه + تازه‌سازی در پس‌زمینه (TTL = ۱۰ دقیقه)
- **اینترنت/هاست قطع → داده‌ها از حافظه محلی با نشان «حافظه محلی (هاست قطع)» می‌مانند**
- پاک‌سازی خودکار بعد از ۷ روز + مدیریت سهمیه پر + کهنه‌شدن با هر نوشتن موفق (epoch)

**۳) هوک اشتراکی `use-bootstrap` + اتصال هوک‌های موجود:**
- استور ماژول‌سطح: هر تعداد فرم که subscribe کند، فقط «یک» درخواست شبکه مشترک
- use-circuits / use-conductors / use-personnel-options حالا از bootstrap می‌خوانند (خروجی قبلی بدون تغییر)
- fallback خودکار: اگر PHP هاست قدیمی بود و /bootstrap نداشت (404) → درخواست‌های تکی قبلی

**۴) نشانگر وضعیت در هدر:** «حافظه محلی» (آبی) / «حافظه محلی — هاست قطع» (کهربایی) با توضیح

**۵) اصلاح v3.5.1:** چند permission درج‌شده در مسیر اشتباه بود (جابه‌جایی شماره خط) —
همه ۵۲ مسیر بازممیزی و درج route-aware شد؛ ممیزی نهایی: تطابق کامل مسیر↔دسترسی

#### 🛠️ فایل‌ها:
- جدید: `src/lib/local-cache.ts`، `src/hooks/use-bootstrap.ts`، `tests/test_bootstrap_route.php` (هارنس تست)
- PHP: `api_powerline/lib/Auth.php` (+canAccess)، `api_powerline/endpoints/modules.php` (+bootstrap، بازممیزی permission ها)
- فرانت: `src/lib/api-client.ts` (markCacheDirty بعد از نوشتن)، `src/hooks/use-circuits.ts`، `use-conductors.ts`، `use-personnel-options.ts`، `src/components/dashboard-layout.tsx`

#### ✅ تست‌های انجام‌شده (دیتابیس واقعی هاست در MariaDB محلی):
- GET /bootstrap: personnel=9، circuits=46، conductors=15، lines=33، errors=null ✓
- توکن refresh به‌عنوان access → 401 رد شد ✓ (فیکس امنیتی v3.5.1)
- GET /towers/nearby → ۵۰ دکل واقعی، نزدیک‌ترین ۲۰۵۴ متر ✓ (قبلاً همیشه 404)
- php -l همه فایل‌های تغییر یافته بدون خطا ✓ + tsc صفر خطا ✓ + build ✓

#### 🚀 استقرار:
1. **هاست:** آپلود `api_powerline/lib/Auth.php` + `api_powerline/endpoints/modules.php` (فقط همین دو)
2. **وب:** پوشه src + VERSION.md + package.json
3. اگر PHP هنوز آپلود نشده باشد، اپ خودکار با درخواست‌های تکی کار می‌کند (fallback) — ترتیب بحرانی ندارد
4. SQL جدیدی ندارد (اگر fix_v3.5.1.sql را هنوز اجرا نکرده‌اید، هنوز لازم است)

---

### v3.5.1 — 2026-08-23
**نوع:** PATCH — ۱۲ اصلاح بر اساس بازرسی کامل کد (PHP + فرانت + دیتا)

**۱) حل مشکل قطعی ارتباط با هاست (مهم‌ترین):**
- **علت کشف‌شده:** لایه ضد DDoS هاست sabadgame.com بعد از ~۸-۹ درخواست پشت‌سرهم، به‌جای JSON صفحه چالش HTML (کوکی `_dgjsc`) می‌فرستد. پراکسی Next.js نمی‌تواند JavaScript اجرا کند → HTML را «هاست قطع است» تعبیر می‌کرد → 503
- **فیکس:** پراکسی (`route.ts`) چالش را خودکار حل می‌کند — مقدار کوکی از HTML استخراج، ذخیره و درخواست با کوکی تکرار می‌شود. کوکی تا انقضا برای درخواست‌های بعدی نگه داشته می‌شود
- **توصیه دائمی:** به پشتیبانی هاست بگویید مسیر `/Powerline/api_powerline/` را از لایه ضد DDoS مستثنا کنند

**۲) امنیت — احراز هویت (lib/Auth.php):**
- توکن refresh (۳۰ روزه) دیگر به‌عنوان access token پذیرفته نمی‌شود — چک `type` در verifyJWT؛ انقضای ۱ ساعته حالا واقعی است
- logout درست ابطال می‌کند: refresh_token از body (خروج همین دستگاه) یا همه توکن‌های کاربر — قبلاً هیچ‌وقت چیزی ابطال نمی‌شد

**۳) امنیت — کنترل دسترسی (endpoints/modules.php):**
- همه ۵۲ مسیر ماژول‌ها حالا permission چک می‌کنند: hard برای permission های موجود (contracts/financial/safety) و `requirePermissionSoft` جدید برای بقیه (اگر permission در جدول تعریف شده باشد اعمال می‌شود)
- SQL جدید `database/fix_v3.5.1.sql` بخش ۲: تعریف ۲۷ permission جدید + گرنت اولیه به نقش‌های موجود — **بعد از اجرا کنترل‌ها فعال می‌شوند** (super_admin همیشه دسترسی کامل دارد)

**۴) رفع باگ‌های PHP:**
- `inspections.php`: تایپو `$ $row['gps_lng']` — مقدار طول جغرافیایی بازدیدها همیشه خراب بود
- `towers.php`: مسیر `towers/nearby` قبل از `towers/{id}` ثبت شد — endpoint نقشه (نزدیک‌ترین دکل‌ها) همیشه 404 می‌داد
- `defects.php`: stack trace و مسیر فایل خام به کلاینت لو می‌رفت — حالا فقط در api.log سرور
- `modules.php` حذف انبوه پرسنل: `UPDATE inspections SET inspector_id=NULL` روی ستون NOT NULL همیشه 500 می‌داد — حالا بازدیدها به پرسنل جانشین منتقل یا پرسنل دارای بازدید skip می‌شوند

**۵) دیتای conductors — پاک‌سازی کوتیشن:**
- جدول با مقادیر `'Fox'` (کوتیشن‌دار) درج شده بود (SQL اولیه `'''Fox'''` داشت) — نمایش/جستجو/merge/bulk-import خراب بودند
- `database/fix_v3.5.1.sql` بخش ۱: TRIM کوتیشن از همه ستون‌های متنی + حذف تکراری + یکسان‌سازی Bs→BS (اجرا در phpMyAdmin)
- `create_conductors_v3.5.0.sql` هم اصلاح شد (نصب تازه سالم است)
- فرانت: نام‌ها هنگام بارگذاری نرمال‌سازی می‌شوند (`normalizeConductorName`) تا حتی قبل از اجرای SQL نمایش درست باشد

**۶) فیکس‌های فرانت:**
- کش GET پراکسی: کلید کش شامل کاربر شد (قبلاً در قطعی موقت، داده کش‌شده کاربر A به کاربر B می‌رفت) + بعد از هر نوشتن موفق کل کش پاک می‌شود (داشبورد کهنه نمی‌ماند)
- api-client: رفرش توکن single-flight — چند 401 همزمن دیگر باعث لاگ‌اوت ناخواسته نمی‌شوند (refresh token یک‌بارمصرف است) + جلوگیری از حلقه retry بی‌نهایت + خطای انقضای نشست هم در «لاگ خطاها» ثبت می‌شود
- auth-context: قطعی/کندی موقت هاست (503/timeout) دیگر کاربر را لاگ‌اوت نمی‌کند — فقط 401 واقعی
- صفحه «لاگ خطاها» حالا زنده آپدیت می‌شود (استور immutable شد)
- import قالب اکسل خود برنامه (conductors): سرستون «استاندارد (BS/ASTM)» به headerMap اضافه شد — قبلاً ستون استاندارد بی‌صدا حذف می‌شد
- ویرایش خط دیگر توضیحات (notes) ذخیره‌شده را پاک نمی‌کند
- mock موقت `/conductors` در حالت توسعه حذف شد (PHP هاست آپلود شده)
- دو خطای tsc باقی‌مانده رفع شد — `tsc --noEmit` الان کاملاً پاک است

#### 🛠️ فایل‌های تغییر یافته:
- `src/app/api/proxy/[...path]/route.ts` — حل چالش ضد DDoS + کش per-user + invalidation کامل
- `src/lib/api-client.ts` + `src/lib/auth-context.tsx` + `src/lib/error-log.ts` — رفرش single-flight، بدون لاگ‌اوت در قطعی، استور زنده
- `src/hooks/use-conductors.ts` + `src/components/pages/conductors-page.tsx` + `src/components/create-line-dialog.tsx` + `src/components/tower-map-inner.tsx`
- `api_powerline/lib/Auth.php` + `api_powerline/endpoints/auth.php` + `modules.php` + `towers.php` + `defects.php` + `inspections.php`
- `database/fix_v3.5.1.sql` (جدید) + `database/create_conductors_v3.5.0.sql` (اصلاح)

#### ✅ تست‌های انجام‌شده:
- `tsc --noEmit` صفر خطا + `npm run build` موفق
- حل چالش با curl آزمایش شد: صفحه چالش → استخراج کوکی → درخواست مجدد → پاسخ JSON واقعی API ✓
- همه endpoint های هاست (lines/towers/circuits/conductors/defects/dashboard/auth) قبل و بعد از فیکس پاسخگو بودند ✓

#### 🚀 استقرار (به همین ترتیب):
1. **phpMyAdmin:** اجرای `database/fix_v3.5.1.sql` (کوتیشن conductors + permission ها)
2. **هاست:** آپلود فایل‌های PHP: `lib/Auth.php`، `endpoints/auth.php`، `endpoints/modules.php`، `endpoints/towers.php`، `endpoints/defects.php`، `endpoints/inspections.php`
3. **هاست (اختیاری ولی توصیه‌شده):** تیکت به پشتیبانی برای مستثنا کردن `/Powerline/api_powerline/` از لایه ضد DDoS
4. **وب:** آپلود پوشه src + VERSION.md + package.json (و اگر هاست Node دارد: بیلد جدید)

**نکته مهم توکن‌ها:** چون چک `type` توکن اضافه شد، اگر کاربری توکن refresh را مستقیم در هدر Authorization می‌گذاشته (نباید)، از این به بعد 401 می‌گیرد. کاربران عادی بدون تغییر کار می‌کنند (ورود مجدد لازم نیست).

---

### v3.5.0 — 2026-08-23
**نوع:** MAJOR — ماژول جدید «انواع سیم‌ها» (طبق جدول نسخه‌بندی: ماژول جدید = MAJOR)

**۱) ماژول جدید «انواع سیم‌ها» — منبع: Conductors Standard.xlsx:**
- **SQL جدید `database/create_conductors_v3.5.0.sql`:** جدول `conductors` با ۲۰ ستون فنی انگلیسی (مطابق اکسل: Standard, Type, Core Type, Conductor Name, Material, Stranding, Sectional Area, Overall Diameter, Diameter Code, Weight, Ultimate Strength, Resistance) + UNIQUE روی نام + درج ۱۵ سیم ACSR (Fox, Mink, Dog, Hyena, Partridge, Oriole, Lynx, Hawk, Peacock, Squab, Drake, Canary, Cardinal, Curlew, Martin) — اجرای مجدد امن
- **نمایش فارسی:** استاندارد (BS → «BS (انگلیس)»، ASTM → «ASTM (آمریکا)»)، ماده (Alum. → آلومینیوم، Steel → فولاد) — داده در دیتابیس انگلیسی می‌ماند
- **صفحه جدید با همه امکانات خطوط:** نوار آمار (کل/ACSR/BS/ASTM) + DataTable (۱۰ ستون: نام، نوع هادی، استاندارد، سطح مقطع کل، قطر کل، تاوده، وزن، تنش نهایی، مقاومت، ماده) + افزودن/ویرایش/کپی جدید/کپی TSV/حذف انبوه با پروگرس/import اکسل (هم سرستون فارسی هم انگلیسی اکسل اصلی)/خروجی/چاپ
- منو: گروه «خطوط و مدارها» بعد از مدارها با آیکون Cable

**۲) API جدید (نیازمند آپلود PHP):**
- GET /conductors (جستجو + مرتب بر اساس سطح مقطع) + POST (چک تکراری نام 409) + PUT/DELETE /{id} + bulk-delete + bulk-import (درج/ویرایش بر اساس نام)

**۳) اتصال فرم‌ها به این جدول:**
- فرم خطوط و عملیات گروهی خطوط: کمبوباکس «نوع سیم» حالا از جدول conductors می‌خواند — گزینه‌ها به‌صورت «نام (سطح مقطع mm²)» با گروه‌بندی استاندارد (مثل «Lynx (۲۲۶٫۲ mm²)»)
- سازگاری: اگر جدول هنوز ساخته نشده/خالی بود، فهرست ثابت قبلی به‌عنوان fallback — مقادیر قدیمی رکوردها هم به گزینه‌ها merge می‌شوند
- در حالت توسعه تا آپلود PHP جدید، /conductors هم mock می‌شود (۱۵ سیم از اکسل)

**۴) نکته هاست قطع:** خطای گزارش‌شده داشبورد (503) همچنان قطعی واقعی هاست است — رفتار صحیح: پیام + تلاش مجدد؛ داده در دیتابیس سالم

#### 🛠️ فایل‌های جدید:
- `database/create_conductors_v3.5.0.sql`
- `src/components/pages/conductors-page.tsx`
- `src/hooks/use-conductors.ts`

#### 🛠️ فایل‌های تغییر یافته:
- `api_powerline/endpoints/modules.php` — ۶ endpoint جدید + حذف NULLS LAST (ناسازگار MariaDB قدیمی)
- `src/lib/api-config.ts` + `src/components/dashboard-layout.tsx` + `src/app/page.tsx` — endpoint و منو و case
- `src/components/create-line-dialog.tsx` + `src/components/lines/bulk-lines-actions.tsx` — سیم از جدول
- `src/app/api/proxy/[...path]/route.ts` + `mock-data.ts` — مجوز mock موقت + ۱۵ سیم و handler ها

#### ✅ تست‌های انجام‌شده:
- CRUD کامل در mock: ۱۵ سیم GET ✓ + POST (id=16) + تکراری 409 ✓ + bulk-import (Lynx→updated, NewWire1→inserted) ✓ + bulk-delete (2) ✓
- مرورگر: صفحه ۱۵ ردیف مرتب (Fox اول) با نمایش فارسی «BS (انگلیس)» ✓ + هر ۵ دکمه اصلی ✓
- فرم خط: کمبوباکس نوع سیم از جدول — «Fox (۴۲٫۸ mm²)», «Lynx (۲۲۶٫۲ mm²)», «Canary (۵۱۵٫۱ mm²)» + انتخاب Lynx ✓
- tsc (۰ خطای جدید) + build ✓

#### 🚀 استقرار:
1. **phpMyAdmin:** اجرای `database/create_conductors_v3.5.0.sql`
2. **هاست:** آپلود `api_powerline/endpoints/modules.php`
3. **وب:** پوشه src + VERSION.md + package.json

---

### v3.4.1 — 2026-08-23
**نوع:** PATCH — ۳ اصلاح بر اساس درخواست کاربر

**۱) رفع ولتاژ تکراری در فرم خط:**
- ولتاژ به‌اشتباه در دو بخش آمده بود (پایه + فنی — از بازچینی v3.4.0) → فقط در «اطلاعات پایه» بالای کد دیسپاچینگ ماند
- بخش فنی حالا: ردیف ۱ (تعداد مدار، تعداد باندل، سال بهره‌برداری) | ردیف ۲ (طول خط، طول مدار — عریض) | ردیف ۳ (نوع سیم، نوع سازه دکل — عریض)

**۲) نوع سیم و نوع سازه دکل → کمبوباکس با مقادیر واقعی:**
- نوع سیم: ۱۵ هادی رایج ACSR ایران با نام‌گذاری پرندگان — لینکس (Lynx)، کاناری (Canary)، کرلو (Curlew)، فینچ (Finch)، پارتریج (Partridge)، رابین (Robin)، کلاغ (Raven)، بلدرچین (Quail)، قرقاول (Pheasant)، شاهین (Hawk)، ماهی‌خورک (Osprey)، کورمورنت (Cormorant)، پلیکان (Pelican)، فلامینگو (Flamingo)، سایر — فرمت هماهنگ با داده موجود («لینکس (Lynx)») + اگر مقدار رکورد قدیمی در فهرست نباشد خودکار به گزینه‌ها اضافه می‌شود
- نوع سازه دکل: دقیقاً ۵ مقدار معتبر دیتابیس (مشبک فلزی، تیر چوبی، تیر بتنی، تلسکوپی بتنی، تلسکوپی فلزی) — PHP از همین نام‌ها tower_type را مشتق می‌کند
- هر دو قابل جستجو + عرض کامل (دو ستون بدون فاصله خالی — درخواست کاربر)
- عملیات گروهی خطوط هم همین دو کمبوباکس را گرفت (به‌جای ورودی متنی)

**۳) داشبورد هنگام قطعی هاست (خطای گزارش‌شده):**
- حالا مثل بقیه صفحات: Toast صادقانه + ثبت در لاگ خطاها + **دکمه «تلاش مجدد»** به‌جای رفرش کل صفحه + نمایش متن خطا داخل کارت

#### 🛠️ فایل‌های تغییر یافته:
- `src/components/create-line-dialog.tsx` — حذف ولتاژ تکراری + CONDUCTOR_OPTIONS/TOWER_STRUCTURE_OPTIONS (export) + کمبوباکس‌ها
- `src/components/lines/bulk-lines-actions.tsx` — نوع سیم/سازه کمبوباکس + حذف شاخه متنی بی‌استفاده
- `src/components/pages/dashboard-page.tsx` — Toast + لاگ + دکمه تلاش مجدد

#### ✅ تست‌های انجام‌شده (مرورگر، هاست قطع):
- داشبورد: پیام «سرور موقتاً در دسترس نیست» + دکمه «تلاش مجدد» ✓
- فرم خط: ولتاژ فقط ۱ بار ✓
- نوع سیم: گزینه‌های لینکس/کاناری/کرلو/فینچ/پارتریج + جستجوی «کاناری» → فقط کاناری ✓
- نوع سازه: ۵ مقدار معتبر ✓
- tsc (صفر خطای جدید) + build ✓

#### 🚀 استقرار: فقط فایل‌های وب (بدون PHP/SQL)

---

### v3.4.0 — 2026-08-23
**نوع:** MINOR — ماژول «لاگ خطاها» + بهبودهای فرم خط + پایان خطاهای تکراری

**۱) ماژول جدید «لاگ خطاها» (سیستمی — درخواست مدیر سیستم):**
- همه خطاهای برنامه در یک جدول: خطاهای API (همه درخواست‌ها)، خطاهای بارگذاری صفحات، خطاهای فرم‌ها
- ستون‌ها: زمان شمسی، کد HTTP (با بج رنگی)، عنوان، پیام، منبع (صفحه/کامپوننت/endpoint)
- جستجو + فیلتر + سورت با همان DataTable استاندارد + پاک‌کردن همه
- ثبت خودکار از دو مسیر: هوک onError در api-client (همه ApiError ها) + فراخوانی logError در صفحات
- حداکثر ۵۰۰ ردیف در حافظه نشست — سمت کلاینت (برای تاریخچه بلندمدت سرور: audit_log)

**۲) پایان خطای تکراری «خطا در دریافت لیست خطوط» در دکل‌ها:**
- علت: فرم دکل و عملیات گروهی هر بار باز می‌شدند و با هاست قطع، Toast پشت‌سرهم تکرار می‌شد
- حالا: Toast فقط یک‌بار در هر نشست فرم (ref) — اما هر رخداد در «لاگ خطاها» ثبت می‌شود
- متن Toast کوتاه‌تر: «سرور موقتاً در دسترس نیست — جزئیات در لاگ خطاها»

**۳) فرم خط — ساده‌سازی برچسب‌ها (درخواست کاربر):**
- «کد دیسپاچینگ (از مدارها — قابل جستجو، فقط هم‌ولتاژ)» → «کد دیسپاچینگ»
- «سرپرست خط (از پرسنل — سرپرست اکیپ)» → «سرپرست خط» | «کارشناس خط (از پرسنل)» → «کارشناس خط»
- placeholder ها ساده شدند و description «سرپرست اکیپ/کارشناس خط» جلوی هر اسم در کمبوباکس حذف شد

**۴) فرم خط — بازچینی مشخصات فنی (درخواست کاربر):**
- گرید منظم: ردیف ۱ (ولتاژ، تعداد مدار، تعداد باندل — ۳ ستون هم‌عرض) | ردیف ۲ (طول خط، طول مدار، سال بهره‌برداری — ۳ ستون هم‌عرض) | ردیف ۳ (نوع سیم، نوع سازه دکل)
- پایان ناهماهنگی ۳/۲/۳ فیلدی — همه ردیف‌ها هم‌عرض

**۵) نکته ثبت مدار/خط با پیام ۵۰۳:** این خطا واقعی و درست است — هاست شما در همان لحظه قطع بوده (nginx 405)؛ با سیاست v3.3.1 داده ذخیره نشده و بعد از پایداری هاست دوباره تلاش کنید

#### 🛠️ فایل‌های جدید:
- `src/lib/error-log.ts` — استور مرکزی + هوک اتصال api-client
- `src/components/pages/error-log-page.tsx` — صفحه لاگ خطاها

#### 🛠️ فایل‌های تغییر یافته:
- `src/lib/api-client.ts` — endpoint در ApiError + هوک onError
- `src/components/dashboard-layout.tsx` + `src/app/page.tsx` — منوی «لاگ خطاها» + فعال‌سازی هوک
- `src/components/towers/create-tower-dialog.tsx` + `bulk-towers-actions.tsx` — Toast یک‌بار + ثبت لاگ
- `src/components/create-line-dialog.tsx` — برچسب‌ها + گرید منظم
- `src/hooks/use-personnel-options.ts` — حذف description تکراری
- ۵ صفحه اصلی — ثبت خطاها در لاگ مرکزی

#### ✅ تست‌های انجام‌شده (با هاست قطع):
- ورود به دکل‌ها: ۰ Toast تکراری (قبلاً پشت‌سرهم) ✓
- لاگ خطاها: ۲۰ ردیف ثبت‌شده با کد ۵۰۳ و منبع (فرم دکل / API) ✓
- فرم خط: برچسب‌های ساده + ۷ گرید منظم در ساختار ✓

#### 🚀 استقرار: فقط فایل‌های وب (بدون PHP/SQL)

---

### v3.3.1 — 2026-08-23
**نوع:** PATCH — رفع «داده‌ها پاک شده به نظر می‌رسند» (درخواست: هیچ‌چیز حذف/اضافه نکن، فقط داده دیتابیس را نشان بده)

**ریشه مشکل:** fallback داده به mock (طراحی v2.5.2 برای توسعه) + ناپایداری هاست اشتراکی = دو حالت غلط:
- جدول‌ها از داده جایگزین خالی خوانده می‌شد → انگار داده پاک شده
- بدتر: اگر import در لحظه قطعی هاست انجام می‌شد، ردیف‌ها به حافظه موقت می‌رفتند و عملاً در دیتابیس ذخیره نمی‌شدند (گم‌شدن بی‌صدا)

**اصلاحات:**
- **پایان fallback به mock برای همه داده‌ها** — mock فقط برای /auth/* در حالت توسعه ماند (تا پیش‌نمایش قابل لاگین بماند)
- **کش پاسخ‌های GET واقعی:** پاسخ موفق هر GET در حافظه پروکسی کش می‌شود (حداکثر ۱۲۰ کلید، ۳۰ دقیقه اعتبار)؛ اگر هاست لحظه‌ای قطع بود، آخرین داده واقعی همان صفحه نمایش داده می‌شود (هدر X-Served-From-Cache) — نه داده ساختگی، نه جدول خالی
- **نوشتن‌ها هرگز موفقیت کاذب نمی‌دهند:** POST/PUT/DELETE هنگام قطعی هاست → خطای ۵۰۳ با پیام واضح «داده ذخیره نشد — دوباره تلاش کنید» (جلوگیری قطعی از گم‌شدن import)
- **با هر نوشتن موفق، کش همان موجودیت بی‌اعتبار** می‌شود (مثلاً بعد از import خطوط، کش /lines تازه می‌شود)
- **پیام واضح در همه صفحات اصلی** (خطوط/دکل‌ها/مدارها/پرسنل/عیوب): اگر سرور در دسترس نبود، Toast قرمز نمایش داده می‌شود: «سرور دیتابیس موقتاً در دسترس نیست — داده‌های شما در دیتابیس کاملاً سالم است» — دیگر هرگز جدول خالی بی‌صدا یعنی «پاک شده» دیده نمی‌شود
- retry یک‌باره 5xx (v3.2.1) همچنان فعال

#### 🛠️ فایل‌های تغییر یافته:
- `src/app/api/proxy/[...path]/route.ts` — بازنویسی کامل سیاست proxy
- صفحات اصلی (۵ فایل) — Toast خطای سرور

#### ✅ تست (با هاست واقعاً قطع — nginx 405):
- لاگین پیش‌نمایش ✓ (auth mock مجاز)
- GET خطوط → ۵۰۳ + پیام «داده‌های شما سالم است» ✓
- POST خطوط → ۵۰۳ + «داده ذخیره نشد — دوباره تلاش کنید» ✓ (بدون موفقیت کاذب)
- Toast قرمز در صفحه خطوط در مرورگر ✓

#### 🚀 استقرار: فقط فایل‌های وب (بدون PHP/SQL)

---

### v3.3.0 — 2026-08-23
**نوع:** MINOR — ۴ بهبود بر اساس درخواست کاربر

**۱) بچ import سبک‌تر:** اندازه بچ از ۲۰۰ به ۱۰۰ ردیف کاهش یافت (تأثیر روی پایداری هاست اشتراکی بررسی می‌شود — تقسیم بازگشتی بچ خطاخورده تا ۲۵ ردیف همچنان فعال است)

**۲) نام و آیکون برنامه:**
- عنوان تب: «سیستم مدیریت خطوط ایلیا» (با template برای صفحات داخلی)
- آیکون اختصاصی `public/favicon.svg`: دکل انتقال برق شبکه‌ای با بازوها، مقره‌های زرد و جرقه برق روی گرادیان بنفش — برای تب، میانبر و اپل

**۳) مسیر حرکت کارت‌ها محدود شد (دیالوگ ویرایش کارت‌های آمار — همه بخش‌ها چون کامپوننت مشترک است):**
- حرکت فقط عمودی (بالا/پایین) — حرکت افقی کاملاً حذف شد (رفت «به همه طرف» فیکس شد)
- کارت بالای لیست فقط به پایین و کارت پایین لیست فقط به بالا می‌رود (محدودساز اختصاصی مسیر)
- ریشه باگ حرکت نامرتب: لیست عمودی بود ولی استراتژی horizontal استفاده می‌شد → به verticalListSortingStrategy اصلاح شد
- کارت‌های غیرفعال: دیگر کم‌رنگ نمی‌شوند (opacity یکسان با فعال‌ها) — فقط تیک ندارند
- کارت‌های غیرفعال هم قابل جابجایی شدند (دستگیره drag برای همه نمایش داده می‌شود؛ قبلاً فقط فعال‌ها)
- فقط چک‌باکس در حالت سقف ۶ کارت غیرفعال می‌شود (بدون کم‌رنگ‌شدن خود کارت)

**۴) انتخاب زنجیره‌ای عیب استاندارد (فرم ثبت/ویرایش عیب):**
- فیلد ۱: «انتخاب دسته» — ۲۴ دسته کلی قابل جستجو
- فیلد ۲: «انتخاب عیب» — فقط عیوب همان دسته لود می‌شود (به‌جای جستجو بین ۴۰۱ مورد) تا انتخاب سریع باشد
- با تغییر دسته، انتخاب قبلی پاک می‌شود؛ در ویرایش/کپی، دسته از خود رکورد پر می‌شود
- راهنمای زیر فیلدها: «N عیب در دسته X» یا «اول دسته کلی را انتخاب کنید»

#### 🛠️ فایل‌های تغییر یافته:
- `src/components/import-excel-dialog.tsx` — بچ ۱۰۰
- `src/app/layout.tsx` + `public/favicon.svg` (جدید) — نام و آیکون
- `src/components/stats-bar-manager.tsx` — استراتژی عمودی + مودیفایر مسیر + غیرفعال‌ها شفاف/قابل جابجایی
- `src/components/defects/create-defect-dialog.tsx` — انتخاب زنجیره‌ای دسته → عیب

#### ✅ تست‌های انجام‌شده (مرورگر):
- عنوان تب: «سیستم مدیریت خطوط ایلیا» ✓ + favicon بارگذاری ✓
- درگ کارت اول به بالا → محدود شد و سر جا ماند ✓
- درگ کارت اول به پایین → با کارت دوم جابجا شد ✓
- درگ افقی → ترتیب تغییر نکرد ✓
- درگ کارت آخر به پایین → محدود شد ✓
- ۷ دستگیره drag برای همه ۷ کارت (فعال و غیرفعال) ✓ + کارت غیرفعال با opacity کامل ✓
- فرم عیب: دسته «عیوب زنجیر مقره ها» → ۳ عیب همان دسته → انتخاب «افست در زنجیره مقره» → عنوان خودکار پر شد ✓
- tsc (صفر خطای جدید) + build ✓

#### 🚀 استقرار: فقط فایل‌های وب (تغییر PHP و SQL ندارد)

---

### v3.2.2 — 2026-08-23
**نوع:** PATCH — بدون داده نمایشی + رفع خطای import دکل‌ها

**۱) حذف کامل داده‌های نمایشی (طبق درخواست کاربر):**
- همه آرایه‌های mock خالی شدند: خطوط (۷ خط فیک «پارس خزر» و...)، دکل‌ها (۹۰ دکل تولیدی)، مدارها، پرسنل، عیوب، فهرست بها، چک‌لیست‌ها + آمار داشبورد صفر
- از این پس هنگام قطعی موقت هاست، جداول **خالی** نمایش داده می‌شوند (نه داده فیک) — فقط لاگین توسعه (admin/admin123) و کاتالوگ عیوب استاندارد (برای فرم عیب) باقی ماند
- رفع کرش Math.max روی آرایه خالی با helper بعدی (`nextId`) + افزودن POST /towers تکی به mock

**۲) رفع «برای درج، خط الزامی است» (۳٬۴۵۲ ردیف خطا):**
- توضیح: دیتابیس شما خطوط و دکل‌ها را خالی کرده بودید — import دکل‌ها قبل از خطوط انجام شده بود و نام خط اکسل با هیچ خطی مطابقت نداشت
- **PHP towers/bulk-import:** خط حالا با سه روش resolve می‌شود — id، سپس «کد خط»، سپس «نام نرمال‌شده» (نیم‌فاصله/ي عربی/فاصله چندتا) — import مستقل از کش کلاینت کار می‌کند
- پیام خطا راهنما شد: «نام/کد خط «X» با هیچ خط ثبت‌شده‌ای مطابقت ندارد — ابتدا اکسل خطوط را در بخش «خطوط انتقال» وارد کنید»
- همان پیام در mock هم اعمال شد + resolve نام/کد در mock

**۳) یادآوری رفتار id و کد دکل (خواسته کاربر — تأیید شد):**
- ستون id خالی در اکسل: خودکار عدد یکتا می‌گیرد
- کد دکل خالی: خودکار از «کد خط + شماره سه‌رقمی» ساخته می‌شود — تست شد: 61404-001، 61404-002، 80301-005

#### ✅ تست‌های انجام‌شده (شبیه‌سازی دقیق سناریوی کاربر):
- جداول خالی از هر داده فیک ✓
- import دکل‌ها بدون خطوط → پیام راهنما (نه خطای خام) ✓
- import خطوط (id خالی) → موفق → import دکل‌ها (فقط نام خط + شماره) → موفق با کدهای خودکار ✓
- صفر خطای duplicate key کنسول ✓ + tsc/build ✓

#### 🚀 استقرار: فایل‌های وب + `api_powerline/endpoints/towers.php` روی هاست (بدون SQL)

---

### v3.2.1 — 2026-08-23
**نوع:** PATCH — رفع خطای import: «Encountered two children with the same key, null»

**۱) ریشه خطا و رفع آن:**
- علت: اگر فایل اکسل ستون id (خالی) داشت، مقدار null آن هنگام ثبت mock روی id تولیدی بازنویسی می‌شد → چند ردیف با id=null → خطای React «two children with the same key, null»
- در همه handler های mock، ترتیب spread اصلاح شد: `{ ...row, id: newId }` — id تولیدی همیشه محافظت می‌شود (POST و bulk-import خطوط + PUT خطوط/دکل‌ها)
- DataTable: key دفاعی — ردیف‌های بدون id از ایندکس استفاده می‌کنند (`row-${i}`) تا هرگز duplicate key رخ ندهد

**۲) رفع «مقادیر در جدول نمایش داده نمی‌شد اما در دیتابیس بود»:**
- علت: هاست اشتراکی گاهی برای GET خطای گذرای 5xx می‌داد → نمایش به mock برمی‌گشت که ردیف‌های جدید import شده را نداشت (داده واقعی در دیتابیس سالم بود)
- towers/bulk-import در mock حالا واقعاً ردیف‌ها را اضافه می‌کند (قبلاً فقط وضعیت inserted برمی‌گشت) — نمایش در حالت fallback هم کامل است
- **پروکسی: خطاهای 5xx گذرا یک‌باره (بعد از ۶۰۰ms) retry می‌شوند** تا داده واقعی از دست نرود و fallback کاذب رخ ندهد — در production هم پایداری بیشتر

**۳) رفع باگ PHP (bulk-import پرسنل):**
- `array_search(...) ?: null` وقتی اندیس 0 بود (کارمند) مقدار null می‌داد — با چک صریح `=== false` اصلاح شد

#### 🛠️ فایل‌های تغییر یافته:
- `src/app/api/proxy/mock-data.ts` — ترتیب spread + towers/bulk-import واقعی
- `src/components/data-table.tsx` — key دفاعی
- `src/app/api/proxy/[...path]/route.ts` — retry یک‌باره 5xx
- `api_powerline/endpoints/modules.php` — چک array_search

#### ✅ تست‌های انجام‌شده:
- شبیه‌سازی دقیق باگ: اکسل با ستون id خالی + ۳ خط → import → همه id های تولیدی حفظ شدند (PASS) → نمایش در جدول بدون خطای کنسول (0 duplicate key error)
- tsc (صفر خطای جدید) + next build موفق

#### 🚀 استقرار: فایل‌های وب + `api_powerline/endpoints/modules.php` روی هاست (تغییر SQL ندارد)

---

### v3.2.0 — 2026-08-23
**نوع:** MINOR — حذف انبوه سریع + پروگرس بار + منوی جدید ۵ سکشنی

**۱) حذف انبوه سریع در همه جدول‌ها (رفع کندی):**
- مشکل: حذف مدارها/پرسنل/عیوب تک‌تک انجام می‌شد (هر ردیف یک درخواست HTTP) — حذف ۴۶ مدار طولانی بود
- حالا همه جدول‌ها همان روش دکل‌ها/خطوط (v2.2.0) را دارند: دسته‌های ۵۰۰تایی در یک تراکنش
- **API جدید (نیازمند آپلود PHP):** POST /circuits/bulk-delete، POST /personnel/bulk-delete، POST /defects/bulk-delete — حداکثر ۵۰۰۰ ردیف در هر درخواست
- حذف پرسنل با مدیریت FK: crew_members/personnel_certificates خودکار CASCADE؛ assigned_to/repaired_by خودکار SET NULL؛ **عیوب ثبت‌شده (discovered_by) به پرسنل جانشین منتقل می‌شوند** و اگر جانشین نباشد آن پرسنل رد می‌شود (گزارش در پاسخ)
- کامپوننت‌های مشترک جدید: useBulkDelete (هوک) + BulkDeleteDialog (دیالوگ)

**۲) نوار پیشرفت حذف + پاک شدن خودکار انتخاب‌ها:**
- همان ظاهر خطوط/دکل‌ها: نوار قرمز + شمارنده «X از Y» در حین حذف
- بعد از اتمام حذف (و عملیات گروهی عیوب)، شمارنده «N ردیف انتخاب شده» خودکار پاک می‌شود (رفع ایراد باقی ماندن شمارنده)
- تشخیص «PHP جدید آپلود نشده» با پیام راهنما

**۳) سرو سامان کامل منو (طبق وابستگی‌ها — ۵ سکشن):**
- **اصلی:** داشبورد، نقشه‌ها
- **خطوط و مدارها:** مدارها → خطوط انتقال → دکل‌ها (ترتیب وابستگی: مدار منبع کد دیسپاچینگ خط، خط والد دکل)
- **بهره‌برداری و تعمیرات:** بازدیدها → عیوب → دستورکارها (چرخه کار روزمره)
- **پیمانکاری و مالی:** پیمانکاران، قراردادها، تجهیزات، **پرسنل پیمانکار** (تغییر نام از «پرسنل»)، فهرست بها، صورت‌وضعیت‌ها
- **ایمنی:** حوادث ایمنی و شخصی (تغییر نام)، حوادث خطوط (تغییر نام)
- **حذف شد:** سازمان، چک‌لیست‌ها، لاگ ممیزی (به درخواست کاربر)
- آیکون پیمانکاران به Briefcase تغییر کرد (تفکیک از دستورکارها)

**۴) جدول مدارها:** ستون «خط مرتبط» حذف شد (به درخواست کاربر)

#### 🛠️ فایل‌های جدید:
- `src/hooks/use-bulk-delete.ts` — هوک مشترک حذف انبوه (دسته‌بندی + پروگرس + پاک‌سازی انتخاب)
- `src/components/bulk-delete-dialog.tsx` — دیالوگ تأیید حذف با نوار پیشرفت

#### 🛠️ فایل‌های تغییر یافته:
- `api_powerline/endpoints/modules.php` — circuits/bulk-delete + personnel/bulk-delete
- `api_powerline/endpoints/defects.php` — defects/bulk-delete
- `src/components/pages/circuits-page.tsx` — حذف انبوه + حذف ستون خط مرتبط
- `src/components/pages/personnel-page.tsx` — حذف انبوه
- `src/components/pages/defects-page.tsx` — حذف انبوه + پاک‌سازی انتخاب بعد از عملیات گروهی
- `src/components/dashboard-layout.tsx` — منوی جدید ۵ سکشنی + حذف ۳ آیتم + تغییر نام‌ها
- `src/app/page.tsx` — هماهنگ با منوی جدید (حذف case های organization/checklists/audit-log)
- `src/app/api/proxy/mock-data.ts` — bulk-delete های mock

#### ✅ تست‌های انجام‌شده:
- tsc (صفر خطای جدید) + next build موفق
- چرخه کامل در mock خالص: ساخت → جستجو → bulk-delete (deleted:1) → خالی ✓
- مرورگر: منوی ۵ سکشنی با ترتیب دقیق ✓ — «پرسنل پیمانکار»/«حوادث ایمنی و شخصی»/«حوادث خطوط» ✓ — لاگ ممیزی/چک‌لیست‌ها/سازمان حذف ✓ — ستون خط مرتبط حذف ✓ — **انتخاب همه ۴۶ مدار → حذف در یک درخواست (~۳ ثانیه شامل تأخیر تست) → شمارنده خودکار پاک شد → toast «۴۶ مدار با موفقیت حذف شد»** ✓
- نکته: در حین تست، هاست sabadgame.com برای POST ها به‌طور موقت 500 می‌داد — fallback به mock درست کار کرد؛ بعد از آپلود PHP جدید این حالت فعال نمی‌شود

#### 🚀 استقرار:
1. **هاست:** آپلود `api_powerline/endpoints/defects.php` و `modules.php` (ضروری برای حذف انبوه)
2. **وب:** پوشه src + VERSION.md + package.json
3. دیتابیسی: ندارد

---

### v3.1.0 — 2026-08-23
**نوع:** MINOR — رفع خطای FK مهاجرت پرسنل + امکانات کامل جدول‌ها در همه بخش‌ها + عیوب استاندارد + راست‌چین کامل

**۱) رفع خطای مهاجرت پرسنل (#1452 fk_pers_org):**
- ستون organization_id در personnel نوع NOT NULL با FK به organization است و در INSERT های v3.0.0 ارسال نشده بود → خطای «Cannot add or update a child row»
- **فایل جدید `database/import_v3.0.1.sql` (جایگزین کامل v3.0.0):** ALTER (بی‌ضرر در اجرای مجدد) + DELETE (پاکسازی تلاش‌های قبلی) + ۸ INSERT با organization_id=4 (واحد خطوط انتقال) + SELECT نتیجه در انتها
- اجرای مجدد امن است — اگر قبلاً نصفه اجرا شده، کاملش می‌کند

**۲) امکانات کامل جدول‌ها در همه بخش‌ها (طبق درخواست کاربر: «هر جا جدولی باشه باید این امکانات رو داشته باشه»):**
- **عیوب:** کپی TSV + کپی به‌عنوان عیب جدید (duplicateFrom) + ورود انبوه اکسل (defects/bulk-import) + **عملیات گروهی: تغییر شدت/اولویت/ریسک ایمنی** روی عیوب انتخاب‌شده + ستون جدید «دسته» (از عیوب استاندارد)
- **مدارها:** کپی TSV + کپی به‌عنوان مدار جدید + ورود انبوه اکسل (circuits/bulk-import — درج/ویرایش بر اساس کد دیسپاچینگ)
- **پرسنل:** کپی TSV + کپی به‌عنوان پرسنل جدید (کد ملی خالی) + ورود انبوه اکسل (personnel/bulk-import — درج/ویرایش بر اساس کد ملی؛ نگاشت خودکار سمت فارسی «سرپرست اکیپ/کارشناس خط/سیمبان/...» به enum) — هماهنگ با ساختار Persons.xlsx
- قالب دانلود اکسل برای هر سه بخش با سرستون‌های فارسی

**۳) عیوب استاندارد (منبع: عیوب_استاندارد.xlsx — ۴۰۱ مورد در ۲۴ دسته که از قبل در دیتابیس هست):**
- در فرم ثبت/ویرایش عیب، فیلد جدید «انتخاب از عیوب استاندارد» — کمبوباکس قابل جستجو با گروه‌بندی بر اساس دسته
- با انتخاب: عنوان، دسته، شدت و اولویت پیش‌فرض خودکار پر می‌شوند + defect_definition_id ذخیره می‌شود
- GET /defects حالا با defect_definitions/defect_categories در JOIN است → ستون «دسته» در جدول عیوب

**۴) راست‌چین کامل همه جدول‌ها:**
- ستون‌های مدارها/پرسنل/عیوب که چپ‌چین بودند (کد دیسپاچینگ، کد ملی، موبایل، کد رهگیری و...) همگی راست‌چین شدند — مطابق خطوط/دکل‌ها (v1.2.9)

**۵) API جدید (نیازمند آپلود PHP):**
- `POST /defects/bulk-import` — درج انبوه عیب با resolve کد خط/کد دکل + کد رهگیری خودکار + ثبت‌کننده
- `POST /circuits/bulk-import` — درج/ویرایش انبوه بر اساس کد دیسپاچینگ
- `POST /personnel/bulk-import` — درج/ویرایش انبوه بر اساس کد ملی/کد پرسلی + نگاشت سمت فارسی به enum

#### 🛠️ فایل‌های جدید:
- `database/import_v3.0.1.sql` — مهاجرت اصلاح‌شده (جایگزین v3.0.0)

#### 🛠️ فایل‌های تغییر یافته:
- `api_powerline/endpoints/defects.php` — JOIN عیوب استاندارد + bulk-import
- `api_powerline/endpoints/modules.php` — bulk-import مدارها و پرسنل
- `src/components/pages/defects-page.tsx` — امکانات کامل + عملیات گروهی + ستون دسته + راست‌چین
- `src/components/pages/circuits-page.tsx` — کپی/مولتی‌کپی/import + راست‌چین
- `src/components/pages/personnel-page.tsx` — کپی/مولتی‌کپی/import + راست‌چین
- `src/components/defects/create-defect-dialog.tsx` — عیوب استاندارد + حالت کپی
- `src/app/api/proxy/mock-data.ts` — bulk-import ها + ۷۴ تعریف نمونه عیوب استاندارد + نوع برگشتی route

#### ✅ تست‌های انجام‌شده:
- tsc (صفر خطای جدید) + next build موفق
- با سرور واقعی (بازگشته): defect-definitions = ۴۰۱ مورد در ۲۴ دسته ✓
- bulk-import ها با خطاهای ردیفی دقیق (کد خالی، نام خالی، خط ناموجود) ✓
- مرورگر: همه دکمه‌ها (افزودن/ویرایش/کپی جدید/کپی اکسل/حذف/عملیات گروهی/import/خروجی) در عیوب ✓ — جستجوی «مقره» در عیوب استاندارد → انتخاب «افست در زنجیره مقره» → پر شدن خودکار عنوان/شدت (عمده)/اولویت (بالا) ✓ — text-align جدول مدارها = right ✓

#### 🚀 استقرار:
1. **phpMyAdmin:** `database/import_v3.0.1.sql` (اگر v3.0.0 را اجرا نکرده‌اید یا خطا خورد — این نسخه امن است)
2. **هاست:** آپلود `api_powerline/endpoints/defects.php` و `modules.php`
3. **وب:** پوشه src + VERSION.md + package.json

---

### v3.0.0 — 2026-08-23
**نوع:** MAJOR — ماژول جدید «مدارها» + تکمیل ماژول عیوب و پرسنل + کمبوباکس‌های قابل جستجو + سلامت داده پرسنل

**۱) ماژول جدید «مدارها» (منبع کدهای دیسپاچینگ):**
- صفحه جدید با همان تجربه جدول خطوط/دکل‌ها: نوار آمار (کل + تفکیب ولتاژ ۴۰۰/۲۳۰/۱۳۲/۶۳ با رنگ‌بندی هماهنگ) + DataTable (سورت/فیلتر/جستجو + چیدمان per-user + خروجی/چاپ) + دیالوگ ایجاد/ویرایش + حذف با تأیید + چک کد تکراری
- **API جدید (نیازمند آپلود PHP):** GET /circuits (با فیلتر ولتاژ + جستجو + JOIN خط)، POST /circuits (چک تکراری 409)، PUT/DELETE /circuits/{id}
- داده ۴۶ مدار از اکسل Circuts.xlsx — بررسی شد: هر ۴۶ کد از قبل در جدول circuits دیتابیس موجود است، import لازم نیست

**۲) فرم خطوط — ولتاژ قبل از کد دیسپاچینگ + انتخاب از مدارها:**
- فیلد ولتاژ به بالای کد دیسپاچینگ منتقل شد (طبق درخواست کاربر: «ولتاژ رو بالا قبل از کد دیسپاچینگ بزاری»)
- کد دیسپاچینگ به‌جای ورودی متنی، **مولتی‌سلکت قابل جستجو از جدول مدارها** شد — فقط کدهای هم‌ولتاژ نمایش داده می‌شوند (خط ۲۳۰ فقط کدهای ۲۳۰ را می‌بیند)
- تا ولتاژ انتخاب نشود، راهنمای زرد نمایش داده می‌شود؛ با تغییر ولتاژ، کدهای نامرتبط خودکار حذف می‌شوند
- کدهای ترکیبی موجود (مثل CM607-MN609 و AR632-AP634-AR631-AP636) در ویرایش به‌درستی تجزیه و به‌صورت چیپ نمایش داده می‌شوند و با «-» ذخیره می‌شوند

**۳) کمبوباکس‌های قابل جستجو در همه جا (طبق درخواست کاربر):**
- کامپوننت جدید `SearchableSelect` (تک‌انتخاب) و `SearchableMultiSelect` (چندانتخاب) بر پایه Popover + Command — با جستجوی فارسی، گروه‌بندی، چیپ و دکمه پاک‌کردن
- **سرپرست خط و کارشناس خط در فرم خطوط، فرم دکل‌ها، فرم عیوب و عملیات گروهی خطوط/دکل‌ها:** به‌جای ورودی متنی، از جدول پرسنل انتخاب می‌شوند — سرپرست‌های اکیپ برای «سرپرست خط» و کارشناس‌های خط برای «کارشناس خط» (سازگار با داده قدیمی: فیلتر بر اساس personnel_type یا position)
- انتخاب خط در فرم عیب هم قابل جستجو شد (کد خط یا نام)

**۴) تکمیل ماژول عیوب — همان تجربه خطوط/دکل‌ها:**
- بازنویسی کامل با DataTable کامل (سورت/فیلتر/جستجو + چیدمان per-user + خروجی/چاپ) + ۱۰ ستون (کد رهگیری، عنوان، شدت، اولویت، وضعیت، خط، دکل، ثبت‌کننده، تاریخ، محل)
- نوار آمار ۶ کارتی: کل، جدید، در حال تعمیر، بحرانی/بالا (با هشدار)، تأیید نهایی، متصل به خط
- **چرخه کامل عمر عیب:** دکمه تأیید (new → approved) و راستی‌آزمایی (repaired → verified) در ستون عملیات — endpointها از قبل آماده بودند و حالا UI دارند
- دیالوگ ثبت/ویرایش (PUT پشتیبانی می‌شود) + حذف گروهی با تأیید

**۵) تکمیل ماژول پرسنل — صفحه اختصاصی:**
- به‌جای GenericModulePage سابق: DataTable کامل با ۱۱ ستون (کد، نام، نام خانوادگی، کد ملی، نام پدر، نوع با بج رنگی، سمت، موبایل، سرپرست، شروع همکاری، فعال)
- نوار آمار: کل، سرپرست اکیپ، کارشناس خط، سیمبان، سایر
- دیالوگ ثبت/ویرایش با همه فیلدها (نوع پرسنل با کمبوباکس قابل جستجو — سمت فارسی خودکار هماهنگ می‌شود)

**۶) سلامت داده — همخوانی با پرسنل (طبق درخواست کاربر):**
- خطوط: اگر «سرپرست خط» در پرسنل (سرپرست اکیپ) یا «کارشناس خط» در پرسنل (کارشناس خط) نباشد، در ستون سلامت داده به‌عنوان خطا نمایش داده می‌شود
- دکل‌ها: همان بررسی برای «سرپرست خط»
- بررسی فقط وقتی پرسنل بارگذاری شده باشد فعال می‌شود (قبل از آن false-positive نمی‌دهد)

**۷) مهاجرت دیتابیس — `database/import_v3.0.0.sql` (اجرا در phpMyAdmin):**
- گسترش enum نوع پرسنل: crew_supervisor / line_expert / lineman / safety_expert / manager / driver اضافه شدند (علت خرابی import قبلی: enum قدیمی فقط ۴ مقدار داشت و مقادیر جدید به رشته خالی تبدیل می‌شدند)
- پاکسازی ۱۵۷ رکورد خراب import قبلی (نوع خالی + نام‌های تکراری)
- درج ۸ پرسنل از اکسل Persons.xlsx با نوع صحیح + کد ملی + نام پدر + موبایل + سرپرست + تاریخ شروع همکاری شمسی
- ⚠️ نکته: کارشناس خط فعلی خطوط («وحید سلیمانی») در اکسل شما نبود — پس از اجرای مهاجرت، خطوط او در سلامت داده مغایرت نشان می‌دهند؛ با عملیات گروهی «کارشناس خط» می‌توانید همه را یکجا به ساسان امیری/رامین رشیدی تغییر دهید یا او را به پرسنل اضافه کنید

**۸) توسعه mock برای تست آفلاین:**
- ۴۶ مدار و ۸ پرسنل اکسل + CRUD کامل به mock اضافه شد
- پروکسی در حالت توسعه، اگر PHP هاست هنوز endpoint جدید را نشناسد (404 «مسیر پیدا نشد»)، خودکار به mock برمی‌گردد — تا پیش‌نمایش قبل از استقرار PHP هم کامل باشد

#### 🛠️ فایل‌های جدید:
- `src/components/searchable-select.tsx` — SearchableSelect + SearchableMultiSelect
- `src/hooks/use-personnel-options.ts` — گزینه‌های سرپرست/کارشناس از پرسنل
- `src/hooks/use-circuits.ts` — گزینه‌های مدار با فیلتر ولتاژ
- `src/components/pages/circuits-page.tsx` — صفحه مدارها
- `src/components/pages/personnel-page.tsx` — صفحه اختصاصی پرسنل
- `database/import_v3.0.0.sql` — مهاجرت دیتابیس

#### 🛠️ فایل‌های تغییر یافته:
- `api_powerline/endpoints/modules.php` — CRUD مدارها + فیلتر personnel_type + فیلدهای جدید ویرایش پرسنل
- `src/app/page.tsx` + `src/components/dashboard-layout.tsx` — منو و case مدارها + پرسنل اختصاصی
- `src/components/pages/defects-page.tsx` — بازنویسی کامل
- `src/components/defects/create-defect-dialog.tsx` — ویرایش + کمبوباکس جستجودار
- `src/components/create-line-dialog.tsx` — ولتاژ قبل از دیسپاچینگ + مولتی‌سلکت مدارها + سرپرست/کارشناس از پرسنل
- `src/components/towers/create-tower-dialog.tsx` — سرپرست از پرسنل
- `src/components/lines/bulk-lines-actions.tsx` + `src/components/towers/bulk-towers-actions.tsx` — سرپرست/کارشناس با کمبوباکس جستجودار
- `src/lib/lines-quality.ts` + `src/lib/towers-quality.ts` — قواعد همخوانی پرسنل
- `src/components/pages/lines-page.tsx` + `src/components/pages/towers-page.tsx` — بارگذاری پرسنل برای سلامت داده
- `src/app/api/proxy/[...path]/route.ts` — fallback مسیر‌های جدید در توسعه
- `src/app/api/proxy/mock-data.ts` — داده و handler های جدید
- `src/lib/api-config.ts` — endpoint مدارها

#### ✅ تست‌های انجام‌شده:
- tsc — صفر خطای جدید (۲ خطای از-پیش-موجود در فایل‌های دست‌نخورده)
- next build production — موفق
- GET /circuits (۴۶ مدار با تفکیک درست ولتاژ) + فیلتر voltage=230 (۹ کد) — موفق
- چرخه کامل POST/PUT/DELETE مدار + کد تکراری 409 — موفق
- POST /personnel + فیلتر personnel_type=crew_supervisor — موفق
- هاست sabadgame.com هنگام تست nginx 405 می‌داد (خرابی موقت) — fallback به mock طبق طراحی v2.5.2 درست کار کرد

#### 🚀 استقرار:
1. **اجرا در phpMyAdmin (قبل از بقیه):** `database/import_v3.0.0.sql` — با بکاپ قبلی
2. آپلود PHP: `api_powerline/endpoints/modules.php` روی هاست (endpointهای جدید مدارها + فیلتر پرسنل)
3. فایل‌های وب: پوشه src + VERSION.md + package.json (اگر Vercel → redeploy)

---

### v2.9.0 — 2026-08-23
**نوع:** MINOR — رفع ۴ اشکال بحرانی + تکمیل دو صفحه ناقص (تحلیل جامع پروژه → فهرست باگ‌ها)

- **رفع ناوبری خراب «فهرست بها» و «چک‌لیست‌ها»:** این دو آیتم از v1.0.0 در منو بودند اما در switch صفحه اصلی (renderPage در src/app/page.tsx) case نداشتند — با کلیک روی آن‌ها داشبورد زیر عنوان اشتباه نمایش داده می‌شد. حالا هر دو صفحه واقعی خود را دارند
- **صفحه جدید «فهرست بها» (`src/components/pages/price-lists-page.tsx`):** انتخاب فهرست با بج‌های نسخه/تاریخ اجرا/تعداد اقلام + جدول اقلام با DataTable (سورت/فیلتر/جستجو + چیدمان per-user با layoutKey) + دیالوگ «فهرست جدید» (نام/نسخه/تاریخ اجرا با تقویم شمسی) + دیالوگ «قلم جدید» (شرح/کد/واحد/بهای واحد/دسته) + حذف اقلام با دیالوگ تأیید — وصل به endpointهای آماده modules.php (GET/POST /price-lists، GET/POST /price-list-items، DELETE /price-list-items/{id})
- **صفحه جدید «چک‌لیست‌ها» (`src/components/pages/checklists-page.tsx`):** جدول قالب‌ها با بج دامنه (دکل/خط/تجهیزات/همه — مطابق enum جدول checklist_templates) + دیالوگ ایجاد قالب — وصل به GET/POST /checklist-templates
- **رفع dropdownهای همیشه‌خالی در فرمها:** در CreateContractDialog (پیمانکار)، CreateEquipmentDialog (گروه تجهیز) و CreateWorkOrderDialog (اکیپ)، fetch با `useState(() => {...})` اشتباهی انجام می‌شد که فقط یک‌بار در mount (وقتی دیالوگ بسته است) اجرا می‌شد → لیستها هرگز لود نمی‌شدند. هر سه به `useEffect([open])` تبدیل شد + تشخیص هر دو شکل پاسخ (آرایه ساده و صفحه‌بندی‌شده)
- **فعال‌سازی دکمه «ثبت عیب جدید»:** دکمه در صفحه عیوب بدون onClick بود. دیالوگ جدید (`src/components/defects/create-defect-dialog.tsx`) با فیلدهای عنوان (اجباری)، شدت (جزئی/عمده/بحرانی)، اولویت، ریسک ایمنی، خط انتقال (اختیاری — از لیست خطوط)، نوع عیب، محل و توضیحات — مقادیر دقیقاً مطابق enum جدول defects. کد رهگیری DEF-...، وضعیت اولیه new و ثبت‌کننده در سرور خودکار تعیین می‌شوند (POST /defects از قبل آماده بود). بعد از ثبت، لیست به صفحه اول برمی‌گردد و تازه می‌شود
- **رفع کرش لاگین در حالت mock (توسعه بدون سرور):** پاسخ mock لاگین توکنها را تخت (data.access_token) برمی‌گرداند اما auth-context منتظر data.tokens.access_token است → TypeError. ساختار login به {user, tokens:{...}}، auth/me به {user, roles, permissions} و refresh به فقط-توکنها اصلاح شد — هم‌شکل با PHP واقعی
- **توسعه mock برای تست آفلاین:** داده نمونه و handler برای /defects (GET صفحه‌بندی‌شده + POST)، /price-lists، /price-list-items (+DELETE) و /checklist-templates اضافه شد تا جریان کامل ثبت عیب و صفحات جدید بدون اتصال به سرور قابل تست باشد

#### ✅ تست‌های انجام‌شده:
- `tsc --noEmit` — صفر خطای جدید (فقط ۲ خطای از-پیش-موجود در فایلهای دست‌نخورده tower-map-inner و api-client)
- `next build` production — موفق
- لاگین واقعی با سرور sabadgame.com — موفق (ساختار {user, tokens} صحیح)
- GET /price-lists (۲ فهرست)، GET /price-list-items?list_id=1 (۵ قلم)، GET /checklist-templates، GET /defects (صفحه‌بندی‌شده) — همگی با سرور واقعی موفق
- چرخه کامل POST /defects + DELETE /defects/{id} با سرور واقعی — موفق (رکورد تستی بلافاصله حذف شد)
- اعتبارسنجی ساختار mock: loginHasTokensObject / meHasUserRolesPermissions / badLoginIs401 — همه PASS

#### 🛠️ فایل‌های جدید:
- `src/components/pages/price-lists-page.tsx`
- `src/components/pages/checklists-page.tsx`
- `src/components/defects/create-defect-dialog.tsx`

#### 🛠️ فایل‌های تغییر یافته:
- `src/app/page.tsx` — افزودن caseهای price-lists و checklists + importها
- `src/components/create-dialogs.tsx` — ۳ مورد useState(() => fetch) به useEffect([open])
- `src/components/pages/defects-page.tsx` — اتصال دکمه ثبت عیب به دیالوگ + رفرش لیست
- `src/app/api/proxy/mock-data.ts` — اصلاح ساختار auth + handlerهای جدید defects/price-lists/checklists
- `package.json` — نسخه 2.9.0

#### 🚀 استقرار: فقط فایل‌های وب (تغییر PHP و SQL ندارد — همه endpointهای لازم از قبل روی هاست هستند)

---

### v2.8.0 — 2026-08-23
**نوع:** MINOR — ۵ بهبود UX

- **ستون «ولتاژ» در جدول دکل‌ها (جدید):** همان ولتاژی که خط در بخش خطوط انتقال دارد. مقدار از JOIN با جدول lines (که در v2.6.0 به towers.php اضافه شد) می‌آید. ستون به‌طور پیش‌فرض قبل از «نام خط» قرار می‌گیرد و با بج رنگی (۴۰۰ بنفش | ۲۳۰ قرمز | ۱۳۲ سبز | ۶۳ آبی) نمایش داده می‌شود. **تغییر دیتابیسی لازم نیست** — ولتاژ از خود خط خوانده می‌شود
- **حذف فیلد «کد دکل خودکار» از فرم افزودن دکل:** در v2.4.0 این فیلد فقط‌خواندنی اضافه شده بود. در v2.8.0 حذف شد تا فضا برای فیلد «نام خط» بازتر شود. کد دکل همچنان به‌طور خودکار در سرور از کد خط + شماره دکل تولید می‌شود (تغییر در فرم فقط، رفتار سرور ثابت است)
- **آواتار کاربر زیبا‌تر در هدر:** جایگزینی دایره ساده با آواتار گرادیان indigo-purple + آیکون UserCog (به‌جای حروف اول) + نقطه وضعیت سبز برای نشان دادن آنلاین بودن
- **Dropdown کاربر راست‌چین + هدر گرادیان:** align از `end` به `start` تغییر کرد تا در RTL درست نمایش داده شود. هدر کاربر با گرادیان indigo-purple + آیکون بزرگ UserCog
- **زنگوله با Popover واقعی و اعلان‌ها:** در نسخه قبلی زنگوله هیچ popup نداشت. حالا با Popover باز می‌شود و شامل ۴ اعلان نمونه است: عیب بحرانی، بازدید جدید، تکمیل دستورکار، کاربر جدید. هر اعلان آیکون رنگی، عنوان، توضیح و زمان دارد. شمارنده اعلان‌های خوانده‌نشده روی زنگوله
- **فاصله کم‌تر بین کارت‌ها، هدر و جدول:** padding هدر از ۱۶ به ۱۴، padding main از ۱۶ به ۱۲، فاصله بین کارت‌ها و جدول از `space-y-4` به `space-y-2`، padding داخلی نوار آمار از `py-1` به `space-y-1` کاهش یافت — حالا کل صفحه جمع‌وجورتر است و ارتفاع کلی کم‌تر شده

#### 🛠️ فایل‌های تغییر یافته:
- `src/components/pages/towers-page.tsx` — افزودن ستون voltage_kv قبل از line_name + searchKeys
- `src/components/towers/create-tower-dialog.tsx` — حذف فیلد «کد دکل خودکار»
- `src/components/dashboard-layout.tsx` — بازنویسی کامل: آواتار گرادیان + UserCog + Dropdown راست‌چین + Popover زنگوله + padding کم‌تر
- `src/components/stats-bar-manager.tsx` — فاصله کم‌تر
- `src/components/pages/lines-page.tsx` — فاصله کم‌تر (space-y-4 → space-y-2)
- `src/components/pages/towers-page.tsx` — فاصله کم‌تر (space-y-4 → space-y-2)

#### 🚀 استقرار: فقط فایل‌های وب (تغییر PHP و SQL ندارد — towers.php از v2.6.0 آماده است)

---

### v2.7.2 — 2026-08-23
**نوع:** PATCH — رفع ۴ ایراد کاربر

- **اسکرول افقی نامرئی روی hover:** وقتی موس روی نوار کارت‌ها نیست، اسکرول‌بار نامرئی می‌شود. وقتی موس را روی نوار می‌برید، اسکرول‌بار ظاهر می‌شود (در صورت نیاز). کلاس CSS جدید `scrollbar-hover` در globals.css
- **محدودیت ۶ کارت:** حداکثر تعداد کارت‌های فعال از حالت نامحدود به ۶ کارت بازگشت. در دیالوگ ویرایش، شمارنده «X / ۶» نمایش داده می‌شود و وقتی به ۶ برسد، Checkbox‌های غیرفعال disabled می‌شوند
- **رفع برش کارت سلامت داده هنگام hover:** در v2.7.1، کارت با `hover:-translate-y-0.5` به بالا می‌رفت اما چون `overflow-x-auto` روی parent بود، بالای کارت برش می‌خورد. در v2.7.2 انیمیشن translate حذف شد و فقط سایه‌گذاری hover باقی ماند. همچنین `pt-1` به `pt-1` + `pb-2` اضافه شد تا فضای کافی باشد
- **داشبورد بزرگ‌تر و خواناتر:**
  - سایز آیکون کارت‌های اصلی: ۴ → ۵
  - سایز آیکون کارت‌های مینی: ۴ → ۵
  - ارتفاع آیکون کارت اصلی: ۱۰→ ۱۲ (۴۰→۴۸px)
  - ارتفاع آیکون کارت مینی: ۸ → ۱۰ (۳۲→۴۰px)
  - سایز متن مقدار کارت اصلی: ۱۸px → ۲۰px
  - سایز متن مقدار کارت مینی: ۱۶px → ۱۸px
  - padding کارت اصلی: ۳ → ۴
  - padding کارت مینی: ۲.۵ → ۳
  - ارتفاع نمودار فعالیت: ۳۲ → ۳۶ (۱۲۸→۱۴۴px)
  - سایز ستون‌های نمودار: ۳ → ۴
  - سایز متن ردیف‌های وضعیت عیوب: ۱۱ → ۱۲
  - سایز آیکون ردیف‌های وضعیت: ۳ → ۳.۵
  - gap بین کارت‌ها: ۲ → ۳
  - متن‌ها حالا خواناتر هستند و داشبورد بهتر از نیمه صفحه پر می‌کند

#### 🛠️ فایل‌های تغییر یافته:
- `src/app/globals.css` — کلاس `scrollbar-hover` و `scrollbar-hidden` اضافه شد
- `src/components/stats-bar-manager.tsx` — MAX_ACTIVE = 6 + کلاس scrollbar-hover + حذف انیمیشن translate + disabled checkbox
- `src/components/pages/dashboard-page.tsx` — متن‌های بزرگ‌تر + آیکون‌های بزرگ‌تر + ارتفاع بیشتر + چیدمان بهتر

#### 🚀 استقرار: فقط فایل‌های وب (تغییر PHP و SQL ندارد)

---

### v2.7.1 — 2026-08-23
**نوع:** PATCH — اصلاحات UX برای جا شدن در یک صفحه بدون اسکرول

- **متن «نمایش X تا Y از Z» در pagination به سمت راست برگشت** — دکمه‌های قبلی/بعدی در وسط باقی می‌مانند، اما متن شمارش به سمت راست (جای اصلی خودش) منتقل شد
- **حذف «kv» از چیپ ولتاژ:** چیپ تفکیک ولتاژ حالا فقط عدد رنگی را نشان می‌دهد (مثلاً «۴۰۰ · ۲») — بدون متن اضافی، تمیز و خوانا. هم در خطوط و هم دکل‌ها
- **عرض بیشتر کارت‌ها:** از ۲۰۰px به ۲۲۰px — تا محتوای کارت (کششی/آویزی) از کارت بیرون نزند
- **فاصله عمودی بیشتر بین لیبل و مقدار:** در کارت‌های نوار آمار، فاصله بین عنوان کارت و مقدار از ۰.۵ به ۱.۵ افزایش یافت تا از هم چسبیده به نظر نرسند
- **ارتفاع کمتر کارت‌ها:** از ۹۶px به ۷۶px کاهش یافت — تا کل صفحه (کارت‌ها + جدول + pagination) بدون نیاز به اسکرول در یک صفحه جا شود
- **ساده‌سازی داشبورد:** حذف گرادیان‌های رنگی پررنگ و استفاده از همان استایل pastel خطوط (هماهنگ با COLOR_THEMES در stats-bar-manager). داشبورد حالا ۳ ردیف دارد: ۴ کارت اصلی (۲×۴) + نمودار فعالیت + وضعیت عیوب (۲ ستونی) + ۶ کارت مینی (۱×۶) — همه در یک صفحه بدون اسکرول
- کارت‌های داشبورد از ارتفاع ۹۶px به ۷۶px کاهش یافتند + کارت‌های مینی فقط ۶۰px ارتفاع دارند

#### 🛠️ فایل‌های تغییر یافته:
- `src/components/data-table.tsx` — pagination: متن شمارش سمت راست، دکمه‌ها وسط
- `src/components/stats-bar-manager.tsx` — کارت‌های کوتاه‌تر (۷۶px) و عرضی‌تر (۲۲۰px) + فاصله عمودی بیشتر
- `src/components/lines/lines-stats-bar.tsx` — چیپ ولتاژ فقط عدد (بدون kv)
- `src/components/towers/towers-stats-bar.tsx` — چیپ ولتاژ فقط عدد + کششی/آویزی فشرده‌تر
- `src/components/pages/dashboard-page.tsx` — بازنویزی کامل با استایل pastel هماهنگ با خطوط + ۳ ردیف فشرده

#### 🚀 استقرار: فقط فایل‌های وب (تغییر PHP و SQL ندارد)

---

### v2.7.0 — 2026-08-23
**نوع:** MINOR — بهبودهای تجربه کاربری + داشبورد گرافیکی

- **حذف محدودیت ۵ کارت:** کاربر می‌تواند هر تعداد کارت را فعال کند (بدون محدودیت)
- **حذف drag-and-drop از صفحه اصلی:** کارت‌ها در صفحه اصلی فقط نمایش داده می‌شوند (نه قابل جابجایی) — جابجایی فقط در دیالوگ ویرایش انجام می‌شود (درخواست کاربر: «دیگه میتونی قابلیت جایی خود کارت ها رو در صفحه برداری نیازی نیست در دو جا قابل جابجایی باشن»)
- **تفکیک ولتاژ به ۲ ردیف دو‌تایی:** چیپ‌های ولتاژ (۴۰۰/۲۳۰/۱۳۲/۶۳) حالا در یک گرید ۲×۲ نمایش داده می‌شوند — هم در نوار آمار خطوط و هم دکل‌ها — به‌جای حالت فشرده تک‌خطی قبلی
- **کششی/آویزی در یک ردیف:** به‌جای دو خط مجزا، حالا در یک ردیف افقی با دو بج رنگی (بنفش/سبز) قرار گرفتند — در کارت «نوع پایه» بخش دکل‌ها
- **عرض بیشتر کارت‌ها:** کارت‌ها از 176px به 200px عرض و 88px به 96px ارتفاع تغییر کردند تا چیپ‌های ولتاژ و کششی/آویزی به‌خوبی جا بگیرند
- **حذف رنگ از ستون «ساختار دکل»:** در جدول دکل‌ها، ستون ساختار دکل حالا فقط متن ساده است (بدون بج رنگی) — رنگ‌بندی فقط برای نام خط بر اساس ولتاژ باقی می‌ماند (درخواست کاربر: «رنگ ستون ساختار دکل در بخش دکل ها رو هم بردار»)

#### 🎨 داشبورد گرافیکی و زیبا (درخواست ویژه کاربر):
- **بخش خوش‌آمدگویی** با گرادیان indigo-purple و تاریخ شمسی + کارت‌های خلاصه فعالیت ۷ روز و کاربران فعال
- **کارت‌های اصلی با گرادیان رنگی:** ۴ کارت با گرادیان‌های indigo-purple (خطوط)، purple-fuchsia (دکل‌ها)، rose-red-orange (عیوب)، emerald-teal (بازدیدها) + سایه رنگی + آیکون‌های سفید در باکس شیشه‌ای
- **نمودار فعالیت ۷ روز** با ستون‌های گرادیانی (indigo برای عیوب، emerald برای بازدیدها) + شمارنده فعالیت روزانه + نام روزهای هفته
- **وضعیت عیوب** با ۵ ردیف رنگی (هر کدام گرادیان اختصاصی) + هشدار قرمز برای عیوب بحرانی
- **۳ کارت خلاصه** (دستورکار باز، معوق، حوادث) با گرادیان رنگی + هایلایت زرد برای موارد هشدار
- **۳ کارت آمار کوچک** (کاربران، پیمانکاران، بازدیدها) با رنگ‌های pastel
- **انیمیشن hover:** همه کارت‌ها هنگام hover کمی بالا می‌آیند و سایه بزرگ‌تر می‌شود

#### 🛠️ فایل‌های تغییر یافته:
- `src/components/stats-bar-manager.tsx` — حذف MAX_ACTIVE + حذف SortableStatCard (تبدیل به StaticStatCard) + کارت‌های عرضی‌تر
- `src/components/lines/lines-stats-bar.tsx` — چیپ ولتاژ ۲×۲
- `src/components/towers/towers-stats-bar.tsx` — چیپ ولتاژ ۲×۲ + کششی/آویزی یک ردیف
- `src/components/pages/towers-page.tsx` — حذف رنگ از ساختار دکل + حذف import بی‌استفاده Badge
- `src/components/pages/dashboard-page.tsx` — بازنویسی کامل با گرادیان و گرافیک
- `src/lib/types.ts` — افزودن by_voltage به lines + with_gps/linked به towers
- `src/app/api/proxy/mock-data.ts` — MOCK_DASHBOARD_STATS با ساختار هماهنگ با types.ts + داده‌های نمونه واقعی‌تر

#### 🚀 استقرار: فقط فایل‌های وب (تغییر PHP و SQL ندارد)

---

### v2.6.0 — 2026-08-23
**نوع:** MINOR — بازطراحی کامل نوار آمار + pagination + رنگ‌بندی نام خط در دکل‌ها

- **دکمه‌های قبلی/بعدی pagination به وسط منتقل شدند** (هم در خطوط و هم در دکل‌ها) — قبلاً در سمت چپ بودند. حالا با آیکون chevron راست‌چین‌شده و شمارنده صفحه در یک کپسول
- **رنگ‌بندی نام خط در جدول دکل‌ها بر اساس ولتاژ** (نه ساختار دکل) — در v2.5.0 فقط جدول خطوط رنگ‌بندی داشت. حالا در جدول دکل‌ها هم ستون «نام خط» با بج رنگی (۴۰۰ بنفش | ۲۳۰ قرمز | ۱۳۲ سبز | ۶۳ آبی) نمایش داده می‌شود. ساختار دکل رنگ‌بندی بج خودش را حفظ کرد
- **تغییر در PHP:** `towers.php` در سه SELECT به جدول lines JOIN می‌شود و `voltage_kv` را هم برمی‌گرداند + در `formatTowerRow` فیلد `voltage_kv` اضافه شد
- **کارت «تفکیک بر اساس ولتاژ» در بخش دکل‌ها** (جدید) — تعداد دکل‌ها را به تفکیک ولتاژ (۴۰۰/۲۳۰/۱۳۲/۶۳ kV) نشان می‌دهد با چیپ‌های رنگی هماهنگ با جدول

#### 🎨 کامپوننت جدید StatsBarManager (درخواست ویژه کاربر):
- **فایل جدید:** `src/components/stats-bar-manager.tsx` — کامپوننت مدیریت کارت‌های آمار با قابلیت‌های:
  - **انتخاب کارت‌ها:** کاربر می‌تواند از بین همه کارت‌های موجود، تا ۵ کارت را فعال کند (بقیه در دیالوگ ویرایش قابل مشاهده هستند اما نمایش داده نمی‌شوند)
  - **drag-and-drop جابجایی:** با کتابخانه `@dnd-kit/sortable` ترتیب کارت‌ها را با ماوس بکشید و جابجا کنید — هم در نوار اصلی و هم در دیالوگ ویرایش
  - **ذخیره per-user:** ترتیب و انتخاب کارت‌ها در `localStorage` با کلید `powerline_stats_<userId>_<layoutKey>` ذخیره و در ورود بعدی بازیابی می‌شود
  - **دکمه «ویرایش»** به‌عنوان کارت مجزا در انتهای نوار — کلیک کنید → دیالوگ ویرایش باز می‌شود
  - **شمارنده فعال‌ها** با هشدار هنگام رسیدن به حد ۵ کارت
  - **دکمه «بازنشانی به پیش‌فرض»** برای برگرداندن انتخاب و ترتیب به حالت اولیه
- **بازنویسی `lines-stats-bar.tsx`:** حالا ۷ کارت ممکن دارد (تعداد خطوط، جمع طول، جمع دکل‌ها، تفکیک ولتاژ، خطوط فعال، جمع مدارها، سلامت داده) — ۵ تای پیش‌فرض فعال
- **بازنویسی `towers-stats-bar.tsx`:** حالا ۷ کارت ممکن دارد (تعداد کل، تفکیک ولتاژ، دارای GPS، متصل به خط، نوع پایه، دکل‌های فعال، سلامت داده)

#### 🎨 زیباسازی کارت‌ها:
- **گرادیان بکگراند** برای هر کارت با رنگ theme آن (مثلاً indigo: from-indigo-50 via-white to-indigo-100/60)
- **آیکون با گرادیان و سایه** (مثلاً from-indigo-500 to-indigo-600 text-white shadow-md)
- **انیمیشن hover:** کارت‌ها هنگام hover کمی بالا می‌آیند (-translate-y-0.5) و سایه بزرگ‌تر می‌شود
- **همه کارت‌ها در یک خط** با `flex + overflow-x-auto` — دیگر کارت تفکیک ولتاژ به خط دوم نمی‌رود
- عرض ثابت ۱۷۶px برای هر کارت + ارتفاع ۸۸px
- **drag handle ظاهر روی hover** — دایره کوچک در گوشه بالا-چپ کارت با آیکون GripVertical
- **ring هایلایت** هنگام درگ و هنگام فعال بودن کارت سلامت داده

#### 🚀 استقرار: فایل‌های وب + `api_powerline/endpoints/towers.php` روی هاست (تغییر SQL ندارد)

#### 🛠️ فایل‌های جدید:
- `src/components/stats-bar-manager.tsx`

#### 🛠️ فایل‌های تغییر یافته:
- `src/components/data-table.tsx` — pagination به وسط + آیکون‌های chevron
- `src/components/lines/lines-stats-bar.tsx` — بازنویسی کامل با StatsBarManager + ۷ کارت
- `src/components/towers/towers-stats-bar.tsx` — بازنویسی کامل + کارت تفکیک ولتاژ دکل
- `src/components/pages/towers-page.tsx` — بج رنگی نام خط بر اساس ولتاژ
- `api_powerline/endpoints/towers.php` — JOIN با lines برای voltage_kv
- `src/app/api/proxy/mock-data.ts` — voltage_kv در mock towers

---

### v2.5.2 — 2026-08-23
**نوع:** PATCH — fallback به mock در محیط توسعه + رفع خطای 405 لاگین

- **مشکل:** هنگام لاگین خطای 405 (Method Not Allowed) از nginx سرور sabadgame.com نمایش داده می‌شد — سرور PHP به‌صورت موقت در دسترس نبود و کاربر نمی‌توانست وارد سیستم شود
- **رفع مشکل:** در پروکسی Next.js، اگر سرور اصلی 405 یا خطای 5xx برگرداند یا HTML خطا بدهد، در محیط توسعه به‌طور خودکار به داده‌های mock محلی برمی‌گردیم تا UI قابل تست بماند. در production، خطا مستقیم به کلاینت برمی‌گردد
- فایل جدید: `src/app/api/proxy/mock-data.ts` — داده‌های نمونه برای auth/login، lines، towers، dashboard و همه endpoint‌ها (۷ خط نمونه با ولتاژهای ۴۰۰/۲۳۰/۱۳۲/۶۳ + ۹۰ دکل نمونه)
- فایل تغییر یافته: `src/app/api/proxy/[...path]/route.ts` — تشخیص خطای nginx/PHP و fallback به mock
- کاربر نمونه: `admin` / `admin123` (همان کاربری که در سرور اصلی هم استفاده می‌شود)
- کارت پیش‌فرض: «admin / admin123» در صفحه لاگین به‌طور خودکار نمایش داده می‌شود
- در محیط production (NODE_ENV=production)، mock به‌هیچ‌وجه فعال نمی‌شود — همیشه به سرور اصلی وصل می‌شود

#### 🚀 استقرار: فقط فایل‌های وب (تغییر PHP و SQL ندارد)

---

### v2.5.1 — 2026-08-23
**نوع:** PATCH — دانلود خطاهای import به‌صورت اکسل برای اصلاح و بارگذاری مجدد

- **دکمه «دانلود خطاها به‌صورت اکسل»** زیر لیست خطاهای import — فایل اکسل با هدر قرمز، شامل: شماره ردیف در فایل اصلی، علت خطا، و همه ستون‌های ردیف ناموفق تا تک‌تک آن‌ها را اصلاح و دوباره بارگذاری کنید
- استایل‌بندی حرفه‌ای اکسل: هدر قرمز سفید، ستون «علت خطا» هایلایت قرمز، RTL، فریز هدر، عرض خودکار ستون‌ها
- نام فایل: `import-errors-<entityName>-YYYY-MM-DD.xlsx`
- نوع `failedDetails` گسترش یافت تا `rowData` ردیف اصلی را هم نگه دارد (هم در حالت بچ و هم تک‌به‌تک)

#### 🚀 استقرار: فقط فایل‌های وب (تغییر PHP و SQL ندارد)

---

### v2.5.0 — 2026-08-23
**نوع:** MINOR — بازطراحی نمایش نام خط + فعال‌سازی واقعی چیدمان per-user

- **حذف نمایش inline «نام مجموعه خط» از ستون «نام خط»:** در v2.4.3 نام مجموعه خط با رنگ کم‌رنگ قبل از نام خط در همان سلول نمایش داده می‌شد. حالا این نمایش inline حذف شد و هر کدام ستون مستقل خود را دارند
- **ستون «نام مجموعه خط» جدا شد و به‌طور پیش‌فرض قبل از ستون «نام خط» قرار گرفت** (به‌جای `hidden: true` در v2.4.3). ترتیب پیش‌فرض جدید: کد خط → کد دیسپاچینگ → **نام مجموعه خط** → **نام خط** → ولتاژ → ...
- **بج بکگراندی برای نام خط بر اساس ولتاژ:** به‌جای رنگ متن ساده، نام خط حالا در یک بج بکگراندی (rounded pill) نمایش داده می‌شود — هماهنگ با چیپ‌های «تفکیک ولتاژ» در نوار آمار و بج ستون «فعال». رنگ‌ها:
  - ۴۰۰ کیلوولت: بکگراند بنفش روشن، متن بنفش تیره
  - ۲۳۰ کیلوولت: بکگراند قرمز روشن، متن قرمز تیره
  - ۱۳۲ کیلوولت: بکگراند سبز روشن، متن سبز تیره
  - ۶۳ کیلوولت: بکگراند آبی روشن، متن آبی تیره
  - سایر/خالی: بکگراند خاکستری روشن
- **فعال‌سازی واقعی ذخیره per-user چیدمان ستون‌ها:** در v2.4.3 زیرساخت `layoutKey` در `DataTable` ساخته شد ولی هیچ صفحه‌ای آن را پاس نمی‌داد! حالا `layoutKey="lines"` به LinesPage و `layoutKey="towers"` به TowersPage پاس می‌شود. ترتیب جابجایی و مخفی‌سازی ستون‌های هر کاربر در `localStorage` با کلید `powerline_dt_layout_<userId>_<layoutKey>` ذخیره و در ورود بعدی بازیابی می‌شود
- تابع کمکی `voltageColor` (که فقط رنگ متن برمی‌گرداند) به `voltageBgClass` (بج کامل با بکگراند و متن) ارتقا یافت — هماهنگ با `voltageChipClass` در `lines-stats-bar.tsx`

#### 🛠️ فایل‌های تغییر یافته:
- `src/components/pages/lines-page.tsx` — چیدمان ستون‌ها + رندر نام خط + فعال‌سازی layoutKey
- `src/components/pages/towers-page.tsx` — فعال‌سازی layoutKey
- `src/components/import-excel-dialog.tsx` — دکمه دانلود خطاها به اکسل + ذخیره rowData

#### 🚀 استقرار: فقط فایل‌های وب (تغییر PHP و SQL ندارد)

---

### v2.4.3 — 2026-08-22
**نوع:** MINOR — خطوط/دکل‌ها بر اساس بازخورد کاربری

- **حذف کامل «نوع خط»** (انتقال/فوق توزیع/توزیع): از جدول، فرم ثبت/ویرایش، تغییرات گروهی، import و PHP — مهاجرت: `database/migration_v2.4.3.sql` (DROP COLUMN)
- **رنگ‌بندی ولتاژ روی نام خط:** ۴۰۰ بنفش | ۲۳۰ قرمز | ۱۳۲ سبز | ۶۳ آبی؛ نام مجموعه خط با رنگ کم‌رنگ قبل از نام خط
- **کارت آماری «تفکیک ولتاژ»** به‌جای تفکیک نوع — هر عدد به رنگ ولتاژ خودش (داشبورد هم به‌روز شد)
- **چیدمان ستون‌های جدول per-user:** ترتیب/مخفی‌سازی هر کاربر در مرورگرش ذخیره و در ورود بعدی بازیابی می‌شود (خطوط و دکل‌ها)
- **پاپ‌آپ «سلامت داده»:** پنجرهٔ مرتب با هدر رنگی، شماره‌گذاری خطاها و اسکرول (به‌جای tooltip خام) — خطوط و دکل‌ها
- کارت کششی/آویزی دکل‌ها تک‌خطی شد: «کششی: ۴۳۱ | آویزی: ۳۰۲۱»

#### 🚀 استقرار: فایل‌های وب + `lines.php` و `dashboard.php` روی هاست + اجرای `migration_v2.4.3.sql` در phpMyAdmin

---

### v2.4.2 — 2026-08-22
**نوع:** PATCH — پایداری و پویایی import

- **بچ تطبیقی:** بچ‌ها به ۲۰۰ ردیف برگشت (بچ ۵۰۰تایی روی هاست اشتراکی تایم‌اوت می‌زد — ۲۵۷ ردیف یکجا همه خطا خوردند). اگر بچ با خطای شبکه/تایم‌اوت شکست بخورد، به‌صورت بازگشتی نصف و دوباره تلاش می‌شود (تا ۲۵ ردیف)
- **مهلت ۶۰ ثانیه** برای درخواست‌های bulk-import (بقیه درخواست‌ها همان ۳۰ ثانیه)
- **پیشرفت لحظه‌ای:** نوار از شروع هر بچ به سمت انتهای آن نرم حرکت می‌کند (نه فقط بعد از پایان بچ)؛ در لغو، به درصد واقعی می‌رسد نه ۱۰۰

#### 🚀 استقرار: فقط فایل‌های وب (تغییر PHP و SQL ندارد)

---

### v2.4.1 — 2026-08-22
**نوع:** PATCH — رفع کرش + گزارش خطای حرفه‌ای import

- **رفع کرش صفحات خطوط/دکل‌ها:** `useEffect` از import جا مانده بود → ReferenceError هنگام باز شدن دیالوگ import
- **حذف تطبیق هوشمند نام خط:** طبق تصمیم کاربر — نام خط فقط با تطابق دقیق (نرمال‌شده) یا کد خط match می‌شود؛ نام‌های متفاوت صریحاً خطا می‌دهند
- **گزارش خطای ردیفی دقیق (همه importها):**
  - اعتبارسنجی محلی قبل از ارسال: نام/کد خط نامعتبر، کد خط خالی، شماره دکل نامعتبر و... با پیام فارسی واضح مثل «نام خط «X» با هیچ خط ثبت‌شده‌ای مطابقت ندارد — ابتدا در بخش خطوط انتقال ثبتش کنید»
  - سرور آرایه `errors` ردیفی برمی‌گرداند (نه فقط اولین خطای بچ) — towers.php و lines.php
  - لیست کامل خطاها با شماره ردیف اکسل + علت، داخل دیالوگ، با باکس قرمز و اسکرول (تا ۵۰۰ مورد)
- کد دکل خودکار و تولید line_id از تطابق دقیق حفظ شد

#### 🚀 استقرار: فایل‌های وب + `api_powerline/endpoints/towers.php` و `lines.php` روی هاست (تغییر دیتابیسی ندارد)

---

### v2.4.0 — 2026-08-22
**نوع:** MINOR — سرعت import + تطبیق هوشمند + تجربه کاربری پیشرفت/خطا

- **سرعت import انبوه:** اندازه بچ از ۲۰۰ به ۵۰۰ (سقف سرور) + وقفه بین بچ‌ها از ۳۰۰ms به ۱۰۰ms + حذف کوئری `exists` برای هر ردیف در سرور (پیش‌بارگذاری کدهای موجود خط در حافظه) → انتظار ۴۰-۶۰٪ سریع‌تر
- **تطبیق هوشمند نام خط (import دکل):** نام خط فایل اکسل به نام استاندارد خطوط دیتابیس map می‌شود — تطابق دقیق نرمال‌شده → سپس تطبیق توکنی با امتیازدهی محتوای پرانتز («501 پایه چوبی»/«17 پایه فلزی») و ترجیح نسخهٔ بدون «مشترک» در ابهام. کد دکل و line_id خالی خودکار تولید می‌شوند.
- **قفل تغییر خط در ویرایش دکل:** خط دکل در حالت ویرایش غیرقابل تغییر است — با هشدار «از بخش خطوط انتقال تغییر دهید» (نام خط از JOIN می‌آید؛ تغییر نام خط در بخش خطوط به‌صورت خودکار روی همه دکل‌ها اعمال می‌شود)
- **نمایش جزئیات خطاها در دیالوگ import:** ۱۰ خطای نخست با شماره ردیف اکسل + متن خطا، داخل خود دیالوگ (نه فقط کنسول)
- **نوار پیشرفت نرم:** حرکت پیوسته بین بچ‌ها (interpolation) + برچسب «دستهٔ X از Y» + شمارنده ثانیه — دیگر در بچ‌های کند ثابت نمی‌ماند

#### 🚀 استقرار: فایل‌های وب + `api_powerline/endpoints/towers.php` روی هاست (تغییر دیتابیسی ندارد)

---

### v2.3.1 — 2026-08-22
**نوع:** PATCH — رفع باگ URL + تولید خودکار کد دکل در import

- **رفع باگ حذف انبوه/ورود انبوه (404):** فراخوانی `apiClient.post("lines/bulk-delete")` بدون اسلش ابتدایی، URL غلط `/api/proxylines/...` می‌ساخت و Next خودش 404 می‌داد (پیام گمراه‌کننده «PHP آپلود نشده»). حالا `buildUrl` جداکننده را خودش اضافه می‌کند.
- **تشخیص دقیق 404:** پیام «فایل‌های PHP آپلود نشده» فقط وقتی روتر PHP واقعاً «مسیر پیدا نشد» بدهد نمایش داده می‌شود.
- **رفع باگ SQL دکل‌ها (۵۰۰):** در INSERT تکی و انبوه دکل‌ها یک `?` اضافه قبل از `ST_GeomFromText` بود → خطای `Column count doesn't match value count` و پاسخ ۵۰۰؛ ثبت دستی و import دکل عملاً از v2.1.0 خراب بود. هر دو اصلاح شد.
- **تولید خودکار کد دکل در import:** اگر ستون «کد دکل» در اکسل خالی باشد، از «نام خط» خط پیدا شده و کد `کد خط-شماره سه‌رقمی` (مثل 61404-001) ساخته می‌شود — همان قاعده سرور. نام خط با نرمال‌سازی (نیم‌فاصله/ی عربی/فاصله اضافی) تطبیق می‌خورد و `line_id` هم خودکار resolve می‌شود.
- **چک تکراری:** کدها قبل از تشخیص تکراری تولید می‌شوند؛ تکراریِ داخل فایل و موجود در دیتابیس طبق حالت «رد/ویرایش» دیالوگ import مدیریت و در سرور هم رد می‌شود.
- کوتاه‌سازی متن دیالوگ تأیید حذف خطوط + هشدار زردرنگ حفظ عیوب/سوابق.

#### 🚀 استقرار: فایل‌های وب + `api_powerline/endpoints/towers.php` روی هاست (تغییر دیتابیسی ندارد)

---

### v2.3.0 — 2026-08-22
**نوع:** MINOR — ورود انبوه + رفع باگ‌های انتخاب و import

- **ورود انبوه اکسل**: endpointهای جدید lines/bulk-import و towers/bulk-import — هر درخواست ۲۰۰ ردیف در یک تراکنش؛ ۴۰۰۰ دکل ≈ ۲۰ درخواست (چند ثانیه) به‌جای ۴۰۰۰ درخواست (۲۰+ دقیقه)
- پاسخ ردیفی: آرایه statuses برای شمارش دقیق درج/ویرایش/خطا
- **دکمه «توقف»** حین import — بین بچ‌ها بررسی و متوقف می‌شود (قبلاً انصراف کار نمی‌کرد)
- **رفع باگ انتخاب چندصفحه‌ای**: چک‌باکس سرصفحه حالا فقط صفحه فعلی را انتخاب/لغو می‌کند و انتخاب صفحات دیگر حفظ می‌شود + حالت indeterminate
- حذف انبوه: اگر PHP هاست قدیمی باشد (خطای 404)، پیام واضح «فایل‌های PHP جدید آپلود نشده» نشان داده می‌شود و ادامه نمی‌دهد

#### 🚀 استقرار: فایل‌های PHP بسته API روی هاست + فایل‌های وب (SQL جدیدی ندارد)

---

### v2.2.0 — 2026-08-22
**نوع:** MINOR — حذف انبوه سریع + انتخاب همه ردیف‌ها + نوار پیشرفت + پایداری شبکه

**۱) endpoint حذف انبوه (PHP — نیازمند آپلود)**
- `POST /lines/bulk-delete` و `POST /towers/bulk-delete` با بدنه `{"ids": [...]}`
- هر درخواست تا ۵۰۰۰ ردیف را داخل یک تراکنش حذف می‌کند — به‌جای صدها درخواست جداگانه
- نتیجه: حذف ۱۰۰۰ ردیف ≈ ۲ درخواست (چند ثانیه) به‌جای ۱۰۰۰ درخواست (چند ساعت)

**۲) دکمه «انتخاب همه» در همه صفحات (DataTable)**
- کنار شمارنده انتخاب، دکمه «انتخاب همه (N)» اضافه شد — همه ردیف‌های فیلترشده در همه صفحات یکجا انتخاب می‌شوند
- با فیلتر/جستجو هماهنگ است (فقط نتایج فعلی)

**۳) نوار پیشرفت حذف**
- هنگام حذف، دیالوگ تأیید نوار پیشرفت قرمز + شمارنده «X از Y» نشان می‌دهد

**۴) تایم‌اوت ۳۰ ثانیه‌ای درخواست‌ها (api-client)**
- درخواست معلق به‌جای چرخش بی‌نهایت (علت قفل‌شدن import در ۶۰٪)، پیام واضح می‌دهد و ادامه می‌دهد

**۵) Import پایدارتر**
- وقفه ۲۰۰ms بین هر بچ — فشار روی سرور کم می‌شود و اتصال drop نمی‌شود
- خطای هر ردیف بقیه را متوقف نمی‌کند (از قبل) + حالا با تایم‌اوت، ردیف معلق رد می‌شود

**⚠️ عیب‌یابی حادثه 2026-08-22:** حذف تک‌تک + import سنگین باعث انبوه درخواست شد و هاست از دسترس خارج شد (timeout از همه نقاط). پس از بازگشت هاست، این نسخه را نصب کنید تا تکرار نشود.

#### 🚀 استقرار: فایل‌های PHP بسته API را روی هاست آپلود کنید (تغییر دیتابیسی ندارد) + فایل‌های وب.

---

### v2.1.1 — 2026-08-21
**نوع:** PATCH — اصلاحات فیدبکی ماژول دکل‌ها

- ستون جدول: «خط» → **«نام خط»** با نمایش نام فارسی خط (به‌جای کد عددی)
- رفع باگ انتخاب خط در فرم افزودن دکل — فیلد به select بومی HTML تبدیل شد (همیشه کار می‌کند) + نمایش وضعیت بارگذاری و Toast خطا در صورت شکست دریافت لیست
- کارت‌های نوار آمار: «تعداد کل دکل ها»، «دکل های دارای GPS»، «دکل های متصل به خط»
- کارت چهارم: تعداد **دکل‌های کششی و آویزی** (به‌جای تفکیک ساختار)
- لیست خطوط در عملیات گروهی «اتصال به خط» هم فقط نام فارسی را نشان می‌دهد
- SQL: فایل اصلی `Powerline_towers_v2.1.0.sql` بازنویسی شد — `DELETE` به‌جای `TRUNCATE` (رفع خطای #1701 کلید خارجی) + `DROP COLUMN IF EXISTS` برای اجرای مجدد امن؛ فایل `Powerline_towers_v2.1.0_fix.sql` برای ادامه اجرای ناقص اضافه شد

---

### v2.1.0 — 2026-08-21
**نوع:** MINOR — بازسازی ساختار دکل‌ها بر اساس اکسل رسمی کاربر (Towers.xlsx)

**۱) ساختار جدید بر اساس اکسل شما — با واژگان خودتان:**
- ستون‌ها: کد دکل، شماره دکل، خط، ساختار دکل (مشبک فلزی و...)، نوع دکل (NN/LT)، نوع پایه (کششی/آویزی)، ارتفاع پایه‌های A تا D، مقره‌ها، GPS، سرپرست خط
- ستون‌های اضافی حذف شدند: `structure_type`، `insulator_type` (مشترک)، `construction_date`، `altitude_m`
- نوع دکل داخلی (lattice_steel و...) از ساختار دکل به‌صورت خودکار مشتق می‌شود و در UI وجود ندارد

**۲) کد دکل خودکار**
- فرمول: کد خط + شماره سه‌رقمی — مثال: `61404-001` (صفرها برای هم‌طول شدن)
- در فرم ثبت/ویرایش خودکار از خط و شماره تولید می‌شود (فقط‌خواندنی)
- در API هم اگر ارسال نشود خودکار تولید می‌شود؛ با تغییر خط/شماره در ویرایش، کد بازتولید می‌شود

**۳) نوع مقره هر فاز جداگانه (۶ فیلد)**
- مدار اول: R/S/T و مدار دوم: R/S/T — هرکدام نوع (سرامیکی/شیشه‌ای/سیلیکونی) + تعداد
- فرم: کارت مجزا برای هر فاز با کامبوباکس نوع + ورودی تعداد
- عملیات گروهی جدید: «نوع مقره (هر ۶ فاز)» — یک نوع روی هر ۶ فاز یکجا اعمال می‌شود

**۴) فرم دکل — چیدمان اصلاح‌شده**
- همه فیلدها دو ردیفی: لیبل کوتاه در ردیف بالا، ورودی در ردیف پایین (رفع مشکل به‌هم‌ریختگی)
- با انتخاب خط، «سرپرست خط» خودکار از خط پر می‌شود (قابل ویرایش)

**۵) عملیات گروهی (واژگان جدید):** فعال/غیرفعال، ساختار دکل، نوع دکل (NN/LT)، نوع پایه، نوع مقره (هر ۶ فاز)، سرپرست خط، اتصال به خط

**۶) بازسازی داده — فایل `sql/Powerline_towers_v2.1.0.sql`:**
- حذف ۴ ستون اضافی + پاک‌سازی کامل داده‌های قبلی + درج ۳,۴۵۲ دکل سالم از اکسل
- همه دکل‌ها به خطوط متصل شدند (۱۸ تطبیق خودکار نام + ۶ نگاشت دستی موارد با عبارت متفاوت)
- کد دکل‌ها با فرمول جدید تولید شد، سرپرست از خط به ارث رسید، geom از GPS ساخته شد
- ۱ ردیف خالی اکسل نادیده گرفته شد

**۷) ستون `geom` نگه داشته شد (توضیح):** همان مختصات GPS در فرمت هندسی است که endpoint «دکل‌های نزدیک» (/towers/nearby) به آن نیاز دارد؛ در UI نمایش داده نمی‌شود و خودکار از lat/lng نگهداری می‌شود.

#### 🚀 مراحل استقرار:
1. بکاپ از دیتابیس بگیرید
2. `Powerline_API_v2.1.0.zip` را روی هاست آپلود کنید
3. فایل `sql/Powerline_towers_v2.1.0.sql` را در phpMyAdmin اجرا کنید (حذف ستون‌ها + درج ۳,۴۵۲ دکل جدید)
4. اپ وب: پوشه src + VERSION.md + package.json (اگر Vercel → redeploy)

#### 🛠️ فایل‌های تغییر یافته:
- `src/components/towers/create-tower-dialog.tsx` (بازطراحی کامل)
- `src/components/towers/bulk-towers-actions.tsx`، `towers-stats-bar.tsx`
- `src/components/pages/towers-page.tsx`، `map-page.tsx`
- `src/lib/types.ts`، `src/lib/towers-quality.ts`
- **API:** `endpoints/towers.php`
- **SQL:** `sql/Powerline_towers_v2.1.0.sql` (جدید)

---

### v2.0.0 — 2026-08-21
**نوع:** MAJOR — بازسازی ماژول دکل‌ها + ماژول جدید «نقشه‌ها» (طبق جدول نسخه‌بندی: ماژول جدید = MAJOR)

**۱) ماژول جدید «نقشه‌ها» در منوی اصلی** (`src/components/pages/map-page.tsx`)
- نقشه GIS دکل‌ها از صفحه دکل‌ها جدا شد و به‌صورت ماژول مستقل با آیتم منوی اختصاصی آمد
- نقشه تمام‌صفحه با ارتفاع داینامیک، جستجوی زنده بر اساس کد دکل / خط، شمارنده «کل دکل‌ها» و «روی نقشه»
- کلیک روی هر دکل → کارت جزئیات (خط، ساختار، ارتفاع، مختصات)
- فقط دکل‌های دارای GPS نمایش داده می‌شوند

**۲) بازسازی کامل صفحه دکل‌ها — هم‌تراز با صفحه خطوط**
- نوار آمار: تعداد دکل‌ها، دارای GPS، متصل به خط، انواع دکل (۳ نوع پرتکار + شمارنده کششی)، کارت سلامت داده (کلیک → فیلتر خطاها)
- جدول حرفه‌ای DataTable با ۲۵ ستون (کد، شماره، نوع با بج رنگی، ساختار کششی/آویزی، خط، ارتفاع پایه‌ها A-D، انواع و تعداد مقره‌های فازی ۶گانه، GPS، سرپرست و...) — سورت/فیلتر/مخفی‌سازی ستون + صفحه‌بندی
- دکمه کپی دکل (Duplicate) — فرم با پیش‌پر کردن مقادیر به‌جز کد دکل
- عملیات گروهی (۹ عمل): فعال/غیرفعال، نوع دکل، ساختار کششی/آویزی، نوع سازه، نوع مقره، سرپرست خط و **اتصال گروهی دکل‌ها به خط**
- ستون سلامت داده: بدون GPS / بدون شماره / بدون نوع سازه / ساختار نامشخص — با تولتیپ جزئیات
- Import اکسل (با قالب فارسی و تبدیل خودکار)، Export استایل‌دار، چاپ گزارش، کپی TSV، حذف گروهی با تأیید
- فرم ثبت/ویرایش دکل با ۵ سکشن رنگی (اطلاعات پایه، مشخصات سازه، ارتفاع پایه‌ها، مقره‌ها، GPS) — انتخاب خط اختیاری

**۳) توسعه API دکل‌ها (PHP — نیازمند آپلود روی هاست)**
- `GET /towers`: همه فیلدها برمی‌گردند (ارتفاع پایه‌ها، مقره‌های فازی R/S/T دو مدار، کد نوع NN/AN، سرپرست) + جستجو در شماره/نوع سازه/سرپرست
- `POST /towers`: `line_id` دیگر اجباری نیست (مطابق داده‌های فعلی که دکل‌ها هنوز متصل نیستند) + پذیرش همه فیلدها
- `PUT /towers/{id}`: ویرایش همه فیلدها + **اتصال/قطع اتصال دکل به خط** (line_id)
- `DELETE /towers/{id}`: تبدیل از soft delete به HARD DELETE (هماهنگ با خطوط) — ارجاع‌های عیوب/بازدیدها/دستورکارها/حوادث قبل از حذف null می‌شوند
- `config.php`: `MAX_PAGE_SIZE` از ۱۰۰ به ۱۰۰٬۰۰۰ افزایش یافت — برای بارگذاری ~۲٬۶۰۰ دکل یکجا در جدول سمت کلاینت

#### 🚀 مراحل استقرار:
1. بسته API (`Powerline_API_v2.0.0.zip`) را روی هاست آپلود کنید — **قبل از تست صفحه دکل‌ها ضروری است** (بدون آن فقط ۱۰۰ ردیف اول نمایش داده می‌شود و فیلدهای جدید خالی‌اند)
2. SQL جدیدی لازم نیست — تغییر دیتابیسی وجود ندارد
3. اپ وب: پوشه src + VERSION.md + package.json (اگر Vercel → redeploy)

#### 🛠️ فایل‌های جدید:
- `src/components/pages/map-page.tsx`
- `src/components/towers/towers-stats-bar.tsx`
- `src/components/towers/bulk-towers-actions.tsx`
- `src/components/towers/create-tower-dialog.tsx`
- `src/lib/towers-quality.ts`

#### 🛠️ فایل‌های تغییر یافته:
- `src/components/pages/towers-page.tsx` (بازنویسی کامل)
- `src/lib/types.ts` (تایپ Tower کامل شد)
- `src/components/dashboard-layout.tsx` + `src/app/page.tsx` (منو و روت نقشه‌ها)
- **API:** `endpoints/towers.php`، `config.php`

---

### v1.9.0 — 2026-08-21
**نوع:** MINOR — قابلیت‌های جدید صفحه خطوط (بازبینی‌شده طبق فیدبک کاربر)

**۱) نوار آمار بالای جدول** (`src/components/lines/lines-stats-bar.tsx`)
- کارت‌های: تعداد خطوط، جمع طول خط (کیلومتر)، جمع تعداد دکل‌ها
- تفکیک نوع خط با همان رنگ‌بندی بج‌های جدول (انتقال/فوق توزیع/توزیع)
- کارت «سلامت داده» — با کلیک، فیلتر خطوط دارای خطا فعال/غیرفعال می‌شود
- آمار بر اساس داده فیلترشده محاسبه می‌شود (با جستجو هماهنگ است)

**۲) دکمه کپی خط (Duplicate)**
- دکمه در نوار ابزار جدول (آیکون + سبز) — مثل ویرایش، دقیقاً یک ردیف باید انتخاب شود
- فرم ثبت خط جدید با پیش‌پر کردن همه مقادیر از خط مبدأ؛ فقط «کد خط» خالی است
- عنوان فرم: «افزودن خط جدید (کپی از: ...)» — مناسب ثبت سریع مدارهای مشترک و تیاف‌ها

**۳) عملیات گروهی** (`src/components/lines/bulk-lines-actions.tsx`)
- دکمه در همان ردیف دکمه‌های اصلی جدول (پراپ `toolbarExtra` در DataTable) — فضای اضافی نمی‌گیرد
- منوی کاملاً راست‌چین (dir=rtl) با ۹ عملیات روی ردیف‌های انتخاب‌شده:
  - فعال / غیرفعال کردن
  - نوع خط (انتخاب از مقادیر فارسی)
  - ولتاژ (۶۳/۱۳۲/۲۳۰/۴۰۰ — هر دو فیلد voltage و voltage_kv پر می‌شوند)
  - نوع سیم (ورودی متنی)
  - نوع سازه دکل (ورودی متنی)
  - سرپرست خط، کارشناس خط (ورودی متنی) و پیمانکار (لیست از سرور)
- گزارش نتیجه با تعداد موفق/ناموفق + پاک شدن خودکار انتخاب‌ها بعد از اعمال

**۴) ستون سلامت داده** (`src/lib/lines-quality.ts`)
- ستون «سلامت داده» در انتهای جدول: ✓ سبز یا «⚠ n خطا» کهربایی
- با نگه‌داشتن موس روی علامت، جزئیات خطاها نمایش داده می‌شود
- قواعد بررسی: کل دکل‌ها ≠ کششی+آویزی | جمع منطقه‌ای ≠ کل | طول خط خالی | ولتاژ خالی | تعداد دکل‌ها خالی
- برای «گزارش خطاهای داده»: کارت سلامت داده را در نوار آمار کلیک کنید و از دکمه خروجی، گزینه «فیلتر شده» را بگیرید

**بازبینی طبق فیدبک کاربر (همان روز):**
- فیلترهای سریع چیپی (نوع/ولتاژ/غیرفعال‌ها) حذف شدند — کاربردی نبودند
- منوی عملیات گروهی راست‌چین شد و ۴ عملیات جدید گرفت (نوع خط، نوع سیم، ولتاژ، نوع سازه دکل)
- دکمه عملیات گروهی از ردیف جدا به داخل نوار ابزار جدول منتقل شد
- متن راهنمای دکمه‌ها: «کپی به‌عنوان خط جدید» و «کپی برای اکسل»

**تغییرات فنی همراه:**
- `data-table.tsx`: پراپ‌های جدید `onDuplicate` و `toolbarExtra`
- `create-line-dialog.tsx`: پراپ جدید `duplicateFrom` برای حالت کپی
- بسته API تغییری نکرد — همان v1.8.0 معتبر است

#### 🛠️ فایل‌های جدید:
- `src/components/lines/lines-stats-bar.tsx`
- `src/components/lines/bulk-lines-actions.tsx`
- `src/lib/lines-quality.ts`

---

### v1.8.0 — 2026-08-21
**نوع:** MINOR — رفع باگ‌های بحرانی + سخت‌سازی امنیتی + پاکسازی اسکیمای دیتابیس

> این نسخه حاوی سه گروه تغییر است که به‌صورت مرحله‌ای نسخه‌بندی شدند:
> v1.7.4 (PATCH باگ بحرانی) → v1.7.5 (PATCH رفع دو باگ) → v1.8.0 (MINOR امنیت + اسکیما)

#### 🔴 v1.7.4 — رفع باگ بحرانی ثبت/ویرایش خط (PATCH)

**۱) یکسان‌سازی مقادیر `line_type` در هر سه لایه**
- **مشکل:** مقدار نوع خط در سه لایه سه‌گانه بود — دیتابیس فارسی (`انتقال/فوق توزیع/توزیع`)، اعتبارسنجی PHP انگلیسی (`transmission/...`)، فرم فرانت‌اند انگلیسی. نتیجه: ثبت خط جدید از فرم با خطای دیتابیس شکست می‌خورد و هنگام ویرایش خطوط موجود، کامبوباکس نوع خط مقدار فعلی را نشان نمی‌داد.
- **راه‌حل:** همه‌جا به مقادیر فارسی مطابق enum دیتابیس یکدست شد.
- **فایل‌ها:** `endpoints/lines.php` (اعتبارسنجی POST + افزودن اعتبارسنجی PUT)، `src/components/create-line-dialog.tsx` (مقادیر SelectItem و پیش‌فرض فرم)، `src/lib/types.ts` (تایپ LineType)

#### 🟠 v1.7.5 — رفع دو باگ منطقی (PATCH)

**۱) اصلاح حذف خط (DELETE /lines/{id})**
- **مشکل:** فقط دکل‌های خط حذف می‌شدند؛ عیوب، بازدیدها، دستورکارها، مدارها، تجهیزات و حوادث ایمنی با `line_id` باقی می‌ماندند و خطای FK یا رکورد یتیم تولید می‌کرد.
- **راه‌حل:** کل عملیات داخل تراکنش — دکل‌ها حذف (HARD DELETE)، در ۶ جدول دیگر ارجاع `line_id` به NULL تبدیل می‌شود (حفظ سابقه)، سپس حذف خط. در صورت خطا rollback.

**۲) اصلاح باگ تغییر رمز عبور (POST /auth/change-password)**
- **مشکل:** کد می‌خواست «همه توکن‌ها به‌جز فعلی» را باطل کند ولی هش access token را با هش refresh token های ذخیره‌شده مقایسه می‌کرد — مقایسه همیشه بی‌نتیجه.
- **راه‌حل:** همه رفرش توکن‌ها باطل می‌شوند؛ access token فعلی تا پایان اعتبارش (حداکثر ۱ ساعت) کار می‌کند.

#### 🔵 v1.8.0 — سخت‌سازی امنیتی + پاکسازی اسکیما (MINOR)

**۱) کلید JWT**
- کلید ثابت و قابل حدس با کلید تصادفی ۶۴ کاراکتری جایگزین شد + پشتیبانی از متغیر محیطی `JWT_SECRET` (با تغییر کلید، همه نشست‌های قبلی باطل می‌شوند — یک بار همه کاربران باید دوباره وارد شوند)

**۲) حذف پذیرش توکن از query string** (`?token=`)
- توکن‌ها در لاگ سرور و تاریخچه مرورگر باقی می‌ماندند؛ فقط هدر `Authorization` پذیرفته می‌شود.

**۳) دسترسی خودکار نقش super_admin**
- `Auth::hasPermission()` حالا برای super_admin همیشه true برمی‌گرداند — دیگر با افزودن هر permission جدید نیاز به دسترسی دستی در جدول role_permissions نیست.

**۴) اصلاح هدرهای CORS (Response.php)**
- ارسال `Access-Control-Allow-Credentials: true` فقط وقتی origin مشخص است (ترکیب `*` + credentials طبق استاندارد نامعتبر است).

**۵) خاموش کردن DEBUG_MODE در تولید**
- جزئیات خطا فقط در `api.log` سرور ثبت می‌شود، نه در پاسخ API.

**۶) فایل `sql/migration_v1.8.0.sql` (اجرای ضروری روی دیتابیس)**
- رفع باگ `auth_tokens.expires_at` (حذف `ON UPDATE CURRENT_TIMESTAMP` که تاریخ انقضا را با هر UPDATE ریست می‌کرد)
- حذف ایندکس‌های تکراری: سه UNIQUE اضافی روی `lines.line_code`، یک UNIQUE تکراری روی `contractors.contractor_code` و یک UNIQUE تکراری روی `personnel.personnel_code`

**۷) همگام‌سازی نسخه package.json** (1.3.2 → 1.8.0)

#### 🛠️ فایل‌های تغییر یافته:
- `src/components/create-line-dialog.tsx` — مقادیر فارسی نوع خط
- `src/lib/types.ts` — تایپ LineType فارسی
- `package.json` — نسخه 1.8.0
- **API (سمت سرور):** `config.php`، `lib/Auth.php`، `lib/Response.php`، `endpoints/lines.php`، `endpoints/auth.php`
- **SQL:** `sql/migration_v1.8.0.sql` (در بسته API)

#### 🚀 مراحل استقرار:
1. فایل‌های PHP پوشه `api_powerline` را روی هاست آپلود کنید (جایگزین قبلی‌ها)
2. `sql/migration_v1.8.0.sql` را در phpMyAdmin روی دیتابیس `sabadga2_Powerline` اجرا کنید (قبلش بکاپ!)
3. یک بار از اپ خارج و دوباره وارد شوید (به‌خاطر کلید JWT جدید)

---

### v1.3.0 — 2026-08-20
**نوع:** MINOR — انتقال سورت به داخل پنجره فیلتر + حذف آیکون سورت از هدر

#### 🔄 تغییرات:

**۱) حذف آیکون سورت از هدر ستون‌ها**
- **مشکل:** آیکون سورت قبل از نام ستون قرار داشت و باعث می‌شد نام هدر و فیلدها در یک راستا قرار نگیرند (نا فرم بود).
- **راه‌حل:** حذف کامل دکمه سورت از هدر. حالا فقط نام ستون نمایش داده می‌شود و همه هدرها در یک راستا هستند.

**۲) انتقال گزینه‌های سورت به داخل پنجره فیلتر**
- **قابلیت جدید:** در پنجره فیلتر (که با کلیک روی آیکون قیف باز می‌شود)، یک بخش "مرتب‌سازی" اضافه شد با ۳ دکمه:
  - **صعودی** (آیکون ↑) — مرتب‌سازی از کوچک به بزرگ
  - **نزولی** (آیکون ↓) — مرتب‌سازی از بزرگ به کوچک
  - **حذف** (آیکون ⇅) — حذف مرتب‌سازی
- دکمه فعال شده با رنگ بنفش (`bg-indigo-600 text-white`) متمایز می‌شود
- فقط ستون‌هایی که `sortable: true` دارند، این بخش را نمایش می‌دهند

**۳) بهبور آیکون فیلتر**
- حالا وقتی ستون سورت شده یا فیلتر شده باشد، آیکون قیف بنفش می‌شود (تا کاربر ببیند این ستون فعال است)
- title آیکون از "فیلتر" به "فیلتر و سورت" تغییر کرد

**۴) بهبور دکمه "پاک کردن"**
- حالا هم فیلتر و هم سورت را همزمان پاک می‌کند (قبلاً فقط فیلتر را پاک می‌کرد)

#### 🛠️ فایل‌های تغییر یافته:
- `src/components/data-table.tsx`:
  - حذف `{col.sortable && <button>...</button>}` از هدر
  - افزودن بخش سورت به داخل `DropdownMenuContent`
  - به‌روزرسانی `className` آیکون فیلتر برای تشخیص سورت فعال
  - به‌روزرسانی `onClick` دکمه "پاک کردن"

#### 📋 دستورالعمل اعمال:
```bash
# ۱) آپلود کدهای frontend جدید (build و deploy)
# ۲) ریفرش مرورگر با Ctrl+Shift+R
```

#### 💡 نحوه استفاده:
1. روی آیکون قیف کنار هر ستون کلیک کنید
2. در بخش "مرتب‌سازی"، یکی از گزینه‌های صعودی/نزولی را انتخاب کنید
3. اگر می‌خواهید فیلتر هم اعمال کنید، مقادیر را تیک بزنید و "اعمال فیلتر" را بزنید
4. برای حذف سورت، روی "حذف" کلیک کنید یا از دکمه "پاک کردن" بالای پنجره استفاده کنید

---

### v1.2.9 — 2026-08-20
**نوع:** PATCH — راست‌چین کردن همه ستون‌های جدول

- تنظیم `align: "right"` برای همه ۲۵ ستون
- به‌روزرسانی منطق ترازبندی در `th` و `td` (پیش‌فرض `text-right`)
- حذف منطق قدیمی که `type === "number"` را به‌طور خودکار چپ‌چین می‌کرد

---

### v1.2.8 — 2026-08-20
- کاهش عرض نام خط از 360px به 340px

---

### v1.2.7 — 2026-08-20
- کاهش عرض ستون نام خط از 560px به 360px

---

### v1.2.6 — 2026-08-20
- افزودن کلاس `table-fixed` به المان `<table>` (حل ریشه‌ای مشکل عرض)

---

### v1.2.5 — 2026-08-20
1. کاهش padding از p-3 به p-2
2. کاهش cap ستون‌های کوتاه از 400px به 220px
3. اصلاح کار نکردن جستجوی داخل فیلتر با getFilteredUniqueValues
4. افزودن autoFocus و پیام "موردی یافت نشد"

---

### v1.2.4 — 2026-08-20
1. تغییر ترتیب فیلدها در فرم
2. تغییر نام "نام مجموعه" به "نام مجموعه خط"

---

### v1.2.3 — 2026-08-20
1. اصلاح چیدمان فرم: نام خط و نام مجموعه به تمام عرض
2. پاک کردن error هنگام باز کردن مجدد فرم
3. افزودن تابع `getColumnWidth`
4. افزودن try-catch برای خطای UNIQUE Constraint

---

### v1.2.2 — 2026-08-20
1. رفع اشتباه UNIQUE INDEX روی dispatch_code
2. اصلاح دکمه close
3. افزودن filterable به ستون‌های عددی
4. قابلیت wrap متن

---

### v1.2.1 — 2026-08-20
1. حل خطای duplicate dispatch_code
2. حذف پیام soft delete
3. clear خودکار selectedRows
4. حذف فیلدهای تاریخ از فرم
5. دکمه ویرایش
6. پشتیبانی از حالت ویرایش

---

### v1.2.0 — 2026-08-20
1. تبدیل soft delete به HARD DELETE
2. حذف ستون‌های تاریخ
3. انتقال دکمه close به چپ
4. راست‌چین DialogHeader و DialogFooter
5. تغییر متن "کل دکل‌ها" به "تعداد کل دکل‌ها"
6. بک‌گراند هدر سفید
7. cursor: pointer با !important

---

### v1.1.0 — 2026-08-20
1. ستون چک‌باکس جداگانه
2. badge "X ردیف انتخاب شده"
3. اصلاح دکمه حذف
4. کپی به فرمت TSV

---

### v1.0.1 — 2026-08-20
بازنویسی `formatLineRow()` در PHP

---

### v1.0.0 — 2026-08-20
نسخه پایه

---

## راهنمای نصب

```bash
npm install
npm run dev        # محیط توسعه
npm run build      # بیلد production
npm run start      # اجرای production
```

## پیکربندی

- آدرس API: در `src/lib/api-config.ts` متغیر `API_BASE_URL`
- proxy داخلی: `src/app/api/proxy/[...path]/route.ts` برای حل CORS
- احراز هویت: JWT با refresh token در `src/lib/auth-context.tsx`

## فایل‌های مرجع
- `CHECKLIST.md` — لیست کامل کارهای انجام‌شده و باقی‌مانده
- `VERSION.md` — همین فایل — تاریخچه نسخه‌ها
