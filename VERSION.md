# Powerline Web — v4.3.79 (2026-09-03)

## ۱) رفع باگ: جدول خالی «امور بهره‌برداری» در داده‌های پایه
- علت: endpoint قدیمی لیست امور پاسخ بدون pagination برمی‌گرداند؛ فرانت انتظار ساختار صفحه‌بندی‌شده داشت و نتیجه را خالی تفسیر می‌کرد (در حالی که ثبت تکراری درست پیام «قبلاً ثبت شده» می‌داد)
- بک‌اند: پاسخ لیست امور به‌صورت صفحه‌بندی‌شده (هم‌شکل بقیهٔ ماژول‌ها) برگردانده می‌شود
- فرانت: صفحهٔ ماژول عمومی به هر دو شکل پاسخ (آرایهٔ خام و صفحه‌بندی‌شده) مقاوم شد — با بک‌اند قبلی (۴.۳.۷۸) هم بدون آپلود مجدد درست کار می‌کند
- نتیجه: رکوردهای امور (کردستان، ایلام و...) حالا در جدول داده‌های پایه نمایش داده می‌شوند

## ۲) ویرایش گروهی امور بهره‌برداری — همهٔ جدول‌های دارای امور
- عملیات جدید «تغییر امور بهره‌برداری» در منوی عملیات گروهی با انتخابگر قابل جستجو + گزینهٔ «نامشخص» (پاک کردن امور ردیف‌ها)
- جدول‌های پوشش‌داده‌شده:
  * خطوط انتقال (بسته‌های ۱۰۰تایی — سریع)
  * دکل‌ها (بسته‌های ۱۰۰تایی)
  * پرسنل (بسته‌های ۱۰۰تایی)
  * مدارها، بازدیدها، عیوب، دستورکارها
  * تجهیزات، صورت‌وضعیت‌ها، حوادث ایمنی، حوادث خطوط
- جریان کار یکسان با «تغییر قرارداد»: انتخاب امور → تأیید → اجرا با نوار پیشرفت
- بک‌اند از قبل district_id را در همهٔ PUT و bulk-update ها می‌پذیرد (بدون تغییر دیتابیس نسبت به ۴.۳.۷۸)

## استقرار
1. سورس فرانت‌اند را دیپلوی کنید (یا فایل zip را باز کنید) — رفع باگ نمایش امور بلافاصله فعال می‌شود
2. برای بهبود سرعت/کیفیت، پوشهٔ `Powerline` (بک‌اند) را هم روی هاست آپلود کنید (اختیاری ولی توصیه‌شده — فرانت با هر دو نسخهٔ بک‌اند سازگار است)
3. فایل SQL جدیدی لازم نیست؛ اگر migration نسخهٔ ۴.۳.۷۸ را اجرا کرده‌اید همین کافی است
