# Powerline Web — v4.3.52 (2026-09-02)

## اصلاح این نسخه: حذف «پرش نور صفحه» در عملیات گروهی

### مشکل
بین دیالوگ انتخاب مقدار و دیالوگ تأیید عملیات گروهی، پنجرهٔ اول کامل بسته و دومی باز می‌شد؛
در این فاصله کوتاه نور پس‌زمینه به حالت عادی برمی‌گشت و بلافاصله دوباره تیره می‌شد —
حس پرش/فلش صفحه ایجاد می‌کرد.

### راه‌حل
- دو مرحله در «یک دیالوگ واحد» ادغام شدند: با زدن «اعمال روی همه» پنجره بسته نمی‌شود؛
  فقط محتوای همان پنجره به مرحلهٔ «تأیید عملیات گروهی» و سپس «نوار پیشرفت» عوض می‌شود.
  چون overlay رادیکس هیچ‌وقت unmount نمی‌شود، دیگر فلش نوری وجود ندارد.
- «انصراف» در مرحلهٔ تأیید، به فرم انتخاب مقدار برمی‌گردد (مقدار انتخابی حفظ می‌شود).
- در هر سه بخش اعمال شد: عملیات گروهی دکل‌ها، خطوط و ماژول‌های عمومی (قرارداد گروهی).
- کامپوننت جدید `BulkOperationPanel` از دل `BulkOperationDialog` استخراج شد
  (`BulkOperationDialog` برای سایر مصرف‌کننده‌ها مثل حذف انبوه دیفکت‌ها دست‌نخورده باقی ماند).
- کد مردهٔ فراخوانی `actionMeta` روی نوع never در bulk-towers-actions حذف شد (خطای TS قدیمی).

## فایل‌های تغییریافته
- src/components/bulk-operation-dialog.tsx (افزودن BulkOperationPanel)
- src/components/towers/bulk-towers-actions.tsx
- src/components/lines/bulk-lines-actions.tsx
- src/components/generic-bulk-actions.tsx
- src/lib/version.ts ، package.json (bump به v4.3.52)

## نکته استقرار
- فقط فرانت‌اند؛ تغییر بک‌اندی نسبت به v4.3.51 ندارد (فایل‌های PHP همان نسخهٔ قبل معتبرند).
