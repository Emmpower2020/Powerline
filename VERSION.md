# Powerline Web — v4.3.84 (2026-09-04)

## تغییرات این نسخه — رفع موارد جزئی + ادغام تب نقش‌ها و دسترسی‌ها

### ۱) ادغام تب «نقش‌ها» و «دسترسی‌ها» در یک تب
- صفحهٔ کاربران حالا **دو تب** دارد: «اطلاعات کاربران» + «نقش‌ها و دسترسی‌ها»
  (دو جدول قبلی خیلی شبیه هم بودند — فقط یک ستون دسترسی تفاوت داشتند)
- جدول واحد نقش‌ها همهٔ امکانات را یک‌جا دارد: افزودن، ویرایش، کپی، حذف،
  ایمپورت اکسل، اکسپورت، چاپ + ستون جدید «دسترسی‌ها» (خلاصهٔ «x از ۲۳ بخش ·
  y ابزار» + نوار پیشرفت)
- **کلیک روی ردیف یا روی ستون «دسترسی‌ها» = ویرایش ماتریس دسترسی همان نقش**
  (تکی)؛ منوی «عملیات گروهی دسترسی»: ویرایش گروهی چند نقش، کپی دسترسی از
  نقش دیگر، دسترسی کامل / فقط مشاهده / حذف همه
- ویرایش نام/توضیح/وضعیت نقش با دکمهٔ ویرایش نوار ابزار (همان فرم نقش)

### ۲) حذف فیلد «تلفن» از فرم پرسنل
- در فرم ثبت/ویرایش/کپی پرسنل، فیلد «تلفن» حذف شد — ستونی که نه در جدول
  برنامه نمایش داده می‌شد و نه داده‌ای داشت (موبایل سر جایش است)
- از نگاشت ستون‌های ایمپورت اکسل پرسنل هم حذف شد

### ۳) حذف ایمیل از کل برنامه
- ستون «ایمیل» از جدول کاربران حذف شد
- فیلد ایمیل از فرم ثبت/ویرایش/کپی کاربر و از قالب ایمپورت اکسل کاربران حذف شد
- ایمیل از پروفایل (تنظیمات) و منوی کاربر (بالا-چپ) حذف شد — به‌جایش نام کاربری
  نمایش داده می‌شود
- ستون ایمیل DB دست‌نخورده است (اختیاری بودها فقط خالی می‌مانند) — نیازی به
  تغییر دیتابیس نیست

### ۴) اصلاحات جزئی
- منوی «عملیات گروهی» کاربران: آیتم «تغییر نقش ...» به «تغییر نقش» تغییر کرد
- چیدمان فرم کاربر مرتب شد (رمز عبور کنار وضعیت، امور بهره‌برداری تمام‌عرض)

## نحوه فعال‌سازی
1. فقط فرانت (پوشهٔ `Powerline` داخل zip) روی هاست جایگزین شود — **تغییری در
   بک‌اند و SQL لازم نیست** (بک‌اند ≥ v4.3.83 کافی است)
2. بک‌اند و SQL نسخهٔ ۴.۳.۸۳ (بستهٔ قبل) اگر هنوز اجرا نشده‌اند، همان دستورالعمل
   قبل معتبر است

# Powerline Web — v4.3.83 (2026-09-04)

## تغییرات این نسخه — دسترسی‌های نقش‌محور (RBAC)

### ۱) تب جدید «نقش‌ها» — تعریف نقش‌های سازمان
- صفحهٔ کاربران سه تب شد: «اطلاعات کاربران» + «نقش‌ها» + «دسترسی‌ها»
- جدول نقش‌ها دقیقاً هم‌شکل بقیهٔ جدول‌های برنامه: افزودن، ویرایش، کپی به‌عنوان
  نقش جدید (با کپی ماتریس دسترسی)، حذف (با نوار پیشرفت)، ایمپورت اکسل،
  اکسپورت، چاپ، جستجو/فیلتر/چیدمان ستون
- ستون‌ها: نام نقش، توضیحات، تعداد کاربران، وضعیت (فعال/غیرفعال)، تاریخ ایجاد
- نقش‌های سیستمی (مدیر ارشد، کاربر امور و...): نامشان قفل است و حذف نمی‌شوند
- نقش غیرفعال در کمبوباکس انتخاب نقشِ کاربران نمایش داده نمی‌شود
- قاعدهٔ امنیتی هم‌گام با بقیهٔ جدول‌ها: ردیف فعال حذف نمی‌شود (اول غیرفعال کنید)؛
  نقشِ دارای کاربر هم حذف نمی‌شود (اول نقش کاربران را تغییر دهید)

### ۲) تب «دسترسی‌ها» — ماتریس روی «نقش» (نه هر کاربر)
- ستون «کاربر» با «نقش» عوض شد: دسترسی‌ها یک‌بار برای نقش تعریف می‌شود و
  همهٔ دارندگان همان نقش بلافاصله شامل می‌شوند (مثلاً ۴۰ سیمبان فقط نقش
  «سیمبان» می‌گیرند — لازم نیست تک‌تک تنظیم شوند)
- جدول نقش‌ها با خلاصهٔ دسترسی («x از ۲۳ بخش · y ابزار» + نوار پیشرفت کوچک)
  و تعداد کاربران؛ کلیک روی ردیف = ویرایش ماتریس همان نقش
- منوی «عملیات گروهی دسترسی»: ویرایش گروهی ماتریس چند نقش، کپی دسترسی از
  نقش دیگر، دسترسی کامل / فقط مشاهده / حذف همه — با نوار پیشرفت
- متن توضیح پایین تب حذف شد (طبق درخواست شما)
- پنجرهٔ ماتریس دسترسی عریض شد (تا ۹۵٪ عرض صفحه): **بدون اسکرول افقی** و
  ردیف‌های فشرده‌تر — کار با ۲۳ بخش × ۶ ابزار راحت‌تر شد

### ۳) نقش در مدیریت کاربران
- فرم ثبت/ویرایش/کپی کاربر: کمبوباکس «نقش» (با جستجو + تعداد کاربران هر نقش)
  جایگزین «دسترسی اولیه» شد؛ «نامشخص» = بدون نقش = همهٔ بخش‌ها فقط مشاهده
- ستون «نقش» در جدول کاربران (+فیلتر)؛ بدون نقش با بج زرد «بدون نقش (فقط مشاهده)»
- منوی «عملیات گروهی» کاربران: آیتم جدید «تغییر نقش...» — تخصیص گروهی نقش
  به چند کاربر با یک کلیک (خودتان مستثنی هستید؛ مدیر سیستم نقش ثابت دارد)
- ایمپورت اکسل کاربران: ستون اختیاری «نقش» (نام نقش) پشتیبانی می‌شود
- کاربر بدون نقش: مجوز شخصی قبلی‌اش (اگر از 4.3.81/82 داشته) هنوز اعمال می‌شود

### ۴) بک‌اند (پوشهٔ Powerline) — نسخهٔ v4.3.83
- CRUD کامل /roles: فهرست (با تعداد کاربران و ماتریس)، ثبت، ویرایش
  (نام/توضیح/وضعیت/ماتریس)، حذف با گاردهای فارسی (سیستمی/درحال‌استفاده)
- GET/POST/PUT /users: فیلد role_id (تخصیص تک‌نقشی؛ حذف نقش با null)
  + گاردها: نقش حساب خودتان قابل تغییر نیست؛ مدیر سیستم همیشه نقش مدیر می‌گیرد
- auth/me: نقش کاربر + «دسترسی مؤثر» = ماتریس نقش مقدم بر مجوز شخصی
- گارد مرکزی guardModuleWrite: همان منطق مؤثر (نقش اول، مجوز شخصی پشتیبان)
- نسخهٔ بک‌اند → v4.3.83؛ فرانت قبل از هر نوشتنِ کاربران/نقش‌ها نسخه را چک
  می‌کند تا بک‌اند قدیمی «موفقیت کاذب» نسازد

## نحوه فعال‌سازی
1. **پوشهٔ `Powerline` (بک‌اند جدید) روی هاست جایگزین شود** — لازم برای
   endpoint نقش‌ها، تخصیص role_id و گارد جدید
2. فایل `Powerline_v4.3.83_Roles.sql` (کنار همین بسته) در phpMyAdmin اجرا شود —
   دو ستون جدید roles + دو نقش نمونه (سیمبان/کارشناس بازدید)
3. فرانت جدید (پوشهٔ Powerline در zip) آپلود شود؛ کاربران یک‌بار خروج/ورود کنند

## سازگاری
- بدون اجرای SQL: تب نقش‌ها کار می‌کند ولی ماتریس ذخیره نمی‌شود و نقش‌ها
  «تعریف‌نشده — فقط مشاهده» می‌مانند؛ بدون آپلود بک‌اند: نوشتن نقش/تخصیص
  نقش با پیام واضح بلاک می‌شود (دسترسی‌ها خراب نمی‌شوند)
- ماتریس‌های شخصی تنظیم‌شده در 4.3.82 برای کاربران بدون نقش هنوز معتبرند؛
  به‌محض تخصیص نقش، ماتریس نقش ملاک می‌شود

# Powerline Web — v4.3.81 (2026-09-04)

## تغییرات این نسخه

### ۱) استانداردسازی منوی «ویرایش گروهی»
- آیتم‌های منوی عملیات گروهی همهٔ جدول‌ها بدون پیشوند «تغییر» شدند —
  «قرارداد»، «امور بهره‌برداری»، «سمت»، «سرپرست»، «ولتاژ» و... (مطابق خطوط/دکل‌ها)
- عنوان پنجره‌های ویرایش گروهی همچنان «تغییر گروهی ...» است (یکسان در همهٔ جدول‌ها)

### ۲) قفل «امور بهره‌برداری» برای غیرمدیران
- در فرم‌های ثبت/ویرایش/کپی همهٔ جدول‌ها، فیلد امور برای کاربر اموردار
  **قفل و خودکار** است (امورِ خودِ کاربر) — فقط مدیر سیستم می‌تواند تغییر دهد
- آیتم «امور بهره‌برداری» در منوی ویرایش گروهی برای غیرمدیران نمایش داده نمی‌شود
- ایمپورت اکسل (تک‌ردیفی و انبوه): ستون امور برای کاربر اموردار خودکار پر می‌شود
- بک‌اند (پوشهٔ Powerline): در PUT/bulk-update امورِ ارسالیِ کاربر غیرمدیر نادیده
  گرفته می‌شود؛ در create/bulk-import امور خودکار تحمیل می‌شود — defense in depth
- مدیریت جدول امور (ثبت/ویرایش/حذف امور) فقط برای مدیر سیستم

### ۳) بخش دسترسی‌ها در صفحهٔ «کاربران» (ماتریس کاربر × بخش)
- تب جدید «دسترسی‌های بخش‌ها»: جدول افقی — ردیف = کاربر، ستون = بخش برنامه (۲۲ بخش)
- تیک = دسترسی دارد؛ برداشتن تیک همان لحظه ذخیره می‌شود و بخش از منوی کاربر حذف می‌شود
- مدیر سیستم (بدون امور) ردیف قفل با دسترسی کامل دارد
- کاربر جدید بدون محدودیت، همهٔ بخش‌ها را می‌بیند (پیش‌فرض مثبت)

### ۴) کاربران آزمایشی پرسنل (SQL)
- برای همهٔ پرسنل پیمانکارِ دارای کد ملی، کاربر ساخته می‌شود:
  **نام کاربری = کد ملی | رمز عبور = 123456 | امور = کرمانشاه غربی | فعال**
- نقش «کاربر امور» با همهٔ دسترسی‌های endpoint ها ساخته و تخصیص داده می‌شود
- شناسهٔ دو رقمی امور: **کردستان=10 | ایلام=11 | کرمانشاه غربی=12 | کرمانشاه شرقی=13**
  (با ON UPDATE CASCADE همهٔ ارجاع‌ها خودکار اصلاح می‌شوند؛ امور بعدی از ۱۴)
- کد خطوط/دکل‌ها/مدارها دست‌نخورده می‌ماند (طبق تصمیم شما — کدها یونیک هستند)

## نحوه فعال‌سازی
1. پوشهٔ `Powerline` (بک‌اند جدید) روی هاست جایگزین شود
2. فایل `Powerline_v4.3.81_UsersAccess.sql` (کنار همین بسته) در phpMyAdmin اجرا شود
3. بدون اجرای SQL هم UI جدید کار می‌کند؛ فقط کاربران آزمایشی/ماتریس پس از SQL فعال می‌شوند

## خروجی‌های تست
- منوی عملیات گروهی مدارها/تجهیزات/صورتحصیل‌ها: [فعال، غیرفعال، قرارداد، امور بهره‌برداری] بدون پیشوند
- فرم ثبت مدار با کاربر اموردار: فیلد امور قفل «امور شما (خودکار)»
- ماتریس دسترسی: ذخیرهٔ تیک با PUT /users/{id}
