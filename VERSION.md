# Powerline Web — v4.3.80 (2026-09-04)

## ۱) یکسان‌سازی جایگاه «فعال/غیرفعال» در منوی ویرایش گروهی همهٔ جدول‌ها
- الگوی واحد در سراسر برنامه: «فعال کردن» و «غیرفعال کردن» همیشه **بالای** منوی عملیات گروهی قرار دارند — دقیقاً مثل خطوط/دکل‌ها/پرسنل
- اصلاح شد: مدارها، تجهیزات، صورت‌وضعیت‌ها، حوادث ایمنی، بازدیدها، دستورکارها، عیوب، چک‌لیست‌ها، فهرست بها، سیم‌ها و مراجع دکل (کامپوننت مشترک GenericBulkActions)
- ترتیب جدید منو: فعال/غیرفعال ← جداکننده ← تغییر قرارداد / تغییر امور بهره‌برداری ← عملیات اختصاصی همان جدول
- جداکننده‌ها فقط زمانی رندر می‌شوند که آیتم بعدی وجود داشته باشد (بدون خط اضافی)

## ۲) رنگ‌بندی ولتاژی ستون‌های نام — هماهنگ با دکل‌ها
- **خطوط**: ستون «ولتاژ (kV)» و «نام مجموعه خط» به‌صورت بج رنگی بر اساس ولتاژ درآمدند («نام خط» از قبل رنگی بود)
- **مدارها**: ستون «نام مدار» رنگی شد؛ بج ستون «ولتاژ» به حالت دارک مجهز شد
- نگاشت رنگ واحد در هر سه جدول خطوط/مدارها/دکل‌ها:
  * ۴۰۰ کیلوولت → بنفش | ۲۳۰ → قرمز | ۱۳۲ → سبز | ۶۳ → آبی | نامشخص → خاکستری
  * پشتیبانی کامل حالت دارک (dark:bg-*-950/60 + dark:text-*-400)
- فیلتر/سورت/جستجوی ستون‌های رنگی‌شده بدون تغییر رفتار کار می‌کنند (فقط نمایش رنگی است)

## ۳) شناسهٔ دو رقمی امور بهره‌برداری (کوئری SQL — نیازمند اجرا)
- فایل `Powerline_v4.3.80_DistrictCode.sql` (کنار همین بسته) در phpMyAdmin اجرا شود:
  * ستون `district_code` (دو رقمی، یکتا) به جدول districts اضافه می‌شود
  * ۴ امور فعلی: کردستان=10 ، ایلام=11 ، کرمانشاه غربی=12 ، کرمانشاه شرقی=13 (قابل ویرایش قبل از اجرا)
  * کد هر خط با شناسهٔ امورش پیشوندگذاری می‌شود (90101 → 1290101) — فقط کدهای ۵ رقمی، ایمن برای اجرای مجدد
  * کد ۳٬۴۵۲ دکل به‌طور خودکار از کد خط جدید بازتولید می‌شود (1290101-210)
  * بخش اختیاری: پیشوند کد دیسپاچینگ مدارها (DS936 → 12DS936) — توصیه می‌شود
  * SELECT های بررسی نهایی در انتهای اسکریپت
- این کوئری مستقل از فرانت‌اند است؛ برنامه قبل و بعد از اجرا بدون خطا کار می‌کند

## سازگاری و استقرار
- فرانت‌اند: آپلود بسته جایگزین قبلی — تغییرات ۱ و ۲ بلافاصله فعال می‌شوند (بدون نیاز به بک‌اند یا SQL جدید)
- بدون تغییر ساختاری دیتابیس از سمت خود برنامه؛ فقط در صورت تمایل به شناسهٔ امور، فایل SQL بخش ۳ را اجرا کنید
