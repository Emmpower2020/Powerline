# Powerline Web 4.3.23

- خطوط: حذف کامل ورودی/ارسال `voltage` و استفاده انحصاری از `voltage_kv`.
- API ایجاد خط: حذف اجباری `voltage` از payload پیش از ساخت Query.
- Bulk Import خطوط: فقط ستون‌های واقعی موجود در schema وارد INSERT می‌شوند.
- عملیات گروهی ولتاژ خطوط: فقط `voltage_kv` ارسال می‌شود.
- نوع سیم: نام فارسیِ آوانویسی‌شده + نام انگلیسی کامل، مانند `لینکس (Lynx)`؛ بدون عدد/سطح مقطع/دسته‌بندی.
- خطای تکراری `Unknown column 'voltage' in INSERT INTO` فقط در صورتی ممکن است که نسخه قدیمی API روی هاست اجرا شود؛ فایل `api_powerline/endpoints/lines.php` این نسخه باید روی سرور جایگزین شود.
