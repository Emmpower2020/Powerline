# اتصال عملیات به فهرست‌بها — v4.3.86

این نسخه، فهرست‌بهای رسمی را به عملیات واقعی بازدید و دستورکار متصل می‌کند تا متره و صورت‌وضعیت بر اساس فهرست‌بهای قراردادی تولید شود.

## 1) اجرای دیتابیس

ابتدا فایل زیر را روی **همان دیتابیس فعلی پروژه** اجرا کنید:

`Powerline_v4.3.86_PriceList_Billing.sql`

این فایل افزایشی است و ستون‌های قبلی را حذف یا بازنویسی نمی‌کند. ستون‌های جدید شامل شرایط زمین، نوع بازدید، شرایط تطبیق و جداول متره/افزایش‌کاهش است.

بخش TEST DATA انتهای فایل فقط ستون‌های جدید را برای چند رکورد نمونه پر می‌کند:

- `plain` = دشت و تپه‌ماهور
- `semi_mountainous` = نیمه‌کوهستانی
- `mountainous` = صعب‌العبور

## 2) وارد کردن فایل فهرست‌بها

در صفحه «فهرست بها»، فایل Excel را Import کنید.

فایل آماده‌شده:

`Fehrest_Baha_Powerline_Ready.xlsx`

شیت اصلی داده برای Import:

`Import - Powerline`

دو شیت اصلی فایل اولیه تغییر نکرده‌اند و شیت سوم فقط برای Import نرمال‌شده اضافه شده است.

ردیف‌های دارای کد رسمی در `price_list_items` ذخیره می‌شوند.
ردیف‌های کاهش/اضافه‌بها که در اکسل کد رسمی ندارند در `price_list_adjustments` ذخیره می‌شوند و کد ساختگی برای آنها تولید نمی‌شود.

## 3) ثبت شرایط دکل

در فرم دکل، «شرایط زمین» را تعیین کنید:

- دشت و تپه‌ماهور
- نیمه‌کوهستانی
- صعب‌العبور

همزمان سه پرچم جدید نیز در دیتابیس نگه‌داری می‌شود تا برای گزارش‌گیری قابل استفاده باشند:

`plain_terrain`, `semi_mountainous`, `mountainous`

## 4) ثبت شرایط بازدید

در فرم بازدید:

- نوع بازدید: پیمایشی یا صعودی
- تعداد نفرات
- شرایط زمین همان بازدید (در صورت نیاز Override شرایط دکل)

ولتاژ، تعداد مدار و باندل از خط خوانده می‌شود و شرایط زمین از بازدید/دکل استفاده می‌شود.

## 5) اتصال عملیات به فهرست‌بها

در جدول «بازدیدها» یا «دستورکارها»، یک رکورد را انتخاب کنید و «فهرست بها / متره» را بزنید.

برنامه از روی این اطلاعات ردیف مناسب فهرست‌بها را پیدا می‌کند:

`فعالیت + ولتاژ + تعداد مدار + باندل + نوع مسیر + نوع بازدید`

سپس کاهش/اضافه‌بهای مرتبط را از `price_list_adjustments` اعمال می‌کند؛ مانند:

- تیر چوبی/بتنی
- بازدید دو نفره
- دو باندل
- سه باندل
- چهار باندل، فقط در صورتی که در فایل فهرست‌بهای انتخاب‌شده Rule مربوط به آن وجود داشته باشد

## 6) ثبت متره و صدور صورت‌وضعیت

با «ثبت متره»، قیمت واحد و مبلغ Snapshot می‌شود؛ بنابراین تغییر فهرست‌بهای آینده روی متره قدیمی اثر نمی‌گذارد.

سپس در صفحه «صورت‌وضعیت‌ها» گزینه «صدور از متره‌ها» را بزنید، متره‌های موردنظر را انتخاب کنید و صورت‌وضعیت پیش‌نویس ایجاد می‌شود.

متره‌های انتخابی باید متعلق به یک قرارداد باشند.

مالیات مانند منطق فعلی پروژه با نرخ ۱۰٪ محاسبه می‌شود.

## جداول جدید

- `price_list_adjustments`
- `billing_measurements`
- `billing_measurement_adjustments`

ستون‌های جدید روی جداول قبلی:

- `price_list_items`: مشخصات تطبیق
- `towers`: شرایط زمین
- `inspections`: نوع بازدید، تعداد نفرات و شرایط زمین
- `invoice_items`: اتصال به متره و بازدید

## ترتیب استقرار

1. اجرای SQL
2. جایگزینی بک‌اند `Powerline`
3. Build و Deploy فرانت
4. Import فایل فهرست‌بها
5. تکمیل شرایط زمین دکل‌ها
6. تکمیل نوع بازدید/نفرات در بازدیدها
7. اتصال عملیات به فهرست‌بها و ثبت متره
8. صدور صورت‌وضعیت از متره‌ها
