# خلاصه وضعیت پروژه Powerline Web — نسخه 4.3.39

## درخواست این مرحله
- افزودن ستون قرارداد به تمام موجودیت‌های عملیاتی مرتبط با قرارداد، به‌جز انواع سیم‌ها (Conductors) که جدول مرجع است.
- نمایش ستون «قرارداد» در جدول‌های برنامه.
- افزودن کمبوباکس «قرارداد جاری» در هدر برنامه.
- انتخاب پیش‌فرض: آخرین قرارداد با وضعیت `active` بر اساس بزرگ‌ترین `id`.
- با تغییر قرارداد، اطلاعات ماژول‌های عملیاتی و داشبورد بر اساس قرارداد انتخاب‌شده نمایش داده شوند.

## تغییرات انجام‌شده
### دیتابیس
Migration جدید:
`database/migration_v4.3.39_contract_scope_all.sql`

همچنین نسخه قابل اجرای مستقیم در ریشه پروژه:
`CONTRACT_SCOPE_SQL_V4.3.39.sql`

جدول‌های Scope شده:
- lines
- towers
- circuits
- personnel
- equipment
- inspections
- defects
- work_orders
- safety_incidents
- price_lists
- invoices

`conductors` عمداً مستثنا است.

Migration در برابر اجرای مجدد مقاوم است، ایندکس اضافه می‌کند و قبل از FK، `contract_id`های orphan را به `NULL` تبدیل می‌کند تا ایجاد FK شکست نخورد.

### Backend
پارامتر سراسری `contract_id` برای GETهای ماژول‌های عملیاتی در نظر گرفته شده است.
فیلتر قرارداد برای خطوط، دکل‌ها، مدارها، پرسنل، تجهیزات، بازدیدها، عیوب، دستورکارها، حوادث ایمنی، فهرست بها و صورت‌وضعیت‌ها اضافه شده است.
Endpointهای `towers/nearby` و `lines/{id}/towers` نیز Scope قرارداد را رعایت می‌کنند.
Dashboard stats و recent defects نیز قرارداد جاری را لحاظ می‌کنند.

### Frontend
در `DashboardLayout` کمبوباکس «قرارداد جاری» اضافه شده است.
قراردادهای فعال از `/contracts` خوانده می‌شوند و آخرین قرارداد فعال به‌صورت پیش‌فرض انتخاب می‌شود.
با تغییر قرارداد، صفحه جاری با `key` جدید Remount می‌شود تا اطلاعات همان قرارداد مجدداً خوانده شوند.
ApiClient برای GETهای Scope شده، `contract_id` قرارداد جاری را خودکار اضافه می‌کند.

ستون «قرارداد» به جدول‌های خطوط، مدارها، دکل‌ها، عیوب، پرسنل، تجهیزات، بازدیدها، دستورکارها، حوادث، فهرست بها و صورت‌وضعیت‌ها اضافه/استاندارد شده است.
انواع سیم‌ها Contract Scope ندارند.

## نکته مهم تست
PHP با `php -l` بررسی شده و خطای Syntax ندارد.
Build/TypeScript کامل در این محیط قابل تأیید نیست چون وابستگی‌های Node (`next`, `react`, ...) در `node_modules` نصب نیستند؛ خطاهای `tsc` عمدتاً مربوط به نبود dependencyها هستند.

## اجرای دیتابیس
قبل از اجرا از دیتابیس Backup بگیرید و فایل زیر را اجرا کنید:
`database/migration_v4.3.39_contract_scope_all.sql`

## فایل خروجی
ZIP نهایی: `Github_fixed_powerline_v4.3.39.zip`
