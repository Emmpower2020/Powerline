# خلاصه پروژه Powerline Web — نسخه 4.3.40

تاریخ: 2026-09-02

## تغییرات این مرحله
- کمبوباکس «قرارداد جاری» فقط **عنوان قرارداد** را نمایش می‌دهد؛ کد قرارداد و متن کمرنگ پیمانکار حذف شده است.
- قرارداد انتخاب‌شده در localStorage نگهداری می‌شود و بعد از تغییر، صفحه reload می‌شود تا همه ماژول‌ها اطلاعات قرارداد جدید را از API دریافت کنند.
- اگر قرارداد ذخیره‌شده معتبر باشد همان باقی می‌ماند؛ در اولین اجرا پیش‌فرض، آخرین قرارداد فعال بر اساس بزرگ‌ترین ID است.
- در عملیات گروهی جدول‌های دارای `contract_id` گزینه **«تغییر قرارداد»** اضافه شده است و با ContractSelect می‌توان چند ردیف را یکجا به قرارداد دیگری منتقل کرد.
- این قابلیت برای خطوط، دکل‌ها، مدارها، پرسنل، تجهیزات، بازدیدها، دستورکارها، عیوب، حوادث ایمنی، فهرست‌ها/صورت‌وضعیت‌های قراردادی و سایر ماژول‌های دارای `contract_id` فعال شده است.
- انواع سیم‌ها (`conductors`) عمداً از قرارداد و تغییر گروهی قرارداد مستثنا هستند.
- جداول مرجع مانند پیمانکاران، سازمان، لاگ ممیزی و انواع سازه/کد دکل قراردادمحور نیستند.
- برای خطوط و دکل‌ها، کنار عملیات گروهی اختصاصی موجود، کنترل عمومی تغییر قرارداد اضافه شده است.
- برای عیوب نیز کنار عملیات گروهی اختصاصی، کنترل عمومی تغییر قرارداد اضافه شده است.

## SQL این مرحله
فایل `database/migration_v4.3.40_set_all_contracts_to_1.sql` همه رکوردهای جداول عملیاتی دارای `contract_id` را به قرارداد ID=1 منتقل می‌کند. `conductors` در آن نیست. همچنین `contract_price_list_items` به قرارداد 1 منتقل می‌شود.

## نسخه
- APP_VERSION: v4.3.40
- package.json: 4.3.40
- VERSION.md: 4.3.40

## نکته تست
وابستگی‌های Node در محیط بررسی وجود نداشت؛ بنابراین build کامل Next.js در این محیط اجرا نشد. تغییرات TypeScript/JS به‌صورت استاتیک و ساختاری بررسی شده‌اند.
